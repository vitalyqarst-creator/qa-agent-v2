[project]
name = "qa-test-case-runtime"
version = "1.0.0"
description = "Lean runtime contracts for producing executable test cases from functional requirements."
requires-python = ">=3.11"
dependencies = [
    "PyMuPDF>=1.27,<2",
]

[tool.unittest]
start-directory = "tests"
