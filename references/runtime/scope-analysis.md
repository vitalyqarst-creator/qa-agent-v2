# Контракт анализа области проверки

Анализатор создаёт модель одного раздела, не перечень TC.

## Обязательные артефакты

- `source-row-inventory.md`;
- `scope-brief.md`;
- `coverage-gaps.md`;
- `test-data-plan.md`;
- `prompt.scope-to-writer.md`;
- `workflow-state.yaml` со ссылкой `clarification_register: "work/scope-clarification-requests.md"`.

Count — `0`; следуй validator-флагам. `source_anchor_repair_allowed` при count `2`–`4` сохраняет count: меняется только `Источник` (точный код/цитата), без изменения ID/смысла/контракта/GAP/данных. Остальные коррекции определяют возвращённые флаги; `quality_findings` их не блокируют. Переход — `writer_allowed=true`.

## Границы источника

В `scope-brief.md` используй contract-шаблоны `boundaries` и `parent_ownership`.

Обязательны `Выбранный раздел`, вводный и завершающий текст родительского раздела. Решение: `Включён`, `Распределён`, `Ранее покрыт: <область>` или `Не применимо: <причина>`.

Диапазон кодов структурного якоря охватывает все кодированные источники активного инвентаря. Для выбранного раздела укажи первый и последний фактические `SR-*` без устаревшего диапазона.

Нормативный фрагмент вне выбранного раздела, включая общее ограничение, помечай `Распределён`, не `Включён`. `selected_requirement_catalog` требует SR/GAP/исключение; parent/incoming — ownership-строки с `| Родительская обязанность |`. Пустой parent: `Нормативные родительские обязанности отсутствуют.`.

## Инвентарь и проверяемость

Каждая `SR-*` встречается однажды и содержит один объект/UI-уровень, действие и результат. Успех и отказ разделяй; невалидные классы с одинаковым отказом параметризуй. Альтернативные ключи поиска или способы ввода (`A или B`) сохраняй как разные классы данных. Неизвестный остаток не блокирует известное; результат без точки наблюдения получает GAP `нет-точки-наблюдения`.

До GAP `нет-точки-наблюдения` выполни один bounded search supporting execution anchors в том же основном XHTML: по действию, объекту и переходу проверь родительские, контейнерные и соседние таблицы действий. Найденный фрагмент пометь `Распределён` для конкретных `SR-*`, не создавая новую активную обязанность. Недоступность Figma сама по себе не blocker, если текст и локальный visual input дают путь и oracle.

При удалении или перенумерации `SR-*` во время коррекции найди этот ID во всех файлах текущего handoff и обнови каждую ссылку. Не записывай сплошной диапазон через отсутствующий ID: используй отдельные непрерывные сегменты либо явный список.

Если обязательное поле системно предзаполняется и недоступно для редактирования, не проектируй недостижимое пустое состояние. Покрой непустое предзаполнение и read-only; отдельный негатив допустим только при подтверждённом способе получить пустое значение.

Если один инициирующий выбор заполняет несколько обязательных зависимых read-only полей, сохрани отдельные атомарные `SR-*`, но в плане данных зафиксируй общее `REL-*` зависимого заполнения. Writer сможет объединить их в одну достижимую проверку `зависимая-обязательность`; analyzer не создаёт отдельные GAP только из-за невозможности очистить один output.

Строка таблицы — anchor, не доказательство полноты. Сверь обязательность, редактируемость, ввод и примечание. Независимые результаты получают свои `SR-*`. Автозаполнение может доказать непустоту; редактируемость, формат и валидация самостоятельны. Разнородные свойства перечисляй явными ID.

Макет конкретизирует UI заданного результата, но не создаёт бизнес-логику. Расхождение с однозначным текстовым требованием фиксируй в визуальной сверке, не в GAP/вопросе. GAP нужен только для неизвестной семантики/полноты.

Для обязательного поля нейтральный результат: объект с пустым полем не сохраняется. Точную UI-реакцию утверждай только по источнику.

Отменённые обязанности перечисли в `Применённые исключения`, без активных `SR-*`/GAP.

## Условные контроли

### Строки таблицы

Если область использует таблицу ФТ, используй шаблон `table_rows`.

Перечисли все строки первого столбца XHTML, копируя их из `source_table_catalog` публичного контракта без ручной транскрипции. Решение: `Включена`, `Передана`, `Не применимо` или `Исключена утверждённым ответом`, с областью/причиной/источником.

Для коротких буквенных заголовков используй шаблон `opaque_headers`.

Значение требует явной легенды или утверждённого support; иначе `Не определено` и GAP.

### Визуальная сверка

Для каждого включённого UI-уровня используй шаблон `visual_crosscheck`.

Указывай зарегистрированный путь/Figma URL, подтверждённые подписи, механику и различия буквально.

Не дублируй реестр locator-а и нерелевантные макеты. Если локального evidence недостаточно и Figma зарегистрирована, используй её один раз либо запиши причину недоступности. Без UI укажи причину отсутствия сверки.

### Проверка согласованности

Для применимых аспектов используй шаблон `consistency`.

Допустимые аспекты: `Идентичность объекта`, `Представления объекта`, `Создание, редактирование и повторное открытие`, `Роли и видимость`, `Статусы и переходы`, `Поля, справочники и внешние источники`, `История изменений и аудит`.

Каждая строка фиксирует вывод и ссылки `SR-*`/`GAP-*`. Неприменимые аспекты пропускаются; повторная сверка выполняется только по skill.

### Зависимые результаты

Добавь `Контроль зависимых результатов` только для фактической причинности; общий source anchor её не доказывает:

| Причинная обязанность | Зависимая обязанность | Связь | Обработка неопределённости |
| --- | --- | --- | --- |

Открытый GAP причины даёт зависимому результату свой GAP либо `Независима: <source-backed причина>`.

## Пробелы и вопросы

GAP связан с одной SR и только её неизвестным остатком. Gate частичной тестируемости требует отдельных SR для известных результатов того же утверждения. Классы: `неоднозначность-требования`, `противоречие-источников`, `нет-бизнес-результата`, `нет-точки-наблюдения`.

Закрытый GAP пометь `Закрыт: <решение>`; не передавай writer. При отмене обязанности удали SR/GAP; история остаётся в реестре.

Единый реестр пакета — `work/scope-clarification-requests.md`. Формат карточки:

```markdown
## CLR-9.3.1-001 — Краткая тема

**Область проверки:** `9.3.1-partner-directory`
**Статус:** `ожидает-ответа`
**Вопрос:** Один однозначный вопрос.
**Основание в ФТ:** Точный source anchor.
**Влияние на покрытие:** Один связанный GAP и неизвестный результат.
**Ответ БА:** _Введите ответ здесь._
```

Статусы: `ожидает-ответа`, `частичный-ответ`, `ответ-получен`, `отменён`. Для частичного ответа укажи `Осталось уточнить`, для support — `Источник ответа`. Вопросы не удаляй/дублируй. Один вопрос закрывает несколько GAP только при общем правиле; перечисли их anchors.

`working-assumption` с текущим поведением временно закрывает GAP. Конечная выборка не закрывает требование «неограниченно»: сохраняй `частичный-ответ`, `SR-*` и GAP до замены границы.
