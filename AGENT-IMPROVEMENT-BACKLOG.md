# Backlog улучшений агента

Это единственный журнал согласованных изменений agent-layer. Он не является
входом practical route и не содержит задач по конкретному FT-scope. Для каждой
записи указаны источник, приоритет и условие завершения; идеи без наблюдаемой
причины сюда не добавляются.

Приоритеты: `P1` — исправить до следующего затронутого practical запуска;
`P2` — планировать после текущего выпуска тест-кейсов; `P3` — legacy-долг или
исследование, не блокирующее practical v0.9.

Правило ведения: если по итогам проверки вместо немедленного изменения
предлагается улучшение agent-layer, оно добавляется в этот backlog в той же
итерации с наблюдаемым основанием и критерием завершения. Исключение — идея
без подтверждённой причины; её не представляют как согласованную работу.

## Незакрытые улучшения practical v0.9

- [ ] `AGI-014` (`P2`) Разделить совместимость source package manifest и
  версию запускающего инструмента.
  - Основание: повторная materialization scope `9.3.2` получила единственное
    transport-warning только потому, что source manifest создан v0.9.7, хотя
    структура и хеши входов совместимы с v0.9.8.
  - Завершено, когда: validator предупреждает только о несовместимой schema
    или изменившихся source inputs; безопасное обновление agent-layer без
    изменения source package не создаёт шумного warning.

- [x] `AGI-015` (`P2`) Не оставлять устаревшую status-проекцию workflow после
  принятого matrix re-review или выпуска TC.
  - Основание: в scope `9.3.2` после verdict `approved` и перехода к
    `phase: test-cases` поле `final_verdict` осталось `changes-required` от
    предыдущего matrix review, а после выпуска TC `next_action` продолжает
    говорить «Написать тест-кейсы»; это вводит в заблуждение человека и
    статусный renderer.
  - Завершено, когда: `final_verdict` описывает только final TC review, а
    результат matrix review хранится в `reviews`; `next_action` отражает
    фактическую следующую фазу; переходы и renderer не показывают устаревший
    verdict как финальный.
  - Выполнено: `final_verdict` нормализуется и относится только к final TC
    review; после matrix approval он сбрасывается в `not-finalized`. После
    записи TC workflow обязан перейти в `phase: review`; validator выявляет
    оставшийся writer-phase. Добавлены migration и transition regressions.

- [x] `AGI-016` (`P1`) Добавить practical writer-gate для исполнимости
  тестовых данных и полноты trigger-шагов.
  - Основание: в первом выпуске TC scope `9.3.2` восемь кейсов содержат
    тавтологичное «Данные, предусмотренные проверяемым правилом», а два кейса
    про второй файл не имеют отдельного шага прикрепления первого файла.
    Scoped validator не выявил эти дефекты.
  - Завершено, когда: practical v0.9 требует в поле «Тестовые данные» либо
    конкретные literals/значения, либо точные свойства и способ подготовки
    отсутствующего набора; запрещает пересказ правила как данные; все
    действия, создающие состояние trigger, перечислены отдельными шагами.
    Добавлены узкие validator-regressions и проверка reviewer-ом на реальном
    scope.
  - Выполнено: scoped validator v0.9.11 блокирует tautological test data и
    неполный trigger для явного ограничения «не более одного файла»; правило
    закреплено в canonical route и practical skill, добавлены regression tests.

- [x] `AGI-017` (`P1`) Разделить бюджеты содержательных доработок matrix и
  canonical TC.
  - Основание: scope `9.3.2` израсходовал общий `revision_count` на matrix
    finding MR-001. В `finalize_practical_review.py` следующий blocking finding
    final TC review при `revision_count >= 1` сразу переводит scope в `blocked`,
    хотя practical route требует одну целевую TC-доработку после final review.
  - Завершено, когда: matrix review имеет не более одной собственной
    matrix-доработки и re-review, final TC review — не более одной собственной
    TC-доработки и fresh final review; workflow migration, validator, renderer
    и regression tests исключают новый repair-loop и не блокируют TC-revision
    только потому, что ранее исправлялась matrix.
  - Выполнено: `matrix_revision_count` и `tc_revision_count` имеют независимые
    лимиты; legacy `revision_count` мигрируется детерминированно по истории
    review при следующем чтении/финализации. Добавлены regression tests для
    миграции, первого TC finding после matrix revision и второго TC finding.

- [ ] `AGI-018` (`P1`) Убрать лексическую зависимость validator-а от слов
  «первый» и «второй» при проверке ограничения на один прикрепляемый файл.
  - Основание: при writer-доработке scope `9.3.2` шаги «прикрепить
    `решение-1.pdf`» и «попытаться прикрепить `решение-2.pdf`» уже однозначно
    задавали начальное состояние и вторую попытку. Validator ошибочно выдал
    два blocking finding только из-за отсутствия буквально слова «второй».
    Агенту пришлось менять формулировку без изменения проверки.
  - Завершено, когда: validator распознаёт две упорядоченные самостоятельные
    операции прикрепления с различающимися файлами без требования порядковых
    слов; регрессии покрывают вариант с явными «первый/второй», вариант с
    разными именами файлов без этих слов и отрицательный случай без начального
    прикрепления.

- [ ] `AGI-001` (`P2`) Сохранить безопасный запас контекста для
  `source_locator.discovery`.
  - Основание: architecture audit, 129.8 KiB из 132 KiB; запас 2.2 KiB при
    минимально допустимых 15 KiB.
  - Завершено, когда: редко используемые references перенесены в conditional
    loading либо предел повышен с обоснованием, а audit не выдаёт warning.

- [ ] `AGI-002` (`P2`) Принять переносимую политику UI evidence.
  - Основание: `instruction-contract-index.md`, Current Cleanup Priorities;
    текущие локальные `output/` paths не дают переносимого набора traces и
    screenshots.
  - Завершено, когда: определён один portable format/manifest, UI-prep умеет
    его выпускать, а policy проверена на одном реальном scope.

- [ ] `AGI-003` (`P3`) Оценить необходимость новых категорий решений БА после
  второго независимого FT-пакета.
  - Основание: практический контракт `BA-DEC-*` проверен только на Partners-v1.
  - Завершено, когда: есть второй пакет и решение — сохранить текущие три типа
    либо добавить ровно один обоснованный тип с регрессионным тестом.

- [ ] `AGI-004` (`P3`) Проверить на нескольких новых FT-пакетах, нужен ли
  дополнительный semantic validator ожидаемого результата.
  - Основание: `instruction-contract-index.md`, Current Cleanup Priorities.
  - Завершено, когда: собраны повторяемые reviewer findings; для устойчивого
    дефекта создан узкий validator/eval. Не вводить правило по одному кейсу.

## Архитектурный и legacy-долг

- [ ] `AGI-005` (`P2`) Устранить orphan
  `references/agent/practical-stage-summary-template.md`.
  - Основание: architecture audit `orphan-reference:practical-stage-summary-template.md`.
  - Завершено, когда: reference удалён как устаревший или связан только с
    действующим маршрутом; audit не сообщает orphan.

- [ ] `AGI-006` (`P3`) Разобрать дублирование, отмеченное audit как possible.
  - Основание: повторения правил о DOCX, UTF-8 и карте skills между source
    locator и scope analyzer.
  - Завершено, когда: для каждого из трёх предупреждений зафиксировано
    решение «допустимое краткое напоминание» или выполнен перенос в один
    canonical reference. Не удалять нужный операционный контекст ради метрики.

- [ ] `AGI-007` (`P3`) Сократить legacy instruction budgets или явно вывести
  legacy-сценарии из active manifest.
  - Основание: audit 2026-08-12: `scope.manual` превышен; near-limit у
    `reviewer.practical_v0_8`, `scope.bounded_production`,
    `writer.initial_draft.table.deep_debug`, `writer.remediation.style`,
    `writer.session_format_revision`, `architecture.audit`.
  - Завершено, когда: каждый сценарий либо имеет требуемый запас, либо
    помечен legacy/conditional и не загружается для practical v0.9.

- [ ] `AGI-008` (`P3`) Принять решение по числу active skills.
  - Основание: audit `skills-count-out-of-range`: 8 skills при целевом
    диапазоне 4–7.
  - Завершено, когда: доказано, что отдельные роли необходимы, либо объединены
    только действительно пересекающиеся skills; не объединять навыки ради
    формального попадания в диапазон.

## Не является улучшением агента

- Выпуск matrix, независимого review и тест-кейсов для scope 9.3.2 — это
  рабочий маршрут FT-пакета, а не backlog agent-layer.

## Завершённые улучшения

- [x] `AGI-013` Исправлен приоритет статуса исполнения из нескольких
  `SETUP-*`.
  - Первичный статус выбирается в порядке: `needs-future-clarification`,
    `blocked-observability`, `needs-test-data`, `candidate-ui-calibration`.
    Отсутствующие данные больше не маскируются UI-калибровкой.
  - Регрессии покрывают все пары availability-status и связку actor плюс
    navigation; повторная проверка scope `9.3.2` находит все пять строк,
    указанных независимым reviewer-ом.

- [x] `AGI-009` Явные контексты исполнения и атомарные потоки matrix/TC.
  - В `scope-obligations.json` добавлены `execution_contexts` (`CTX-*`) и
    каталог `execution_setups` (`SETUP-*`); matrix/TC обязаны сохранять одну
    пару OBL/CTX. Регрессия проверяет, что второй поток не теряется.
- [x] `AGI-010` Статус исполнения выводится из полной цепочки предпосылок.
  - Matrix содержит поля «Проверяемое правило», «Ожидаемый результат» и
    «Нужные предпосылки»; validator сопоставляет status с actor/fixture/
    integration/initial-state из `SETUP-*`.
- [x] `AGI-011` Предварительная проверка доставки immutable review inputs.
  - Добавлены `practical_review_input_snapshot.py`, SHA-256 preflight
    целевого checkout и read-only snapshot для другого окружения; regression
    покрывает missing worktree input и подмену copy.
- [x] `AGI-012` Неизменяемое сохранение результата reviewer-а.
  - Добавлен byte-for-byte capture raw JSON; исправлен ложный mojibake detector
    для легитимной формулировки «столбец Р»; regression запрещает перезапись
    и подтверждает отсутствие семантической нормализации controller-ом.

- [x] `AGI-BA-001` Пакетный реестр утверждённых решений БА с ограниченной
  областью действия.
- [x] `AGI-BA-002` Обязательный `CLR-*` для неразрешённого противоречия ФТ и
  ожидаемого практического поведения.
- [x] `AGI-BA-003` Исключение отменённого BA-решением обязательства из matrix,
  TC и review.
- [x] `AGI-BA-004` Регрессионные тесты и проверка на scope 9.3.2: OBL-008/013
  исключены, 50 активных OBL, проверка структуры — без замечаний.
