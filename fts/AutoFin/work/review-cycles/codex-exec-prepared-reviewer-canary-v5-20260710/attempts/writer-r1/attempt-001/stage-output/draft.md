# Тест-кейсы

**Статус:** unsigned draft  
**package_id:** `autofin-widget-selection-prepared-v2`  
**Scope:** `iteration-smoke-widget-selection-types`  
**Раздел ФТ:** `3`  
**targeted_source_fallback:** `not_used`

## TC-PREP-001

**Название:** Ограничение одиночного выбора в виджете типа «Список»  
**Тип:** позитивный  
**Приоритет:** средний  
**package_id:** `autofin-widget-selection-prepared-v2`  
**Трассировка:** `ATOM-PREP-001` (`SRC-001`)

### Runtime metadata

```yaml
runtime_fixture:
  widget_locator: "Точный UI-локатор доступного виджета типа «Список», переданный средой выполнения"
  first_value: "Первое доступное значение фактического справочника виджета"
  second_value: "Второе доступное значение того же справочника, отличное от first_value"
minimum_fixture_requirements:
  distinct_available_values: 2
persistence_check: false
```

### Предусловия

1. Среда выполнения передала `widget_locator`, который однозначно указывает на доступный для взаимодействия виджет типа «Список».
2. В фактическом справочнике виджета доступны как минимум два разных значения, переданные как `first_value` и `second_value`.
3. Пользователь открыл UI по переданному локатору; сохранение данных не выполнялось.

### Тестовые данные

- `widget_locator` — точный локатор виджета из runtime fixture.
- `first_value` — доступное значение фактического справочника.
- `second_value` — другое доступное значение того же справочника.

### Шаги

1. Открыть список значений виджета по `widget_locator`.
2. Выбрать `first_value`.
3. Повторно открыть список значений этого же виджета и выбрать `second_value`.

### Итоговый ожидаемый результат

После последовательного выбора двух разных доступных значений виджет содержит ровно одно выбранное значение.

### Постусловия

- Сохранение данных не выполняется; проверка и восстановление состояния в API, БД или ином хранилище не применимы к данному UI scope.

## TC-PREP-002

**Название:** Выбор нескольких значений в виджете типа «Список с множественным выбором»  
**Тип:** позитивный  
**Приоритет:** средний  
**package_id:** `autofin-widget-selection-prepared-v2`  
**Трассировка:** `ATOM-PREP-002` (`SRC-002`)

### Runtime metadata

```yaml
runtime_fixture:
  widget_locator: "Точный UI-локатор доступного виджета типа «Список с множественным выбором», переданный средой выполнения"
  first_value: "Первое доступное значение фактического справочника виджета"
  second_value: "Второе доступное значение того же справочника, отличное от first_value"
minimum_fixture_requirements:
  distinct_available_values: 2
persistence_check: false
```

### Предусловия

1. Среда выполнения передала `widget_locator`, который однозначно указывает на доступный для взаимодействия виджет типа «Список с множественным выбором».
2. В фактическом справочнике виджета доступны как минимум два разных значения, переданные как `first_value` и `second_value`.
3. Пользователь открыл UI по переданному локатору; в виджете не выбраны `first_value` и `second_value`, сохранение данных не выполнялось.

### Тестовые данные

- `widget_locator` — точный локатор виджета из runtime fixture.
- `first_value` — доступное значение фактического справочника.
- `second_value` — другое доступное значение того же справочника.

### Шаги

1. Открыть список значений виджета по `widget_locator`.
2. Выбрать `first_value`.
3. Выбрать `second_value`, не отменяя выбор `first_value`.

### Итоговый ожидаемый результат

После выбора двух разных доступных значений `first_value` и `second_value` одновременно отображаются как выбранные.

### Постусловия

- Сохранение данных не выполняется; проверка и восстановление состояния в API, БД или ином хранилище не применимы к данному UI scope.

## TC-PREP-003

**Название:** Начальное пустое состояние нового виджета  
**Тип:** позитивный  
**Приоритет:** средний  
**package_id:** `autofin-widget-selection-prepared-v2`  
**Трассировка:** `ATOM-PREP-003` (`SRC-003`)

### Runtime metadata

```yaml
runtime_fixture:
  widget_locator: "Точный UI-локатор подходящего нового виджета, переданный средой выполнения"
  initial_state: "Новый виджет до первого взаимодействия пользователя"
interaction_before_check: false
null_representation_check: false
```

### Предусловия

1. Среда выполнения передала `widget_locator`, который однозначно указывает на подходящий новый виджет.
2. Для виджета обеспечено состояние `initial_state`: пользователь ещё не выбирал и не вводил в нём значение.

### Тестовые данные

- `widget_locator` — точный локатор нового виджета из runtime fixture.
- `initial_state` — состояние виджета до первого взаимодействия пользователя.

### Шаги

1. Открыть UI по `widget_locator`, не взаимодействуя с виджетом, и проверить его отображаемое значение.

### Итоговый ожидаемый результат

До первого взаимодействия с новым виджетом в нём отсутствует выбранное значение.

### Постусловия

- Значение виджета не изменено; проверка внутренней интерпретации пустого значения как `NULL` не выполняется.

## Непокрытое исполнимыми тестами обязательство

- `ATOM-PREP-004` / `GAP-PREP-001` (`SRC-003`): внутренняя интерпретация отсутствующего значения как `NULL` не имеет наблюдаемого артефакта в подтверждённом UI scope. Исполнимый UI-тест не создан; для проверки требуется отдельный подтверждённый API/БД/persistence scope.

