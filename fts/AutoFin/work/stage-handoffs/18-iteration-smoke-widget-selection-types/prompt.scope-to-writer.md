# Prompt: Scope To Writer

This prompt is preserved only as a compatibility artifact. The active route for this task is `prompt.scope-to-iteration.md` and the session-based runner.

Writer inputs are the same confirmed scope artifacts listed in `prompt.scope-to-iteration.md`. Writer work must happen inside a runner-started writer SDK session, not in the orchestrator session.
