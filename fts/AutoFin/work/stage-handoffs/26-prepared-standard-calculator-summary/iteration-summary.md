# Итог prepared-standard calculator-summary

## Что реализовано

- Prepared transport отделён от fast semantic policy.
- `standard-required` package запускает полный `writer.session_initial_draft` и `reviewer.semantic_traceability_test_design`.
- Writer/reviewer получают role-specific manifests и context budgets.
- Deterministic readiness блокирует unresolved angle-bracket fixtures до reviewer.
- Evidence policy различает requirement access, read-only contamination checks и bounded technical scripts inventory.
- Reviewer JSON Schema связывает exact `OBL -> ATOM` и допустимый verdict с coverage status.

## Live результат

| run | result | total | quality |
| --- | --- | ---: | --- |
| v5 | `changes-required` | 523.141 s | 2 actionable findings |
| v7 | `accepted-not-promoted` | 504.156 s | 5/5 testable obligations; 1 gap preserved |

- Median: `513.648 s` / `8.561 min`.
- Speedup относительно raw standard v4: `2.37x`.
- Primary context сокращён более чем на 99%.
- Цель `<= 8 min` не достигнута на `33.648 s`; следующий performance target — instruction loading.

## Source correction

- Active calculator traceability исправлена на `BSR 43-46` по зарегистрированному `FT4AutoFinFinal.xhtml`.
- `BSR 46` разделён на window opening, observable prefill presence и отдельный exact-mapping gap.
- Handoff 05 всё ещё ссылается на `AutoFinPreFinal`; перед broader rollout требуется canonical scope/parity rebase.

## Production decision

Promotion не выполнялся. Существующий baseline не изменён; overwrite запрещён. V7 terminal replay отклонён, state hash не изменился.

## Canonical report

- `evals/prepared-standard-route/20260711/calculator-summary-runtime-report.md`.
