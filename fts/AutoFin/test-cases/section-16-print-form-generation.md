# Тест-кейсы: формирование печатной формы заявления-анкеты

## Metadata

| field | value |
| --- | --- |
| `ft_slug` | `AutoFin` |
| `scope_slug` | `print-form-generation` |
| `legacy_section_id` | `section-16` |
| `ft_section` | `4.4 Требования к формированию печатных форм документов` |
| `source_ft` | `FT4AutoFinFinal.xhtml`; `FT4AutoFinFinal.docx`; `FT4AutoFinFinal.pdf` |
| `canonical_test_design_dir` | `fts/AutoFin/work/test-design/section-16-print-form-generation` |
| `terminal_status` | `signed-off` |

## Scope Boundaries

Набор покрывает только формирование и содержимое печатной формы `Заявление-анкета на получение потребительского кредита` по разделу `4.4`: шаблон, привязку к типу тегов, точное соответствие тегов и заполнение полей/чек-боксов в PDF по данным клиента из Системы.

Не покрываются: UI-путь запуска генерации, роли, статусы заявки, API/RabbitMQ/БД, валидация исходных полей, правила сохранения исходных данных и поведение раздела `4.3`, кроме использования его данных как уже подготовленной фикстуры.

## Source Notes

- В разделе `4.4` нет BSR-кодов. Трассировка ведется через `ATOM-PFG-*`, строку таблицы и источник.
- Приоритетный источник требований: `FT4AutoFinFinal.xhtml`.
- Значение шаблона `Анкета клиента 04.02.2026.pdf` подтверждено `FT4AutoFinFinal.pdf`, page 36; в XHTML соответствующая ячейка пустая.
- Точное написание тегов сверено по `FT4AutoFinFinal.docx`, table 9, column `Тег`, потому что XHTML/PDF содержат визуальные переносы тегов.

## Coverage Summary

| package_id | atom_id | source_ref | test_case_id | coverage_status |
| --- | --- | --- | --- | --- |
| `WP-01` | `ATOM-PFG-001` | `4.4 paragraph; PDF p.36` | `TC-AF-PFG-001` | `covered` |
| `WP-01` | `ATOM-PFG-002` | `4.4 document table; PDF p.36` | `TC-AF-PFG-001`; `TC-AF-PFG-002` | `covered` |
| `WP-01` | `ATOM-PFG-003` | `4.4 paragraph before tag table` | `TC-AF-PFG-002` | `covered` |
| `WP-01` | `ATOM-PFG-004` | `4.4 paragraph before tag table` | `TC-AF-PFG-002`; `TC-AF-PFG-016` | `covered` |
| `WP-02` | `ATOM-PFG-005` | `4.4 tag rows 1-4` | `TC-AF-PFG-003` | `covered` |
| `WP-02` | `ATOM-PFG-006` | `4.4 tag rows 5-19` | `TC-AF-PFG-004` | `covered` |
| `WP-02` | `ATOM-PFG-007` | `4.4 tag row 18` | `TC-AF-PFG-005` | `covered` |
| `WP-02` | `ATOM-PFG-008` | `4.4 tag row 20` | `TC-AF-PFG-006` | `covered` |
| `WP-02` | `ATOM-PFG-009` | `4.4 tag rows 21-29` | `TC-AF-PFG-007` | `covered` |
| `WP-02` | `ATOM-PFG-010` | `4.4 tag rows 30-33` | `TC-AF-PFG-008` | `covered` |
| `WP-02` | `ATOM-PFG-011` | `4.4 tag rows 34-39` | `TC-AF-PFG-009` | `covered` |
| `WP-03` | `ATOM-PFG-012` | `4.4 tag rows 40-43` | `TC-AF-PFG-010` | `covered` |
| `WP-03` | `ATOM-PFG-013` | `4.4 tag rows 44-51` | `TC-AF-PFG-011` | `covered` |
| `WP-03` | `ATOM-PFG-014` | `4.4 tag rows 52-56` | `TC-AF-PFG-012` | `partial-gap` |
| `WP-03` | `ATOM-PFG-015` | `4.4 tag rows 57-60` | `TC-AF-PFG-013` | `covered` |
| `WP-04` | `ATOM-PFG-016` | `4.4 tag rows 61-87` | `TC-AF-PFG-014`; `TC-AF-PFG-015` | `covered` |

## Test Cases

### TC-AF-PFG-001

**Название:** Автоматическое формирование заявления-анкеты по шаблону `Анкета клиента 04.02.2026.pdf`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-PFG-001`; `ATOM-PFG-002`; `4.4 Требования к формированию печатных форм документов`; PDF page 36

#### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-BASE`.
- Пользователю доступен штатный способ сформировать печатную форму заявления-анкеты.

#### Тестовые данные

- Печатная форма: `Заявление-анкета на получение потребительского кредита`.
- Шаблон: `Анкета клиента 04.02.2026.pdf`.

#### Шаги

1. Открыть подготовленную заявку.
2. Сформировать печатную форму заявления-анкеты доступным в системе способом.
3. Открыть сформированный PDF.

#### Итоговый ожидаемый результат

Сформирован PDF `Заявление-анкета на получение потребительского кредита` по шаблону `Анкета клиента 04.02.2026.pdf`, и данные клиента в PDF заполнены на основании данных этой заявки из Системы.

#### Постусловия

Сохранить сформированный PDF как evidence выполнения проверки.

### TC-AF-PFG-002

**Название:** Привязка шаблона к типу и точное соответствие тегов колонке `Тег`

**Тип:** Configuration

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-PFG-002`; `ATOM-PFG-003`; `ATOM-PFG-004`; `4.4 paragraph before tag table`; `DICT-PFG-001`

#### Предусловия

- Доступна конфигурация шаблона печатной формы или другой проверяемый артефакт настройки шаблона.

#### Тестовые данные

- Oracle точного написания тегов: `FT4AutoFinFinal.docx`, section `4.4`, table 9, column `Тег`.

#### Шаги

1. Открыть настройку шаблона `Анкета клиента 04.02.2026.pdf` для печатной формы заявления-анкеты.
2. Проверить, что шаблон привязан к типу, собранному на основе тегов из таблицы раздела `4.4`.
3. Проверить, что все плейсхолдеры шаблона имеют имена из `DICT-PFG-001`.
4. Проверить, что в шаблоне нет плейсхолдеров с визуально перенесенными именами из XHTML/PDF, например `<last_ n ame>`, `< d ividends>`, `<serial_number _1 >`.

#### Итоговый ожидаемый результат

Шаблон `Анкета клиента 04.02.2026.pdf` привязан к типу на основе таблицы тегов раздела `4.4`, а все теги шаблона точно совпадают с колонкой `Тег` из DOCX-версии Final.

#### Постусловия

Не требуются.

### TC-AF-PFG-003

**Название:** Заполнение блока `Параметры о кредите`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-PFG-005`; `4.4 tag rows 1-4`

#### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-BASE` с заполненными параметрами автомобиля и кредита.

#### Тестовые данные

- Теги блока: `<auto>`, `<loan_amount>`, `<monthly_payment>`, `<Loan term>`.

#### Шаги

1. Сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок `Параметры о кредите`.

#### Итоговый ожидаемый результат

В блоке `Параметры о кредите` значения тегов `<auto>`, `<loan_amount>`, `<monthly_payment>`, `<Loan term>` соответствуют данным автомобиля, суммы кредита, ежемесячного платежа и срока кредита из заявки.

#### Постусловия

Не требуются.

### TC-AF-PFG-004

**Название:** Заполнение персональных и паспортных данных заявителя

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-PFG-006`; `4.4 tag rows 5-19`

#### Предусловия

- В Системе есть заявка по фикстурам `FIX-PFG-BASE` и `FIX-PFG-FIO-CHANGED`.

#### Тестовые данные

- Теги персональных данных: `<last_name>`, `<first_name>`, `<middle_name>`, `<previous_last_name>`, `<previous_first_name>`, `<previous_middle_name>`, `<snils>`, `<code_word>`, `<inn>`.
- Теги действующего паспорта: `<serial_number>`, `<number>`, `<issue_date>`, `<issue_place>`.
- Тег иждивенцев: `<dependency>`.

#### Шаги

1. Сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок `Персональные данные заявителя`.

#### Итоговый ожидаемый результат

В блоке `Персональные данные заявителя` заполнены ФИО, прежние ФИО при `Клиент менял ФИО = Да`, СНИЛС, кодовое слово, ИНН, серия/номер/дата выдачи/кем выдан действующего паспорта и число иждивенцев в соответствии с данными заявки.

#### Постусловия

Не требуются.

### TC-AF-PFG-005

**Название:** Заполнение чек-бокса `Зарегистрированный брак`

**Тип:** Decision table

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-PFG-007`; `4.4 tag row 18`; `DICT-PFG-002`

#### Предусловия

- Подготовлены заявки по фикстуре `FIX-PFG-MARRIAGE` для двух состояний семейного положения.

#### Тестовые данные

| case | значение `Семейное положение` | ожидаемый чек-бокс |
| --- | --- | --- |
| 1 | `женат/замужем` | `Да` |
| 2 | любое другое значение | `Нет` |

#### Шаги

1. Для каждого значения из таблицы сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить чек-бокс `Зарегистрированный брак`.

#### Итоговый ожидаемый результат

Чек-бокс `Зарегистрированный брак` равен `Да` только при значении `женат/замужем`; во всех остальных проверяемых состояниях равен `Нет`.

#### Постусловия

Не требуются.

### TC-AF-PFG-006

**Название:** Заполнение чек-боксов `Социальный статус`

**Тип:** Decision table

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-PFG-008`; `4.4 tag row 20`; `DICT-PFG-003`

#### Предусловия

- Подготовлены заявки по фикстуре `FIX-PFG-SOCIAL-STATUS`.

#### Тестовые данные

Значения поля `Социальный статус`: `cобственник бизнеса/ИП`, `наемный работник`, `военнослужащий`, `пенсионер`, `самозанятый`.

#### Шаги

1. Для каждого значения социального статуса сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить группу чек-боксов `Социальный статус`.

#### Итоговый ожидаемый результат

Для выбранного значения социального статуса соответствующий чек-бокс в печатной форме равен `Да`; остальные чек-боксы этой группы не отмечены как `Да`.

#### Постусловия

Не требуются.

### TC-AF-PFG-007

**Название:** Заполнение блока `Адрес фактического проживания`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-PFG-009`; `4.4 tag rows 21-29`

#### Предусловия

- Подготовлены заявки по фикстурам `FIX-PFG-ADDR-SAME` и `FIX-PFG-ADDR-DIFFERENT`.

#### Тестовые данные

- Теги адреса: `<region_with_type>`, `<city_district>`, `<city>`, `<street>`, `<house>`, `<block>`, `<str>`, `<flat>`.

#### Шаги

1. Для каждой фикстуры сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок `Адрес фактического проживания`.

#### Итоговый ожидаемый результат

Теги фактического адреса заполнены значениями фактического адреса из заявки, а чек-бокс `Совпадает с адресом постоянной регистрации` равен `Да` при совпадении адресов и `Нет` при несовпадении.

#### Постусловия

Не требуются.

### TC-AF-PFG-008

**Название:** Заполнение блока `Контактная информация`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-PFG-010`; `4.4 tag rows 30-33`

#### Предусловия

- Подготовлены заявки по фикстурам `FIX-PFG-WORKING` и `FIX-PFG-NOT-WORKING-PENSIONER`.

#### Тестовые данные

- Теги: `<mobile_phone>`, `<email>`, `<work_phone>`, `<home_phone>`.

#### Шаги

1. Для каждой фикстуры сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок `Контактная информация`.

#### Итоговый ожидаемый результат

Мобильный телефон, email и домашний телефон заполнены из заявки; рабочий телефон выводится для занятости, отличной от `Пенсионер (не работает)`, и не выводится как доступное заполненное значение для `Пенсионер (не работает)`.

#### Постусловия

Не требуются.

### TC-AF-PFG-009

**Название:** Заполнение блока `Контактное лицо`

**Тип:** Decision table

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-PFG-011`; `4.4 tag rows 34-39`; `DICT-PFG-004`

#### Предусловия

- Подготовлены заявки по фикстуре `FIX-PFG-RELATION`.
- Для каждой заявки заполнены ФИО, дата рождения и мобильный телефон контактного лица.

#### Тестовые данные

Значения поля `Отношение к заявителю`: `супруг/супруга`, `отец/мать`, `сестра/брат`, `теща/свекровь/тесть/свекр`, `сын/дочь`, `друг/знакомый/коллега`, `иное`.

#### Шаги

1. Для каждого значения отношения к заявителю сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок `Контактное лицо`.

#### Итоговый ожидаемый результат

В блоке `Контактное лицо` заполнены теги `<last_name_contact>`, `<first_name_contact >`, `<middle_name_contact >`, `<date_of_birth_contact>`, `<mobile_phone_contact >`, а чек-бокс выбранного отношения к заявителю равен `Да`.

#### Постусловия

Не требуются.

### TC-AF-PFG-010

**Название:** Заполнение блока `Информация о работодателе`

**Тип:** Decision table

**Приоритет:** High

**package_id:** WP-03

**Трассировка:** `ATOM-PFG-012`; `4.4 tag rows 40-43`; `DICT-PFG-005`

#### Предусловия

- Подготовлены заявки по фикстуре `FIX-PFG-OPF`.
- В каждой заявке заполнены наименование и ИНН работодателя.

#### Тестовые данные

Значения поля `ОПФ`: `Гос. Предприятие (ГУП.ФГУП, МГУП, в/ч и пр.`, `ООО/АО`, `ИП`, `Прочее`.

#### Шаги

1. Для каждого значения ОПФ сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок `Информация о работодателе`.

#### Итоговый ожидаемый результат

Для выбранной ОПФ соответствующий чек-бокс работодателя равен `Да`, а теги `<name_main_job>` и `<inn_job>` заполнены наименованием и ИНН работодателя из заявки.

#### Постусловия

Не требуются.

### TC-AF-PFG-011

**Название:** Заполнение блока `Адрес местонахождения работодателя`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-03

**Трассировка:** `ATOM-PFG-013`; `4.4 tag rows 44-51`

#### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-BASE` с заполненным адресом работодателя.

#### Тестовые данные

- Теги адреса работодателя: `<region_with_type_job>`, `<city_district_job >`, `<city_job >`, `<street_job >`, `<house_job >`, `<block_job >`, `<str_job>`, `<office>`.

#### Шаги

1. Сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок `Адрес местонахождения работодателя`.

#### Итоговый ожидаемый результат

Теги адреса работодателя заполнены значениями региона, района, города, улицы, дома, корпуса, строения и офиса из заявки.

#### Постусловия

Не требуются.

### TC-AF-PFG-012

**Название:** Заполнение блока `Сведения о доходе и должности заявителя`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-03

**Трассировка:** `ATOM-PFG-014`; `4.4 tag rows 52-56`; `GAP-PFG-004`

#### Предусловия

- В Системе есть заявка по фикстурам `FIX-PFG-BASE` и `FIX-PFG-INCOME-ALL`.
- Для проверки `<job_start>` доступен расчетный oracle или заранее известное значение, соответствующее правилу строки `<job_start>`.

#### Тестовые данные

- Теги: `<job_title>`, `<start_job_date>`, `<main_job_income>`, `<job_start>`, `<job_income>`.

#### Шаги

1. Сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок `Сведения о доходе и должности заявителя`.

#### Итоговый ожидаемый результат

В печатной форме заполнены должность, дата начала работы, доход по основному месту работы, начало общего трудового стажа и общий доход; `<job_income>` равен сумме среднемесячного дохода по основному месту работы после вычета налогов и дополнительных доходов.

#### Постусловия

Если расчетный oracle для `<job_start>` недоступен из-за отсутствия справочника `Стаж работы`, зафиксировать блокировку проверки по `GAP-PFG-004`.

### TC-AF-PFG-013

**Название:** Заполнение других регулярных доходов

**Тип:** Decision table

**Приоритет:** Medium

**package_id:** WP-03

**Трассировка:** `ATOM-PFG-015`; `4.4 tag rows 57-60`; `DICT-PFG-006`

#### Предусловия

- Подготовлены заявки по фикстурам `FIX-PFG-INCOME-ALL` и `FIX-PFG-ADDITIONAL-INCOME-TYPE`.

#### Тестовые данные

| тип дохода | ожидаемый чек-бокс | тег суммы |
| --- | --- | --- |
| `Аренда` | `Сдача в аренду квартиры` = `Да` | `<rent>` |
| `Пенсия` | `Пенсия` = `Да` | `<pension>` |
| `Дивиденды` | `Дивиденды` = `Да` | `<dividends>` |

#### Шаги

1. Для каждого типа дополнительного дохода сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок других регулярных доходов.

#### Итоговый ожидаемый результат

Для каждого типа дополнительного дохода соответствующий чек-бокс равен `Да`, а тег суммы (`<rent>`, `<pension>` или `<dividends>`) заполнен суммой соответствующего дохода из заявки.

#### Постусловия

Не требуются.

### TC-AF-PFG-014

**Название:** Выбор адреса регистрации в блоке `Согласия на обработку ПДн`

**Тип:** Decision table

**Приоритет:** High

**package_id:** WP-04

**Трассировка:** `ATOM-PFG-016`; `4.4 tag row 61`

#### Предусловия

- Подготовлены заявки по фикстурам `FIX-PFG-ADDR-SAME` и `FIX-PFG-ADDR-DIFFERENT`.

#### Тестовые данные

- `Тег 1`: `<region_with_type>`, `<city_district>`, `<city>`, `<street>`, `<house>`, `<block>`, `<str>`, `<flat>`.
- `Тег 2`: `<region_with_type_reg>`, `<city_district_reg>`, `<city_reg>`, `<street_reg>`, `<house_reg>`, `<block_reg>`, `<str_reg>`, `<flat_reg>`.

#### Шаги

1. Для каждой фикстуры сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить адрес регистрации в блоке `Согласия на обработку ПДн`.

#### Итоговый ожидаемый результат

Если фактический адрес совпадает с адресом регистрации, в блоке согласий используется `Тег 1`; если адреса не совпадают, используется `Тег 2`, где поля `Тег 2` соответствуют адресу регистрации из заявки.

#### Постусловия

Не требуются.

### TC-AF-PFG-015

**Название:** Заполнение кода подразделения и ранее выданных паспортов

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-04

**Трассировка:** `ATOM-PFG-016`; `4.4 tag rows 62-87`

#### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-PREV-PASSPORTS-5`.
- В заявке заполнен код подразделения действующего паспорта.

#### Тестовые данные

- Тег действующего паспорта: `<dep_code>`.
- Наборы тегов ранее выданных паспортов: `<serial_number_1>`..`<serial_number_5>`, `<number_1>`..`<number_5>`, `<issue_date_1>`..`<issue_date_5>`, `<issue_place_1>`..`<issue_place_5>`, `<dep_code_1>`..`<dep_code_5>`.

#### Шаги

1. Сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить код подразделения действующего паспорта и блоки ранее выданных паспортов 1-5.

#### Итоговый ожидаемый результат

Тег `<dep_code>` заполнен кодом подразделения действующего паспорта, а каждый набор тегов ранее выданного паспорта 1-5 заполнен серией, номером, датой выдачи, органом выдачи и кодом подразделения соответствующего ранее выданного паспорта из заявки.

#### Постусловия

Не требуются.

### TC-AF-PFG-016

**Название:** Отсутствие незаполненных или неизвестных тегов после генерации PDF

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-PFG-004`; `DICT-PFG-001`; `GAP-PFG-002`

#### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-BASE` с заполненными данными для всех применимых строк таблицы раздела `4.4`.
- Шаблон настроен по `TC-AF-PFG-002`.

#### Тестовые данные

- Перечень тегов: `DICT-PFG-001`.

#### Шаги

1. Сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Поискать в PDF текстовые остатки плейсхолдеров из `DICT-PFG-001`.
4. Поискать в PDF неизвестные плейсхолдеры в формате угловых скобок, например `<...>`.

#### Итоговый ожидаемый результат

В сформированном PDF нет незаполненных плейсхолдеров из таблицы раздела `4.4` и нет неизвестных тегов, не входящих в `DICT-PFG-001`.

#### Постусловия

Сохранить PDF и результат поиска плейсхолдеров как evidence.
