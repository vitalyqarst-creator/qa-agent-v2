# Тест-кейсы: формирование печатной формы заявления-анкеты

## Metadata

| field | value |
| --- | --- |
| `ft_slug` | `AutoFin` |
| `scope_slug` | `print-form-generation` |
| `legacy_section_id` | `section-16` |
| `ft_section` | `4.4 Требования к формированию печатных форм документов` |
| `source_ft` | `FT4AutoFinFinal.xhtml`; `FT4AutoFinFinal.docx`; `FT4AutoFinFinal.pdf` |
| `canonical_test_design_dir` | `fts/AutoFin/work/test-design/section-16-print-form-generation` |
| `terminal_status` | `blocked-validator-profile` |

## Scope Boundaries

Набор покрывает формирование и содержимое печатной формы `Заявление-анкета на получение потребительского кредита` по разделу `4.4`: шаблон, подстановку тегов и заполнение полей/чек-боксов в PDF по данным клиента из Системы.

Не покрываются: UI-путь запуска генерации, роли, статусы заявки, API/RabbitMQ/БД, валидация исходных полей, правила сохранения исходных данных и поведение раздела `4.3`, кроме использования его данных как уже подготовленной фикстуры.

## Source Notes

- В разделе `4.4` нет BSR-кодов. Трассировка ведется через `ATOM-*`, строку таблицы и источник.
- Приоритетный источник требований: `FT4AutoFinFinal.xhtml`.
- Значение шаблона `Анкета клиента 04.02.2026.pdf` подтверждено `FT4AutoFinFinal.pdf`, page 36; в XHTML соответствующая ячейка пустая.
- Точное написание тегов сверено по `FT4AutoFinFinal.docx`, table 9, column `Тег`, потому что XHTML/PDF содержат визуальные переносы тегов.
- Проверка конфигурационной привязки шаблона к типу тегов не является executable TC без конкретного доступного артефакта настройки; см. `GAP-006`.
- Проверка расчета `<job_start>` не является executable TC без раскрытого справочника `Стаж работы`; см. `GAP-004`.
- Writer-side split artifacts для source inventory, package plan, quality gate, self-check и review находятся в `fts/AutoFin/work/test-design/section-16-print-form-generation/`.

## Coverage Gaps

Подробное описание gaps хранится в `work/test-design/section-16-print-form-generation/coverage-gaps.md`.

- `GAP-001`: XHTML has an empty template cell; PDF page 36 is used as the template oracle.
- `GAP-002`: XHTML/PDF visual extraction distorts some tag names; DOCX table is used for exact tag spelling.
- `GAP-003`: UI/API entrypoint for generation is not described; test stand entrypoint is a precondition.
- `GAP-004`: `<job_start>` calculation is not executable until dictionary/calculation oracle for `Стаж работы` is available.
- `GAP-005`: Preparing source data belongs to other sections; this scope uses ready fixtures.
- `GAP-006`: Template binding configuration artifact is not named; no executable config TC is created.

## Coverage Summary

| package_id | atom_id | source_ref | test_case_id | coverage_status |
| --- | --- | --- | --- | --- |
| `WP-01` | `ATOM-001` | `4.4 paragraph; PDF p.36` | `TC-AF-PFG-001` | `covered` |
| `WP-01` | `ATOM-002` | `4.4 document table; PDF p.36` | `TC-AF-PFG-001` | `covered` |
| `WP-01` | `ATOM-003` | `4.4 paragraph before tag table` | `GAP-006` | `gap` |
| `WP-01` | `ATOM-004` | `4.4 paragraph before tag table` | `TC-AF-PFG-002` | `covered` |
| `WP-02` | `ATOM-005` | `4.4 tag rows 1-4` | `TC-AF-PFG-003` | `covered` |
| `WP-02` | `ATOM-006` | `4.4 tag rows 5-19` | `TC-AF-PFG-004` | `covered` |
| `WP-02` | `ATOM-007` | `4.4 tag row 18` | `TC-AF-PFG-005` | `covered` |
| `WP-02` | `ATOM-008` | `4.4 tag row 20` | `TC-AF-PFG-006` | `covered` |
| `WP-02` | `ATOM-009` | `4.4 tag rows 21-29` | `TC-AF-PFG-007`; `TC-AF-PFG-008` | `covered` |
| `WP-02` | `ATOM-010` | `4.4 tag rows 30-31, 33` | `TC-AF-PFG-009` | `covered` |
| `WP-02` | `ATOM-018` | `4.4 tag row 32` | `TC-AF-PFG-010`; `TC-AF-PFG-011` | `covered` |
| `WP-02` | `ATOM-011` | `4.4 tag rows 34-39` | `TC-AF-PFG-012` | `covered` |
| `WP-03` | `ATOM-012` | `4.4 tag rows 40-43` | `TC-AF-PFG-013` | `covered` |
| `WP-03` | `ATOM-013` | `4.4 tag rows 44-51` | `TC-AF-PFG-014` | `covered` |
| `WP-03` | `ATOM-014` | `4.4 tag rows 52-54, 56` | `TC-AF-PFG-015` | `covered` |
| `WP-03` | `ATOM-015` | `4.4 tag row 55` | `GAP-004` | `gap` |
| `WP-03` | `ATOM-016` | `4.4 tag rows 57-60` | `TC-AF-PFG-016` | `covered` |
| `WP-04` | `ATOM-017` | `4.4 tag rows 61-87` | `TC-AF-PFG-017`; `TC-AF-PFG-018` | `covered` |

## Test Cases

## TC-AF-PFG-001

**Название:** Автоматическое формирование заявления-анкеты по шаблону `Анкета клиента 04.02.2026.pdf`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-001`; `ATOM-002`; `4.4 Требования к формированию печатных форм документов`; PDF page 36

### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-BASE`.
- Для тестового стенда определен entrypoint формирования печатной формы; проверка самого entrypoint не входит в scope.

### Тестовые данные

- Фикстура: `FIX-PFG-BASE`.
- Печатная форма: `Заявление-анкета на получение потребительского кредита`.
- Шаблон: `Анкета клиента 04.02.2026.pdf`.

### Шаги

1. Открыть подготовленную заявку `FIX-PFG-BASE`.
2. Запустить формирование печатной формы заявления-анкеты через entrypoint тестового стенда.
3. Открыть сформированный PDF.

### Итоговый ожидаемый результат

Сформирован PDF `Заявление-анкета на получение потребительского кредита` по шаблону `Анкета клиента 04.02.2026.pdf`, и данные клиента в PDF заполнены на основании данных заявки `FIX-PFG-BASE`.

### Постусловия

Сохранить сформированный PDF как evidence выполнения проверки.

## TC-AF-PFG-002

**Название:** Отсутствие незаполненных или неизвестных тегов после генерации PDF

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-004`; `DICT-001`; `4.4 paragraph before tag table`

### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-BASE`.
- Для всех применимых строк таблицы раздела `4.4` в заявке заполнены исходные значения из `FIX-PFG-BASE`.

### Тестовые данные

- Перечень ожидаемых тегов: `DICT-001`.
- Негативный oracle: в итоговом PDF не должно остаться плейсхолдеров формата `<...>`.

### Шаги

1. Сформировать печатную форму заявления-анкеты для заявки `FIX-PFG-BASE`.
2. Открыть сформированный PDF.
3. Выполнить поиск текстовых остатков плейсхолдеров из `DICT-001`.
4. Выполнить поиск неизвестных плейсхолдеров в формате угловых скобок, например `<...>`.

### Итоговый ожидаемый результат

В сформированном PDF нет незаполненных плейсхолдеров из таблицы раздела `4.4` и нет неизвестных тегов, не входящих в `DICT-001`.

### Постусловия

Сохранить PDF и результат поиска плейсхолдеров как evidence.

## TC-AF-PFG-003

**Название:** Заполнение блока `Параметры о кредите`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-005`; `4.4 tag rows 1-4`

### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-BASE`.

### Тестовые данные

- Теги блока: `<auto>`, `<loan_amount>`, `<monthly_payment>`, `<Loan term>`.
- Ожидаемые значения берутся из `FIX-PFG-BASE`: автомобиль, сумма кредита, ежемесячный платеж, срок кредита.

### Шаги

1. Сформировать печатную форму заявления-анкеты для заявки `FIX-PFG-BASE`.
2. Открыть сформированный PDF.
3. Проверить блок `Параметры о кредите`.

### Итоговый ожидаемый результат

В блоке `Параметры о кредите` значения тегов `<auto>`, `<loan_amount>`, `<monthly_payment>`, `<Loan term>` соответствуют значениям `Автомобиль`, `Сумма кредита`, `Ежемесячный платеж`, `Срок кредита` из `FIX-PFG-BASE`.

### Постусловия

Не требуются.

## TC-AF-PFG-004

**Название:** Заполнение персональных и паспортных данных заявителя

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-006`; `4.4 tag rows 5-19`

### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-FIO-CHANGED`.

### Тестовые данные

- Фикстура: `FIX-PFG-FIO-CHANGED`.
- Теги персональных данных: `<last_name>`, `<first_name>`, `<middle_name>`, `<previous_last_name>`, `<previous_first_name>`, `<previous_middle_name>`, `<snils>`, `<code_word>`, `<inn>`.
- Теги действующего паспорта: `<serial_number>`, `<number>`, `<issue_date>`, `<issue_place>`.
- Тег иждивенцев: `<dependency>`.

### Шаги

1. Сформировать печатную форму заявления-анкеты для заявки `FIX-PFG-FIO-CHANGED`.
2. Открыть сформированный PDF.
3. Проверить блок `Персональные данные заявителя`.

### Итоговый ожидаемый результат

В блоке `Персональные данные заявителя` заполнены ФИО, прежние ФИО, СНИЛС, кодовое слово, ИНН, серия/номер/дата выдачи/кем выдан действующего паспорта и число иждивенцев в соответствии с `FIX-PFG-FIO-CHANGED`.

### Постусловия

Не требуются.

## TC-AF-PFG-005

**Название:** Заполнение чек-бокса `Зарегистрированный брак`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-007`; `4.4 tag row 18`; `DICT-002`

### Предусловия

- Подготовлены заявки по фикстуре `FIX-PFG-MARRIAGE`.

### Тестовые данные

| fixture row | значение `Семейное положение` | ожидаемый чек-бокс |
| --- | --- | --- |
| `FIX-PFG-MARRIAGE-YES` | `женат/замужем` | `Да` |
| `FIX-PFG-MARRIAGE-NO` | `холост/не замужем` | `Нет` |

### Шаги

1. Для каждой строки fixture из таблицы сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить чек-бокс `Зарегистрированный брак`.

### Итоговый ожидаемый результат

Для каждой строки fixture значение чек-бокса `Зарегистрированный брак` соответствует ожидаемому значению из таблицы тестовых данных.

### Постусловия

Не требуются.

## TC-AF-PFG-006

**Название:** Заполнение чек-боксов `Социальный статус`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-008`; `4.4 tag row 20`; `DICT-003`

### Предусловия

- Подготовлены заявки по фикстуре `FIX-PFG-SOCIAL-STATUS`.

### Тестовые данные

Значения поля `Социальный статус`: `cобственник бизнеса/ИП`, `наемный работник`, `военнослужащий`, `пенсионер`, `самозанятый`.

### Шаги

1. Для каждого значения социального статуса из тестовых данных сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить группу чек-боксов `Социальный статус`.

### Итоговый ожидаемый результат

Для каждого значения социального статуса соответствующий чек-бокс в печатной форме равен `Да`; остальные чек-боксы этой группы не отмечены как `Да`.

### Постусловия

Не требуются.

## TC-AF-PFG-007

**Название:** Заполнение блока `Адрес фактического проживания`, когда адрес совпадает с регистрацией

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-009`; `4.4 tag rows 21-29`

### Предусловия

- Подготовлена заявка по фикстуре `FIX-PFG-ADDR-SAME`.

### Тестовые данные

- Теги адреса: `<region_with_type>`, `<city_district>`, `<city>`, `<street>`, `<house>`, `<block>`, `<str>`, `<flat>`.

### Шаги

1. Сформировать печатную форму заявления-анкеты для `FIX-PFG-ADDR-SAME`.
2. Открыть сформированный PDF.
3. Проверить блок `Адрес фактического проживания`.

### Итоговый ожидаемый результат

Теги фактического адреса заполнены значениями фактического адреса из `FIX-PFG-ADDR-SAME`, а чек-бокс `Совпадает с адресом постоянной регистрации` равен `Да`.

### Постусловия

Не требуются.

## TC-AF-PFG-008

**Название:** Заполнение блока `Адрес фактического проживания`, когда адрес отличается от регистрации

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-009`; `4.4 tag rows 21-29`

### Предусловия

- Подготовлена заявка по фикстуре `FIX-PFG-ADDR-DIFFERENT`.

### Тестовые данные

- Теги адреса: `<region_with_type>`, `<city_district>`, `<city>`, `<street>`, `<house>`, `<block>`, `<str>`, `<flat>`.

### Шаги

1. Сформировать печатную форму заявления-анкеты для `FIX-PFG-ADDR-DIFFERENT`.
2. Открыть сформированный PDF.
3. Проверить блок `Адрес фактического проживания`.

### Итоговый ожидаемый результат

Теги фактического адреса заполнены значениями фактического адреса из `FIX-PFG-ADDR-DIFFERENT`, а чек-бокс `Совпадает с адресом постоянной регистрации` равен `Нет`.

### Постусловия

Не требуются.

## TC-AF-PFG-009

**Название:** Заполнение мобильного телефона, email и домашнего телефона

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-010`; `4.4 tag rows 30-31, 33`

### Предусловия

- Подготовлена заявка по фикстуре `FIX-PFG-BASE`.

### Тестовые данные

- Теги: `<mobile_phone>`, `<email>`, `<home_phone>`.

### Шаги

1. Сформировать печатную форму заявления-анкеты для `FIX-PFG-BASE`.
2. Открыть сформированный PDF.
3. Проверить блок `Контактная информация`.

### Итоговый ожидаемый результат

Мобильный телефон, email и домашний телефон в блоке `Контактная информация` заполнены значениями из `FIX-PFG-BASE`.

### Постусловия

Не требуются.

## TC-AF-PFG-010

**Название:** Заполнение рабочего телефона для работающего клиента

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-018`; `4.4 tag row 32`

### Предусловия

- Подготовлена заявка по фикстуре `FIX-PFG-WORKING`.

### Тестовые данные

- Тег: `<work_phone>`.
- Тип занятости: `Работа по найму`.

### Шаги

1. Сформировать печатную форму заявления-анкеты для `FIX-PFG-WORKING`.
2. Открыть сформированный PDF.
3. Проверить тег `<work_phone>` в блоке `Контактная информация`.

### Итоговый ожидаемый результат

`<work_phone>` заполнен рабочим телефоном из `FIX-PFG-WORKING`.

### Постусловия

Не требуются.

## TC-AF-PFG-011

**Название:** Отсутствие заполненного рабочего телефона для неработающего пенсионера

**Тип:** Negative

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-018`; `4.4 tag row 32`

### Предусловия

- Подготовлена заявка по фикстуре `FIX-PFG-NOT-WORKING-PENSIONER`.

### Тестовые данные

- Тег: `<work_phone>`.
- Тип занятости: `Пенсионер (не работает)`.

### Шаги

1. Сформировать печатную форму заявления-анкеты для `FIX-PFG-NOT-WORKING-PENSIONER`.
2. Открыть сформированный PDF.
3. Проверить тег `<work_phone>` в блоке `Контактная информация`.

### Итоговый ожидаемый результат

`<work_phone>` отсутствует как заполненное значение для неработающего пенсионера.

### Постусловия

Не требуются.

## TC-AF-PFG-012

**Название:** Заполнение блока `Контактное лицо`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-011`; `4.4 tag rows 34-39`; `DICT-004`

### Предусловия

- Подготовлены заявки по фикстуре `FIX-PFG-RELATION`.
- Для каждой заявки заполнены ФИО, дата рождения и мобильный телефон контактного лица.

### Тестовые данные

Значения поля `Отношение к заявителю`: `супруг/супруга`, `отец/мать`, `сестра/брат`, `теща/свекровь/тесть/свекр`, `сын/дочь`, `друг/знакомый/коллега`, `иное`.

### Шаги

1. Для каждого значения отношения к заявителю сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок `Контактное лицо`.

### Итоговый ожидаемый результат

В блоке `Контактное лицо` заполнены теги `<last_name_contact>`, `<first_name_contact >`, `<middle_name_contact >`, `<date_of_birth_contact>`, `<mobile_phone_contact >`, а чек-бокс выбранного отношения к заявителю равен `Да`.

### Постусловия

Не требуются.

## TC-AF-PFG-013

**Название:** Заполнение блока `Информация о работодателе`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-03

**Трассировка:** `ATOM-012`; `4.4 tag rows 40-43`; `DICT-005`

### Предусловия

- Подготовлены заявки по фикстуре `FIX-PFG-OPF`.
- В каждой заявке заполнены наименование и ИНН работодателя.

### Тестовые данные

Значения поля `ОПФ`: `Гос. Предприятие (ГУП.ФГУП, МГУП, в/ч и пр.`, `ООО/АО`, `ИП`, `Прочее`.

### Шаги

1. Для каждого значения ОПФ сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок `Информация о работодателе`.

### Итоговый ожидаемый результат

Для выбранной ОПФ соответствующий чек-бокс работодателя равен `Да`, а теги `<name_main_job>` и `<inn_job>` заполнены наименованием и ИНН работодателя из соответствующей фикстуры.

### Постусловия

Не требуются.

## TC-AF-PFG-014

**Название:** Заполнение блока `Адрес местонахождения работодателя`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-03

**Трассировка:** `ATOM-013`; `4.4 tag rows 44-51`

### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-BASE` с заполненным адресом работодателя.

### Тестовые данные

- Теги адреса работодателя: `<region_with_type_job>`, `<city_district_job >`, `<city_job >`, `<street_job >`, `<house_job >`, `<block_job >`, `<str_job>`, `<office>`.

### Шаги

1. Сформировать печатную форму заявления-анкеты для заявки `FIX-PFG-BASE`.
2. Открыть сформированный PDF.
3. Проверить блок `Адрес местонахождения работодателя`.

### Итоговый ожидаемый результат

Теги адреса работодателя заполнены значениями региона, района, города, улицы, дома, корпуса, строения и офиса работодателя из `FIX-PFG-BASE`.

### Постусловия

Не требуются.

## TC-AF-PFG-015

**Название:** Заполнение блока `Сведения о доходе и должности заявителя`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-03

**Трассировка:** `ATOM-014`; `4.4 tag rows 52-54, 56`

### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-INCOME-ALL`.

### Тестовые данные

- Теги: `<job_title>`, `<start_job_date>`, `<main_job_income>`, `<job_income>`.
- Тег `<job_start>` не проверяется этим TC из-за `GAP-004`.

### Шаги

1. Сформировать печатную форму заявления-анкеты для заявки `FIX-PFG-INCOME-ALL`.
2. Открыть сформированный PDF.
3. Проверить блок `Сведения о доходе и должности заявителя`.

### Итоговый ожидаемый результат

В печатной форме заполнены должность, дата начала работы и доход по основному месту работы из `FIX-PFG-INCOME-ALL`; `<job_income>` равен сумме среднемесячного дохода по основному месту работы после вычета налогов и дополнительных доходов из `FIX-PFG-INCOME-ALL`.

### Постусловия

Не требуются.

## TC-AF-PFG-016

**Название:** Заполнение других регулярных доходов

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-03

**Трассировка:** `ATOM-016`; `4.4 tag rows 57-60`; `DICT-006`

### Предусловия

- Подготовлены заявки по фикстуре `FIX-PFG-ADDITIONAL-INCOME-TYPE`.

### Тестовые данные

| тип дохода | ожидаемый чек-бокс | тег суммы |
| --- | --- | --- |
| `Аренда` | `Сдача в аренду квартиры` = `Да` | `<rent>` |
| `Пенсия` | `Пенсия` = `Да` | `<pension>` |
| `Дивиденды` | `Дивиденды` = `Да` | `<dividends>` |

### Шаги

1. Для каждого типа дополнительного дохода из таблицы сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить блок других регулярных доходов.

### Итоговый ожидаемый результат

Для каждого типа дополнительного дохода соответствующий чек-бокс равен `Да`, а тег суммы (`<rent>`, `<pension>` или `<dividends>`) заполнен суммой соответствующего дохода из фикстуры.

### Постусловия

Не требуются.

## TC-AF-PFG-017

**Название:** Выбор адреса регистрации в блоке `Согласия на обработку ПДн`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-04

**Трассировка:** `ATOM-017`; `4.4 tag row 61`

### Предусловия

- Подготовлены заявки по фикстурам `FIX-PFG-ADDR-SAME` и `FIX-PFG-ADDR-DIFFERENT`.

### Тестовые данные

- `Тег 1`: `<region_with_type>`, `<city_district>`, `<city>`, `<street>`, `<house>`, `<block>`, `<str>`, `<flat>`.
- `Тег 2`: `<region_with_type_reg>`, `<city_district_reg>`, `<city_reg>`, `<street_reg>`, `<house_reg>`, `<block_reg>`, `<str_reg>`, `<flat_reg>`.

### Шаги

1. Для каждой адресной фикстуры сформировать печатную форму заявления-анкеты.
2. Открыть сформированный PDF.
3. Проверить адрес регистрации в блоке `Согласия на обработку ПДн`.

### Итоговый ожидаемый результат

Для `FIX-PFG-ADDR-SAME` в блоке согласий используется `Тег 1`; для `FIX-PFG-ADDR-DIFFERENT` используется `Тег 2`, где поля `Тег 2` соответствуют адресу регистрации из фикстуры.

### Постусловия

Не требуются.

## TC-AF-PFG-018

**Название:** Заполнение кода подразделения и ранее выданных паспортов

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-04

**Трассировка:** `ATOM-017`; `4.4 tag rows 62-87`

### Предусловия

- В Системе есть заявка по фикстуре `FIX-PFG-PREV-PASSPORTS-5`.
- В заявке заполнен код подразделения действующего паспорта.

### Тестовые данные

- Тег действующего паспорта: `<dep_code>`.
- Наборы тегов ранее выданных паспортов: `<serial_number_1>`..`<serial_number_5>`, `<number_1>`..`<number_5>`, `<issue_date_1>`..`<issue_date_5>`, `<issue_place_1>`..`<issue_place_5>`, `<dep_code_1>`..`<dep_code_5>`.

### Шаги

1. Сформировать печатную форму заявления-анкеты для заявки `FIX-PFG-PREV-PASSPORTS-5`.
2. Открыть сформированный PDF.
3. Проверить код подразделения действующего паспорта и блоки ранее выданных паспортов 1-5.

### Итоговый ожидаемый результат

Тег `<dep_code>` заполнен кодом подразделения действующего паспорта, а каждый набор тегов ранее выданного паспорта 1-5 заполнен серией, номером, датой выдачи, органом выдачи и кодом подразделения соответствующего ранее выданного паспорта из `FIX-PFG-PREV-PASSPORTS-5`.

### Постусловия

Не требуются.
