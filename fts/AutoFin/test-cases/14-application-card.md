# Тест-кейсы: 4.3 Карточка «Заявка»

## Metadata

| field | value |
| --- | --- |
| `ft_slug` | `AutoFin` |
| `scope_slug` | `application-card` |
| `section_id` | `14` |
| `ft_section` | `4.3 Карточка «Заявка»` |
| `source_ft` | `FT4AutoFinFinal.xhtml`; `FT4AutoFinFinal.docx`; `FT4AutoFinFinal.pdf` |
| `assembled_from` | `14-application-card-task-title-and-common-actions.md`; `14-application-card-calculator-summary-entrypoints.md`; `14-application-card-client-personal-data.md`; `14-application-card-document-recognition-popup.md`; `14-application-card-passport-current-and-previous.md`; `14-application-card-client-addresses.md`; `14-application-card-client-contacts-and-extra-info.md`; `14-application-card-documents-and-questionnaire-files.md`; `14-application-card-participants-coborrower-pledger.md`; `14-application-card-employment-income-gosuslugi.md`; `14-application-card-visual-assessment-consents-checks.md` |
| `test_case_count` | `262` |

## Порядок Сборки

Кейсы расположены в порядке описания ФТ: сначала требования к заголовку карточки, затем строки Таблицы 4 сверху вниз, затем standalone действия из описания действий раздела `Заявка`.

| order | блок ФТ | источник | TC count |
| --- | --- | --- | --- |
| 1 | 0. Заголовок карточки задачи | До Таблицы 4, требования к заголовку карточки задачи | 2 |
| 2 | 1. Краткая информация с калькулятора и кредитный калькулятор | Таблица 4, rows 002-003 | 4 |
| 3 | 2. Блок «Персональные данные» | Таблица 4, rows 004-014 | 15 |
| 4 | 3. Всплывающее окно распознавания документов | Таблица 4, row 015 | 8 |
| 5 | 4. Паспортные данные и предыдущие паспорта | Таблица 4, rows 016-031 | 54 |
| 6 | 5. Адрес регистрации и фактический адрес | Таблица 4, rows 032-057 | 27 |
| 7 | 6. Контакты клиента, контактные лица и дополнительная информация | Таблица 4, rows 058-080 | 27 |
| 8 | 7. Документы по заявке и файлы анкеты | Таблица 4, rows 081-094 | 31 |
| 9 | 8. Созаемщик и залогодатель | Таблица 4, rows 095-103 | 28 |
| 10 | 9. Сведения о занятости, доходы и подтверждение дохода | Таблица 4, rows 104-129 | 44 |
| 11 | 10. Визуальная информация, согласия и проверки | Таблица 4, rows 130-136 | 19 |
| 12 | 11. Действия «Далее» и «Вернуть в общий список» | Описание действий в разделе «Заявка» | 3 |

## Test Cases

## 0. Заголовок карточки задачи

Источник порядка: До Таблицы 4, требования к заголовку карточки задачи.

### TC-AF-CARD-001

**Название:** Заголовок новой карточки заявки для заемщика

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `4.3 Карточка «Заявка»`; `BSR 3`

### Предусловия

Пользователь открывает создание новой заявки заемщика.

### Тестовые данные

Не требуются.

### Шаги

1. Открыть карточку создания новой заявки.
2. Проверить заголовок карточки задачи.

### Итоговый ожидаемый результат

В заголовке карточки задачи отображается текст `Новая заявка (Заемщик)`.

### Постусловия

Закрыть карточку без сохранения, если заявка не нужна для следующих проверок.

### TC-AF-CARD-002

**Название:** Заголовок карточки при редактировании существующей заявки

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `4.3 Карточка «Заявка»`; `BSR 3`

### Предусловия

Существует заявка с известным номером.

### Тестовые данные

- Номер заявки: номер выбранной заявки из тестового контура.

### Шаги

1. Открыть существующую заявку на редактирование.
2. Проверить заголовок карточки задачи.

### Итоговый ожидаемый результат

В заголовке карточки задачи отображается текст `Заявка №номер заявки`, где `номер заявки` соответствует номеру открытой заявки.

### Постусловия

Закрыть карточку без сохранения, если изменения не требуются.

## 1. Краткая информация с калькулятора и кредитный калькулятор

Источник порядка: Таблица 4, rows 002-003.

### TC-ACCS-001

**Название:** Видимость виджета краткой информации с калькулятора в карточке заявки

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-001`; `BSR 43`; `SRC-001`; `WP-01`

### Предусловия

- Открыта карточка заявки.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к области карточки заявки, где отображаются entrypoints кредитного калькулятора.

### Итоговый ожидаемый результат

Виджет `Краткая информация с калькулятора` отображается в карточке заявки.

### Постусловия

Не требуются.

### TC-ACCS-002

**Название:** Отображение параметров краткой информации с этапа кредитного калькулятора

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-002`; `BSR 44`; `SRC-001`; `WP-01`

### Предусловия

- Открыта карточка заявки.
- На этапе `Кредитный калькулятор` для этой заявки зафиксированы значения параметров из тестовых данных.

### Тестовые данные

- Сумма кредита: `1500000 Р`.
- VIN: `XW8ZZZ61ZKG000001`.
- Ставка: `12,5 %`.
- Платеж в месяц: `35000 Р`.
- Срок: `60 мес`.

### Шаги

1. В карточке заявки перейти к уже отображаемому виджету `Краткая информация с калькулятора`.
2. Проверить состав данных, показанных внутри виджета.

### Итоговый ожидаемый результат

В виджете отображаются параметры `Сумма кредита, Р`, `VIN`, `Ставка, %`, `Платеж в месяц, Р`, `Срок, мес.` со значениями из тестовых данных.

### Постусловия

Не требуются.

### TC-ACCS-003

**Название:** Переход на этап кредитного калькулятора при нажатии на виджет

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-003`; `BSR 45`; `SRC-001`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки.
- Виджет `Краткая информация с калькулятора` отображается.

### Тестовые данные

Не требуются.

### Шаги

1. Нажать на виджет `Краткая информация с калькулятора`.

### Итоговый ожидаемый результат

Выполнен переход на этап `Кредитный калькулятор`.

### Постусловия

Не требуются.

### TC-ACCS-004

**Название:** Открытие окна кредитного калькулятора с предзаполненными данными по заявке

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-004`; `BSR 46`; `SRC-002`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки.
- В заявке есть сохраненные данные, доступные для предзаполнения кредитного калькулятора.

### Тестовые данные

- Заявка содержит VIN `XW8ZZZ61ZKG000001`.
- Заявка содержит сумму кредита `1500000 Р`.

### Шаги

1. Нажать кнопку `Кредитный калькулятор`.

### Итоговый ожидаемый результат

Открыто окно `Кредитный калькулятор` с предзаполненными данными по текущей заявке.

### Постусловия

Не требуются.

## 2. Блок «Персональные данные»

Источник порядка: Таблица 4, rows 004-014.

### TC-ACPD-001

**Название:** Отображение блока и основных полей персональных данных

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-001`; `SRC-001`; `SRC-002`; `SRC-003`; `SRC-004`; `SRC-005`; `SRC-006`; `SRC-007`; `SRC-008`; `BSR 47`; `BSR 48`; `BSR 49`; `BSR 50`; `BSR 51`; `BSR 52`; `BSR 53`; `BSR 54`; `BSR 55`; `BSR 56`; `BSR 57`; `BSR 58`; `BSR 59`; `BSR 60`; `BSR 61`; `BSR 62`; `BSR 63`; `BSR 64`; `BSR 65`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Персональные данные` в карточке заявки.

### Итоговый ожидаемый результат

В блоке `Персональные данные` отображаются поля `Фамилия`, `Имя`, `Отчество`, `ID клиента`, `Пол`, `Дата рождения`, `Клиент менял ФИО`.

### Постусловия

Не требуются.

### TC-ACPD-002

**Название:** Видимые маркеры обязательных и необязательных полей персональных данных

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-002`; `ATOM-003`; `SRC-002`; `SRC-003`; `SRC-004`; `SRC-005`; `SRC-006`; `SRC-007`; `SRC-008`; `SRC-011`; `BSR 47`; `BSR 48`; `BSR 49`; `BSR 50`; `BSR 51`; `BSR 52`; `BSR 53`; `BSR 54`; `BSR 55`; `BSR 56`; `BSR 57`; `BSR 58`; `BSR 59`; `BSR 60`; `BSR 61`; `BSR 62`; `BSR 63`; `BSR 64`; `BSR 65`; `BSR 74`; `BSR 75`; `BSR 76`; `BSR 77`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Персональные данные`.
2. Проверить видимые UI-маркеры обязательности у основных полей блока.
3. Установить поле `Клиент менял ФИО` в значение `Да`.
4. Проверить условную обязательность группы полей `Предыдущая фамилия`, `Предыдущее имя`, `Предыдущее отчество`.

### Итоговый ожидаемый результат

Поля `Фамилия`, `Имя`, `Пол`, `Дата рождения` имеют видимый UI-маркер обязательного поля; поля `Отчество`, `ID клиента`, `Клиент менял ФИО` не имеют видимого UI-маркера обязательного поля. После выбора `Клиент менял ФИО = Да` отображаются поля `Предыдущая фамилия`, `Предыдущее имя`, `Предыдущее отчество`; форма требует заполнить хотя бы одно из этих трех полей, но не требует заполнять все три одновременно.

### Постусловия

Вернуть значение `Клиент менял ФИО` в `Нет` или отменить несохраненные изменения.

### TC-ACPD-014

**Название:** Подсказки DaData для полей предыдущей ФИО

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `SRC-009`; `SRC-010`; `SRC-011`; `BSR 66`; `BSR 67`; `BSR 68`; `BSR 69`; `BSR 70`; `BSR 71`; `BSR 72`; `BSR 73`; `BSR 74`; `BSR 75`; `BSR 76`; `BSR 77`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Поле `Клиент менял ФИО` установлено в значение `Да`.
- Интеграция DaData доступна на стенде.

### Тестовые данные

- Предыдущая фамилия для подсказки DaData: `Сидоров`.
- Предыдущее имя для подсказки DaData: `Иван`.
- Предыдущее отчество для подсказки DaData: `Петрович`.

### Шаги

1. Начать ввод значения в поле `Предыдущая фамилия`.
2. Начать ввод значения в поле `Предыдущее имя`.
3. Начать ввод значения в поле `Предыдущее отчество`.

### Итоговый ожидаемый результат

Для каждого поля предыдущей ФИО отображаются подсказки DaData, доступные для выбора пользователем.

### Постусловия

Вернуть значение `Клиент менял ФИО` в `Нет` или отменить несохраненные изменения.

### TC-ACPD-015

**Название:** Дата рождения раньше границы 100 лет не принимается

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `SRC-007`; `BSR 60`; `BSR 61`; `BSR 62`; `BSR 63`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Текущая календарная дата стенда для примера: `30.06.2026`.

### Тестовые данные

- Дата рождения раньше границы `текущая дата - 100 лет`: `29.06.1926`.

### Шаги

1. В поле `Дата рождения` ввести `29.06.1926`.
2. Снять фокус с поля.

### Итоговый ожидаемый результат

Значение не проходит валидацию как дата рождения меньше допустимой границы `текущая дата - 100 лет`; форма не позволяет пройти дальше с этим значением.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACPD-003

**Название:** Редактируемость основных полей персональных данных и read-only состояние ID клиента

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-004`; `ATOM-008`; `SRC-002`; `SRC-003`; `SRC-004`; `SRC-005`; `SRC-006`; `SRC-007`; `SRC-008`; `BSR 47`; `BSR 48`; `BSR 49`; `BSR 50`; `BSR 51`; `BSR 52`; `BSR 53`; `BSR 54`; `BSR 55`; `BSR 56`; `BSR 57`; `BSR 58`; `BSR 59`; `BSR 60`; `BSR 61`; `BSR 62`; `BSR 63`; `BSR 64`; `BSR 65`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Фамилия: `Иванов`.
- Имя: `Петр`.
- Отчество: `Сергеевич`.
- Дата рождения: `30.06.2000`.
- Значение переключателя `Клиент менял ФИО`: `Да`.

### Шаги

1. Ввести тестовые значения в поля `Фамилия`, `Имя`, `Отчество`, `Дата рождения`.
2. Установить `Клиент менял ФИО` в значение `Да`.
3. Проверить возможность пользовательского изменения поля `Пол`.
4. Проверить возможность пользовательского изменения поля `ID клиента`.

### Итоговый ожидаемый результат

Поля `Фамилия`, `Имя`, `Отчество`, `Пол`, `Дата рождения`, `Клиент менял ФИО` доступны для пользовательского изменения; поле `ID клиента` недоступно для ручного изменения.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACPD-004

**Название:** Формат допустимого ввода текстовых значений с дефисом в текущую ФИО клиента

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-005`; `ATOM-006`; `ATOM-007`; `SRC-002`; `SRC-003`; `SRC-004`; `BSR 47`; `BSR 48`; `BSR 49`; `BSR 50`; `BSR 51`; `BSR 52`; `BSR 53`; `BSR 54`; `BSR 55`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Фамилия: `Иванов-Петров`.
- Имя: `Анна-Мария`.
- Отчество: `Сергеевна`.

### Шаги

1. В поле `Фамилия` ввести `Иванов-Петров`.
2. В поле `Имя` ввести `Анна-Мария`.
3. В поле `Отчество` ввести `Сергеевна`.

### Итоговый ожидаемый результат

Поля `Фамилия`, `Имя`, `Отчество` принимают допустимый формат `текстовые символы и дефис` и отображают введенные значения, включая символ `-` в фамилии и имени.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACPD-005

**Название:** Автоматическое заполнение ID клиента после сохранения заявки

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-009`; `SRC-005`; `BSR 56`; `BSR 57`; `GAP-001`; `WP-01`

### Предусловия

- Открыта новая или редактируемая заявка.
- Все поля, необходимые для сохранения заявки в выбранном тестовом стенде, заполнены валидными данными из соответствующих signed-off scope.

### Тестовые данные

- Черновая заявка без заполненного `ID клиента`.

### Шаги

1. Сохранить заявку.
2. Вернуться к блоку `Персональные данные`.

### Итоговый ожидаемый результат

Поле `ID клиента` заполнено автоматически сформированным значением из АБС и остается недоступным для ручного изменения.

### Постусловия

Использовать одноразовую тестовую заявку или удалить созданные тестовые данные по регламенту стенда.

### TC-ACPD-006

**Название:** Значения справочника Пол клиента

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-010`; `DICT-001`; `SRC-006`; `BSR 58`; `BSR 59`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Активные значения `DICT-001`: `Мужчина`, `Женщина`.

### Шаги

1. Перейти к полю `Пол`.
2. Открыть или просмотреть доступные значения переключателя/справочника `Пол`.

### Итоговый ожидаемый результат

В поле `Пол` доступны все и только активные значения `DICT-001`: `Мужчина`, `Женщина`.

### Постусловия

Не требуются.

### TC-ACPD-007

**Название:** Обновление пола после заполнения ФИО через подсказку DaData

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-011`; `DICT-001`; `SRC-006`; `BSR 58`; `BSR 59`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Интеграция DaData доступна на стенде.

### Тестовые данные

- ФИО для подсказки DaData: `Иванов Иван Иванович`.
- Ожидаемое значение `Пол` из `DICT-001`: `Мужчина`.

### Шаги

1. Начать ввод ФИО `Иванов Иван Иванович` в поля ФИО клиента.
2. Выбрать подходящую подсказку DaData для указанного ФИО.
3. Проверить значение поля `Пол`.

### Итоговый ожидаемый результат

Поле `Пол` обновлено значением `Мужчина` из `DICT-001`.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACPD-008

**Название:** Ввод даты рождения на границе 18 лет

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-012`; `ATOM-013`; `SRC-007`; `BSR 60`; `BSR 61`; `BSR 62`; `BSR 63`; `GAP-002`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Текущая календарная дата стенда для примера: `30.06.2026`.

### Тестовые данные

- Дата рождения на границе `текущая дата - 18 лет`: `30.06.2008`.

### Шаги

1. В поле `Дата рождения` ввести `30.06.2008`.

### Итоговый ожидаемый результат

Поле `Дата рождения` отображает введенную дату `30.06.2008`.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACPD-009

**Название:** Значение по умолчанию Нет у признака Клиент менял ФИО

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-014`; `SRC-008`; `BSR 64`; `BSR 65`; `WP-01`

### Предусловия

- Открыта новая заявка или форма без ранее измененного значения `Клиент менял ФИО`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к полю `Клиент менял ФИО`.

### Итоговый ожидаемый результат

Для поля `Клиент менял ФИО` выбрано значение `Нет`.

### Постусловия

Не требуются.

### TC-ACPD-010

**Название:** Переключение признака Клиент менял ФИО между Да и Нет

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-015`; `SRC-008`; `BSR 64`; `BSR 65`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Значения переключателя: `Да`, `Нет`.

### Шаги

1. Установить поле `Клиент менял ФИО` в значение `Да`.
2. Установить поле `Клиент менял ФИО` в значение `Нет`.

### Итоговый ожидаемый результат

Поле `Клиент менял ФИО` последовательно отображает выбранные значения `Да` и `Нет`.

### Постусловия

Вернуть значение `Нет` или отменить несохраненные изменения.

### TC-ACPD-011

**Название:** Отображение полей предыдущей ФИО при значении Да

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-016`; `SRC-009`; `SRC-010`; `SRC-011`; `BSR 66`; `BSR 67`; `BSR 68`; `BSR 69`; `BSR 70`; `BSR 71`; `BSR 72`; `BSR 73`; `BSR 74`; `BSR 75`; `BSR 76`; `BSR 77`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Значение `Клиент менял ФИО`: `Да`.

### Шаги

1. Установить поле `Клиент менял ФИО` в значение `Да`.

### Итоговый ожидаемый результат

Отображаются поля `Предыдущая фамилия`, `Предыдущее имя`, `Предыдущее отчество`.

### Постусловия

Вернуть значение `Нет` или отменить несохраненные изменения.

### TC-ACPD-012

**Название:** Скрытие полей предыдущей ФИО при значении Нет

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-017`; `SRC-009`; `SRC-010`; `SRC-011`; `BSR 66`; `BSR 67`; `BSR 68`; `BSR 69`; `BSR 70`; `BSR 71`; `BSR 72`; `BSR 73`; `BSR 74`; `BSR 75`; `BSR 76`; `BSR 77`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Значение `Клиент менял ФИО`: `Нет`.

### Шаги

1. Установить поле `Клиент менял ФИО` в значение `Нет`.

### Итоговый ожидаемый результат

Поля `Предыдущая фамилия`, `Предыдущее имя`, `Предыдущее отчество` не отображаются.

### Постусловия

Не требуются.

### TC-ACPD-013

**Название:** Формат допустимого ввода текстовых значений с дефисом в предыдущую ФИО клиента

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-018`; `ATOM-019`; `ATOM-020`; `SRC-009`; `SRC-010`; `SRC-011`; `BSR 66`; `BSR 67`; `BSR 68`; `BSR 69`; `BSR 70`; `BSR 71`; `BSR 72`; `BSR 73`; `BSR 74`; `BSR 75`; `BSR 76`; `BSR 77`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Поле `Клиент менял ФИО` установлено в значение `Да`.

### Тестовые данные

- Предыдущая фамилия: `Сидорова-Петрова`.
- Предыдущее имя: `Мария-Анна`.
- Предыдущее отчество: `Игоревна`.

### Шаги

1. В поле `Предыдущая фамилия` ввести `Сидорова-Петрова`.
2. В поле `Предыдущее имя` ввести `Мария-Анна`.
3. В поле `Предыдущее отчество` ввести `Игоревна`.

### Итоговый ожидаемый результат

Поля `Предыдущая фамилия`, `Предыдущее имя`, `Предыдущее отчество` принимают допустимый формат `текстовые символы и дефис` и отображают введенные значения, включая символ `-` в предыдущей фамилии и предыдущем имени.

### Постусловия

Вернуть значение `Клиент менял ФИО` в `Нет` или отменить несохраненные изменения.

## 3. Всплывающее окно распознавания документов

Источник порядка: Таблица 4, row 015.

### TC-AFDRP-001

**Название:** Видимость кнопки `Распознать документ` в карточке заявки

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-001`; `BSR 78`; `SRC-001`; `WP-01`

### Предусловия

- Открыта карточка заявки.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к области карточки заявки, где размещено действие распознавания документа.

### Итоговый ожидаемый результат

Кнопка `Распознать документ` отображается в карточке заявки.

### Постусловия

Не требуются.

### TC-AFDRP-002

**Название:** Открытие popup распознавания документов по кнопке `Распознать документ`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-002`; `BSR 79`; `SRC-001`; `WP-01`

### Предусловия

- Открыта карточка заявки.
- Кнопка `Распознать документ` отображается.

### Тестовые данные

Не требуются.

### Шаги

1. Нажать кнопку `Распознать документ`.

### Итоговый ожидаемый результат

Открыто всплывающее окно распознавания документов.

### Постусловия

- Закрыть всплывающее окно без распознавания, если оно осталось открытым.

### TC-AFDRP-003

**Название:** Состав раскрывающегося списка `Тип документа` в popup распознавания

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-003`; `BSR 79`; `SRC-001`; `DICT-001`; `GAP-001`; `WP-01`

### Предусловия

- Открыто всплывающее окно распознавания документов.

### Тестовые данные

Активные значения `DICT-001`: `ВУ`, `СНИЛС`, `Загран. паспорт`, `Паспорт`, `Анкета`.

### Шаги

1. Раскрыть список `Тип документа`.

### Итоговый ожидаемый результат

В списке `Тип документа` доступны все и только активные значения `DICT-001`: `ВУ`, `СНИЛС`, `Загран. паспорт`, `Паспорт`, `Анкета`.

### Постусловия

- Закрыть список `Тип документа`, если он остался раскрытым.

### TC-AFDRP-004

**Название:** Отображение контейнера прикрепления файлов с drag&drop в popup распознавания

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-004`; `BSR 79`; `SRC-001`; `WP-01`

### Предусловия

- Открыто всплывающее окно распознавания документов.

### Тестовые данные

Не требуются.

### Шаги

1. Осмотреть область прикрепления файлов во всплывающем окне.

### Итоговый ожидаемый результат

Во всплывающем окне отображается контейнер для прикрепления файлов с drag&drop, предназначенных для распознавания.

### Постусловия

Не требуются.

### TC-AFDRP-005

**Название:** Отображение кнопок `Отменить` и `Распознать` в popup распознавания

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-005`; `BSR 80`; `SRC-001`; `WP-01`

### Предусловия

- Открыто всплывающее окно распознавания документов.

### Тестовые данные

Не требуются.

### Шаги

1. Осмотреть набор кнопок во всплывающем окне.

### Итоговый ожидаемый результат

Во всплывающем окне отображаются кнопки `Отменить` и `Распознать`.

### Постусловия

Не требуются.

### TC-AFDRP-006

**Название:** Закрытие popup распознавания по кнопке `Отменить`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-006`; `BSR 81`; `SRC-001`; `WP-01`

### Предусловия

- Открыто всплывающее окно распознавания документов.

### Тестовые данные

Не требуются.

### Шаги

1. Нажать кнопку `Отменить`.

### Итоговый ожидаемый результат

Всплывающее окно распознавания документов закрыто.

### Постусловия

Не требуются.

### TC-AFDRP-007

**Название:** Предупреждение при распознавании без вложения в контейнере файлов

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-007`; `BSR 82`; `SRC-001`; `WP-01`

### Предусловия

- Открыто всплывающее окно распознавания документов.
- В контейнере для файлов отсутствуют вложения.

### Тестовые данные

Не требуются.

### Шаги

1. Нажать кнопку `Распознать`.

### Итоговый ожидаемый результат

Отображается предупреждение `Отсутствуют файлы для распознавания`.

### Постусловия

- Закрыть предупреждение, если интерфейс требует отдельного закрытия.

### TC-AFDRP-008

**Название:** Распознавание документа при наличии файла запускает запрос и заполняет поля заявки

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-008`; `BSR 83`; `SRC-001`; `GAP-002`; `WP-01`

### Предусловия

- Открыто всплывающее окно распознавания документов.
- Система распознавания документов доступна на стенде или заменена управляемым stub-сервисом.
- Есть способ проверить факт запроса в систему распознавания: лог, stub history, API evidence или иной согласованный артефакт.

### Тестовые данные

- Файл документа поддерживаемого формата: `recognition-passport.pdf`.
- Fixture распознавания возвращает значения для полей заявки, соответствующих выбранному типу документа.

### Шаги

1. Добавить файл `recognition-passport.pdf` в контейнер файлов popup.
2. Выбрать соответствующий тип документа.
3. Нажать кнопку `Распознать`.
4. Проверить evidence запроса в систему распознавания.
5. Проверить поля заявки, для которых fixture вернул значения.

### Итоговый ожидаемый результат

При наличии файла система выполняет запрос в систему распознавания. После успешного ответа распознанные значения из fixture заполнены в соответствующих полях заявки.

### Постусловия

Очистить заполненные распознаванием поля или отменить несохраненные изменения.

## 4. Паспортные данные и предыдущие паспорта

Источник порядка: Таблица 4, rows 016-031.

### TC-ACPCP-001

**Название:** Отображение блока текущих паспортных данных

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 93`; `BSR 90`; `BSR 87`; `BSR 84`; `ATOM-001`; `ATOM-002`; `SRC-001`; `SRC-002`; `SRC-003`; `SRC-004`; `SRC-005`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Паспортные данные`.

### Итоговый ожидаемый результат

Отображается блок `Паспортные данные` с полями `Серия`, `Номер`, `Код подразделения`, `Кем выдан`.

### Постусловия

Не требуются.

### TC-ACPCP-002

**Название:** Видимые маркеры обязательных полей текущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 99`; `ATOM-003`; `ATOM-019`; `ATOM-031`; `SRC-002`; `SRC-003`; `SRC-004`; `SRC-005`; `SRC-008`; `SRC-009`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Паспортные данные`.
2. Проверить видимые UI-маркеры обязательности у полей текущего паспорта.

### Итоговый ожидаемый результат

Поля `Серия`, `Номер`, `Код подразделения`, `Кем выдан`, `Дата выдачи`, `Место рождения` имеют видимый UI-маркер обязательного поля.

### Постусловия

Не требуются.

### TC-ACPCP-003

**Название:** Редактируемость полей текущего паспорта

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `BSR 99`; `ATOM-003`; `ATOM-019`; `ATOM-031`; `SRC-002`; `SRC-003`; `SRC-004`; `SRC-005`; `SRC-008`; `SRC-009`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Серия: `1234`.
- Номер: `123456`.
- Код подразделения: `770001`.
- Кем выдан: `ГУ МВД России по г. Москве`.
- Дата выдачи: `30.06.2024`.
- Место рождения: `Г. МОСКВА`.

### Шаги

1. Ввести тестовые значения в поля текущего паспорта.

### Итоговый ожидаемый результат

Поля отображают введенные значения и доступны для пользовательского изменения.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-004

**Название:** Допустимый формат серии текущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 85`; `ATOM-004`; `SRC-002`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Серия: `1234`.

### Шаги

1. В поле `Серия` ввести `1234`.

### Итоговый ожидаемый результат

Поле `Серия` отображает значение `1234`.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-005

**Название:** Запрет трех одинаковых цифр подряд в серии текущего паспорта

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 86`; `ATOM-005`; `SRC-002`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Серия с тремя одинаковыми цифрами подряд: `1112`.

### Шаги

1. В поле `Серия` ввести `1112`.
2. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Отображается валидация для поля `Серия`, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-006

**Название:** Допустимый формат номера текущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 88`; `ATOM-006`; `SRC-003`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Номер: `123456`.

### Шаги

1. В поле `Номер` ввести `123456`.

### Итоговый ожидаемый результат

Поле `Номер` отображает значение `123456`.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-007

**Название:** Запрет шести одинаковых цифр подряд в номере текущего паспорта

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 89`; `ATOM-007`; `SRC-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Номер из шести одинаковых цифр: `111111`.

### Шаги

1. В поле `Номер` ввести `111111`.
2. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Отображается валидация для поля `Номер`, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-008

**Название:** Допустимый формат кода подразделения

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 91`; `ATOM-008`; `SRC-004`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Код подразделения: `770001`.

### Шаги

1. В поле `Код подразделения` ввести `770001`.

### Итоговый ожидаемый результат

Поле `Код подразделения` принимает шесть числовых символов.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-009

**Название:** Маска отображения кода подразделения

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `BSR 92`; `ATOM-009`; `SRC-004`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Код подразделения: `770001`.

### Шаги

1. В поле `Код подразделения` ввести `770001`.

### Итоговый ожидаемый результат

Поле `Код подразделения` отображает введенный код в форме `770 -001`.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-010

**Название:** Режим выбора Кем выдан при ручном вводе подразделения Нет

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 93`; `ATOM-010`; `SRC-005`; `SRC-006`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- `Ввести вручную подразделение`: `Нет`.

### Шаги

1. Установить `Ввести вручную подразделение` в значение `Нет`.
2. Перейти к полю `Кем выдан`.

### Итоговый ожидаемый результат

Поле `Кем выдан` отображается как раскрывающийся список.

### Постусловия

Вернуть исходные значения или отменить несохраненные изменения.

### TC-ACPCP-011

**Название:** Предзаполнение Кем выдан по коду подразделения

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 94`; `ATOM-011`; `SRC-005`; `GAP-004`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Интеграция DaData доступна на стенде.

### Тестовые данные

- Код подразделения с доступной подсказкой DaData: `770001`.

### Шаги

1. Установить `Ввести вручную подразделение` в значение `Нет`.
2. В поле `Код подразделения` ввести `770001`.
3. Перейти к полю `Кем выдан`.

### Итоговый ожидаемый результат

Поле `Кем выдан` предзаполнено значением, полученным по введенному коду подразделения.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-013

**Название:** Отображение и значение по умолчанию Ввести вручную подразделение

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `BSR 97`; `BSR 96`; `ATOM-013`; `ATOM-014`; `SRC-006`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к полю `Ввести вручную подразделение`.

### Итоговый ожидаемый результат

Поле `Ввести вручную подразделение` отображается, значение по умолчанию равно `Нет`.

### Постусловия

Не требуются.

### TC-ACPCP-014

**Название:** Переключение Ввести вручную подразделение

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-015`; `SRC-006`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Значения переключателя: `Да`, `Нет`.

### Шаги

1. Установить `Ввести вручную подразделение` в значение `Да`.
2. Установить `Ввести вручную подразделение` в значение `Нет`.

### Итоговый ожидаемый результат

Поле `Ввести вручную подразделение` последовательно отображает выбранные значения `Да` и `Нет`.

### Постусловия

Вернуть значение `Нет` или отменить несохраненные изменения.

### TC-ACPCP-015

**Название:** Ручное поле Кем выдан при значении Да

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 98`; `ATOM-016`; `SRC-007`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- `Ввести вручную подразделение`: `Да`.

### Шаги

1. Установить `Ввести вручную подразделение` в значение `Да`.

### Итоговый ожидаемый результат

Отображается ручное текстовое поле `Кем выдан`.

### Постусловия

Вернуть значение `Нет` или отменить несохраненные изменения.

### TC-ACPCP-016

**Название:** Пустое обязательное ручное поле Кем выдан

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-017`; `SRC-007`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Остальные обязательные поля текущего паспорта заполнены валидными данными.

### Тестовые данные

- `Ввести вручную подразделение`: `Да`.
- `Кем выдан`: пусто.

### Шаги

1. Установить `Ввести вручную подразделение` в значение `Да`.
2. Оставить ручное поле `Кем выдан` пустым.
3. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Для ручного поля `Кем выдан` отображается ошибка обязательности, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-017

**Название:** Ввод строкового значения в ручное поле Кем выдан

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-018`; `SRC-007`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Кем выдан: `ГУ МВД России по г. Москве`.

### Шаги

1. Установить `Ввести вручную подразделение` в значение `Да`.
2. В ручное поле `Кем выдан` ввести `ГУ МВД России по г. Москве`.

### Итоговый ожидаемый результат

Ручное поле `Кем выдан` отображает введенное строковое значение `ГУ МВД России по г. Москве`.

### Постусловия

Вернуть значение `Нет` или отменить несохраненные изменения.

### TC-ACPCP-018

**Название:** Допустимая дата выдачи текущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 99`; `ATOM-019`; `SRC-008`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Текущая дата стенда: `30.06.2026`.
- Дата выдачи: `01.07.2020`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `01.07.2020`.

### Итоговый ожидаемый результат

Поле `Дата выдачи` отображает дату `01.07.2020`; сообщения `Выдача паспорта предусмотрена с 14 лет`, `Паспорт недействителен (просрочен)` и `Дата должна быть не больше текущей даты` не отображаются.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-019

**Название:** Пустая обязательная дата выдачи текущего паспорта

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 99`; `ATOM-029`; `SRC-008`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Остальные обязательные поля текущего паспорта заполнены валидными данными.

### Тестовые данные

- Дата выдачи: пусто.

### Шаги

1. Оставить поле `Дата выдачи` пустым.
2. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Для поля `Дата выдачи` отображается ошибка обязательности, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-020

**Название:** Дата выдачи раньше 14-летия

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-020`; `SRC-008`; `BSR 100`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Дата_14: `30.06.2020`.
- Дата выдачи: `29.06.2020`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `29.06.2020`.
3. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Отображается сообщение `Выдача паспорта предусмотрена с 14 лет`, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-021

**Название:** Дата выдачи на 14-летии

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-021`; `SRC-008`; `BSR 100`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Дата_14: `30.06.2020`.
- Текущая дата стенда: `30.06.2026`.
- Дата выдачи: `30.06.2020`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `30.06.2020`.

### Итоговый ожидаемый результат

Сообщение `Выдача паспорта предусмотрена с 14 лет` не отображается.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-022

**Название:** Паспорт до 20 лет действителен на Дата_20_90

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-022`; `SRC-008`; `BSR 100`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Текущая дата стенда: `28.09.2026` (`Дата_20_90`).
- Дата выдачи: `30.06.2020`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `30.06.2020`.

### Итоговый ожидаемый результат

Сообщение `Паспорт недействителен (просрочен)` не отображается.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-023

**Название:** Дата выдачи на 20-летии входит в первый диапазон

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-022`; `SRC-008`; `BSR 100`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Дата_20: `30.06.2026`.
- Текущая дата стенда: `28.09.2026`.
- Дата выдачи: `30.06.2026`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `30.06.2026`.

### Итоговый ожидаемый результат

Дата выдачи `30.06.2026` обрабатывается как выдача до/на 20 лет; сообщение `Паспорт недействителен (просрочен)` не отображается на `Дата_20_90`.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-024

**Название:** Паспорт до 20 лет просрочен после Дата_20_90

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-023`; `SRC-008`; `BSR 100`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Текущая дата стенда: `29.09.2026` (`Дата_20_90 + 1 день`).
- Дата выдачи: `30.06.2020`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `30.06.2020`.
3. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Отображается сообщение `Паспорт недействителен (просрочен)`, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-025

**Название:** Дата выдачи на Дата_20_1 входит во второй диапазон

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-024`; `SRC-008`; `BSR 100`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Дата_20_1: `01.07.2026`.
- Текущая дата стенда: `28.09.2051`.
- Дата выдачи: `01.07.2026`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `01.07.2026`.

### Итоговый ожидаемый результат

Дата выдачи `01.07.2026` обрабатывается как выдача после 20 лет; сообщение `Паспорт недействителен (просрочен)` не отображается на `Дата_45_90`.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-026

**Название:** Паспорт после 20 лет действителен на Дата_45_90

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-025`; `SRC-008`; `BSR 100`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Текущая дата стенда: `28.09.2051` (`Дата_45_90`).
- Дата выдачи: `01.07.2026`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `01.07.2026`.

### Итоговый ожидаемый результат

Сообщение `Паспорт недействителен (просрочен)` не отображается.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-027

**Название:** Дата выдачи перед 45-летием входит во второй диапазон

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-025`; `SRC-008`; `BSR 100`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Дата_45: `30.06.2051`.
- Текущая дата стенда: `28.09.2051`.
- Дата выдачи: `29.06.2051`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `29.06.2051`.

### Итоговый ожидаемый результат

Дата выдачи `29.06.2051` обрабатывается как выдача после 20 и до 45 лет; сообщение `Паспорт недействителен (просрочен)` не отображается на `Дата_45_90`.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-028

**Название:** Паспорт после 20 лет просрочен после Дата_45_90

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-026`; `SRC-008`; `BSR 100`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Текущая дата стенда: `29.09.2051` (`Дата_45_90 + 1 день`).
- Дата выдачи: `01.07.2026`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `01.07.2026`.
3. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Отображается сообщение `Паспорт недействителен (просрочен)`, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-029

**Название:** Дата выдачи на 45-летии бессрочна

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-027`; `SRC-008`; `BSR 100`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата рождения: `30.06.2006`.
- Текущая дата стенда: `29.09.2051`.
- Дата выдачи: `30.06.2051`.

### Шаги

1. Ввести дату рождения `30.06.2006`.
2. В поле `Дата выдачи` ввести `30.06.2051`.

### Итоговый ожидаемый результат

Для даты выдачи `30.06.2051` сообщение `Паспорт недействителен (просрочен)` по причине возраста клиента не отображается.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-030

**Название:** Дата выдачи больше текущей даты

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-028`; `SRC-008`; `BSR 101`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Текущая дата стенда: `30.06.2026`.
- Дата выдачи: `01.07.2026`.

### Шаги

1. В поле `Дата выдачи` ввести `01.07.2026`.
2. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Отображается сообщение `Дата должна быть не больше текущей даты`, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-031

**Название:** Пустая дата выдачи текущего паспорта

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 99`; `ATOM-029`; `SRC-008`; `GAP-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Остальные обязательные поля текущего паспорта заполнены валидными данными.

### Тестовые данные

- Дата выдачи: пусто.

### Шаги

1. Оставить поле `Дата выдачи` пустым.
2. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Для поля `Дата выдачи` отображается ошибка обязательности, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-032

**Название:** Ввод места рождения

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `BSR 102`; `ATOM-030`; `ATOM-032`; `SRC-009`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Место рождения: `Г. МОСКВА`.

### Шаги

1. В поле `Место рождения` ввести `Г. МОСКВА`.

### Итоговый ожидаемый результат

Поле `Место рождения` отображает введенное строковое значение `Г. МОСКВА`.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-033

**Название:** Отображение и значение по умолчанию Клиент менял паспорт

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `BSR 104`; `BSR 103`; `ATOM-033`; `ATOM-034`; `SRC-010`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к полю `Клиент менял паспорт`.

### Итоговый ожидаемый результат

Поле `Клиент менял паспорт` отображается, значение по умолчанию равно `Нет`.

### Постусловия

Не требуются.

### TC-ACPCP-034

**Название:** Переключение Клиент менял паспорт

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-035`; `SRC-010`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Значения переключателя: `Да`, `Нет`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. Установить `Клиент менял паспорт` в значение `Нет`.

### Итоговый ожидаемый результат

Поле `Клиент менял паспорт` последовательно отображает выбранные значения `Да` и `Нет`.

### Постусловия

Вернуть значение `Нет` или отменить несохраненные изменения.

### TC-ACPCP-035

**Название:** Автовыбор Клиент менял паспорт по дате выдачи менее трех лет назад

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 105`; `ATOM-036`; `SRC-010`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Текущая дата стенда: `30.06.2026`.
- Дата выдачи текущего паспорта: `01.07.2023`.

### Шаги

1. Ввести дату выдачи текущего паспорта `01.07.2023`.
2. Перейти к полю `Клиент менял паспорт`.

### Итоговый ожидаемый результат

Поле `Клиент менял паспорт` отображает значение `Да`.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-036

**Название:** Отображение блока предыдущего паспорта при значении Да

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `BSR 114`; `BSR 112`; `BSR 110`; `ATOM-037`; `ATOM-045`; `ATOM-051`; `SRC-011`; `SRC-014`; `SRC-015`; `SRC-016`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- `Клиент менял паспорт`: `Да`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.

### Итоговый ожидаемый результат

Отображается блок `Данные предыдущих паспортов` с полями последнего предыдущего паспорта `Серия`, `Номер`, `Дата выдачи`.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-037

**Название:** Скрытие блока предыдущего паспорта при значении Нет

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-038`; `SRC-011`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- `Клиент менял паспорт`: `Нет`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Нет`.

### Итоговый ожидаемый результат

Блок `Данные предыдущих паспортов` не отображается.

### Постусловия

Не требуются.

### TC-ACPCP-038

**Название:** Отображение кнопки Добавить паспорт

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `BSR 106`; `ATOM-039`; `SRC-012`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- `Клиент менял паспорт`: `Да`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.

### Итоговый ожидаемый результат

В блоке предыдущих паспортов отображается кнопка `Добавить паспорт`.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-039

**Название:** Добавление последнего предыдущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `BSR 114`; `BSR 112`; `BSR 110`; `BSR 107`; `ATOM-040`; `ATOM-041`; `ATOM-045`; `SRC-012`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- `Клиент менял паспорт`: `Да`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. Нажать `Добавить паспорт`.

### Итоговый ожидаемый результат

Добавлены поля последнего предыдущего паспорта `Серия`, `Номер`, `Дата выдачи`, а также виджеты `+Добавить паспорт` и `корзина` для этого блока.

### Постусловия

Удалить добавленный блок через `корзина` или отменить несохраненные изменения.

### TC-ACPCP-040

**Название:** Отображение виджета Корзина для предыдущего паспорта

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `BSR 108`; `ATOM-042`; `SRC-013`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- `Клиент менял паспорт`: `Да`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. При необходимости нажать `Добавить паспорт`.

### Итоговый ожидаемый результат

Для блока предыдущего паспорта отображается виджет `корзина`.

### Постусловия

Удалить добавленный блок или отменить несохраненные изменения.

### TC-ACPCP-041

**Название:** Удаление блока предыдущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `BSR 109`; `ATOM-043`; `SRC-013`; `GAP-002`; `GAP-005`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- `Клиент менял паспорт`: `Да`.
- Добавлен блок предыдущего паспорта.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. Нажать `Добавить паспорт`.
3. Нажать виджет `корзина` у добавленного блока.

### Итоговый ожидаемый результат

Из формы удалены соответствующие поля добавленного блока предыдущего паспорта: `Серия`, `Номер`, `Код подразделения`, `Кем выдан`, `Дата выдачи`, `Ввести вручную подразделение`, а также связанные с добавленным блоком виджеты.

### Постусловия

Не требуются.

### TC-ACPCP-042

**Название:** Маркеры обязательности полей последнего предыдущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-046`; `SRC-014`; `SRC-015`; `SRC-016`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- `Клиент менял паспорт`: `Да`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. Проверить видимые UI-маркеры обязательности у полей последнего предыдущего паспорта.

### Итоговый ожидаемый результат

Поля `Серия`, `Номер`, `Дата выдачи` последнего предыдущего паспорта имеют видимый UI-маркер обязательного поля.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-043

**Название:** Редактируемость полей последнего предыдущего паспорта

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `BSR 114`; `ATOM-052`; `SRC-014`; `SRC-015`; `SRC-016`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Серия: `4321`.
- Номер: `654321`.
- Дата выдачи: `01.06.2020`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. Ввести тестовые значения в поля последнего предыдущего паспорта.

### Итоговый ожидаемый результат

Поля `Серия`, `Номер`, `Дата выдачи` последнего предыдущего паспорта отображают введенные значения и доступны для пользовательского изменения.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-044

**Название:** Допустимый формат серии последнего предыдущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `BSR 111`; `ATOM-047`; `SRC-014`; `GAP-002`; `GAP-003`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Серия: `4321`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. В поле `Серия` последнего предыдущего паспорта ввести `4321`.

### Итоговый ожидаемый результат

Поле `Серия` последнего предыдущего паспорта отображает значение `4321`.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-045

**Название:** Запрет трех одинаковых цифр подряд в серии предыдущего паспорта

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `BSR 111`; `ATOM-048`; `SRC-014`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Серия с тремя одинаковыми цифрами подряд: `2223`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. В поле `Серия` последнего предыдущего паспорта ввести `2223`.
3. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Отображается валидация для поля `Серия` последнего предыдущего паспорта, сохранение формы запрещено.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-046

**Название:** Допустимый формат номера последнего предыдущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `BSR 113`; `ATOM-049`; `SRC-015`; `GAP-002`; `GAP-003`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Номер: `654321`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. В поле `Номер` последнего предыдущего паспорта ввести `654321`.

### Итоговый ожидаемый результат

Поле `Номер` последнего предыдущего паспорта отображает значение `654321`.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-047

**Название:** Запрет шести одинаковых цифр подряд в номере предыдущего паспорта

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `BSR 113`; `ATOM-050`; `SRC-015`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Номер из шести одинаковых цифр: `222222`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. В поле `Номер` последнего предыдущего паспорта ввести `222222`.
3. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Отображается валидация для поля `Номер` последнего предыдущего паспорта, сохранение формы запрещено.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-048

**Название:** Ввод даты выдачи последнего предыдущего паспорта

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `BSR 114`; `ATOM-052`; `SRC-016`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Дата выдачи предыдущего паспорта: `01.06.2020`.

### Шаги

1. Установить `Клиент менял паспорт` в значение `Да`.
2. В поле `Дата выдачи` последнего предыдущего паспорта ввести `01.06.2020`.

### Итоговый ожидаемый результат

Поле `Дата выдачи` последнего предыдущего паспорта отображает дату `01.06.2020`.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-049

**Название:** Числовой класс полей текущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 91`; `BSR 88`; `BSR 85`; `ATOM-053`; `SRC-002`; `SRC-003`; `SRC-004`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Серия: `1234`.
- Номер: `123456`.
- Код подразделения: `770001`.

### Шаги

1. Ввести числовые значения в поля `Серия`, `Номер`, `Код подразделения`.

### Итоговый ожидаемый результат

Поля `Серия`, `Номер`, `Код подразделения` отображают введенные числовые значения.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-050

**Название:** Числовой класс полей последнего предыдущего паспорта

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `BSR 113`; `BSR 111`; `ATOM-054`; `SRC-014`; `SRC-015`; `GAP-002`; `GAP-003`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Поле `Клиент менял паспорт` установлено в значение `Да`.

### Тестовые данные

- Серия: `4321`.
- Номер: `654321`.

### Шаги

1. Ввести числовые значения в поля `Серия`, `Номер` последнего предыдущего паспорта.

### Итоговый ожидаемый результат

Поля `Серия`, `Номер` последнего предыдущего паспорта отображают введенные числовые значения.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-051

**Название:** Запрет недопустимой длины и нечисловых символов в серии текущего паспорта

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 91`; `BSR 88`; `BSR 85`; `ATOM-053`; `SRC-002`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Проверяемые значения серии: `123`, `12345`, `12A4`, `12 4`, `+123`, `12.4`.

### Шаги

1. Для каждого проверяемого значения ввести значение в поле `Серия`.
2. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Для каждого проверяемого значения отображается валидация поля `Серия`, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-052

**Название:** Запрет недопустимой длины и нечисловых символов в номере текущего паспорта

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 91`; `BSR 85`; `BSR 88`; `ATOM-053`; `SRC-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Проверяемые значения номера: `12345`, `1234567`, `12345A`, `123 45`, `+12345`, `123.45`.

### Шаги

1. Для каждого проверяемого значения ввести значение в поле `Номер`.
2. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Для каждого проверяемого значения отображается валидация поля `Номер`, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-053

**Название:** Запрет недопустимой длины и нечисловых символов в коде подразделения

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `BSR 88`; `BSR 85`; `BSR 91`; `ATOM-053`; `SRC-004`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Проверяемые значения кода подразделения: `12345`, `1234567`, `12345A`, `123 45`, `+12345`, `123.45`.

### Шаги

1. Для каждого проверяемого значения ввести значение в поле `Код подразделения`.
2. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Для каждого проверяемого значения отображается валидация поля `Код подразделения`, сохранение формы запрещено.

### Постусловия

Отменить несохраненные изменения.

### TC-ACPCP-054

**Название:** Запрет недопустимой длины и нечисловых символов в серии последнего предыдущего паспорта

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `BSR 113`; `BSR 111`; `ATOM-054`; `SRC-014`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Поле `Клиент менял паспорт` установлено в значение `Да`.

### Тестовые данные

- Проверяемые значения серии: `321`, `32109`, `32A1`, `32 1`, `+321`, `32.1`.

### Шаги

1. Для каждого проверяемого значения ввести значение в поле `Серия` последнего предыдущего паспорта.
2. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Для каждого проверяемого значения отображается валидация поля `Серия` последнего предыдущего паспорта, сохранение формы запрещено.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

### TC-ACPCP-055

**Название:** Запрет недопустимой длины и нечисловых символов в номере последнего предыдущего паспорта

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `BSR 111`; `BSR 113`; `ATOM-054`; `SRC-015`; `GAP-002`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Поле `Клиент менял паспорт` установлено в значение `Да`.

### Тестовые данные

- Проверяемые значения номера: `65432`, `6543217`, `65432A`, `654 32`, `+65432`, `654.32`.

### Шаги

1. Для каждого проверяемого значения ввести значение в поле `Номер` последнего предыдущего паспорта.
2. Попытаться сохранить форму.

### Итоговый ожидаемый результат

Для каждого проверяемого значения отображается валидация поля `Номер` последнего предыдущего паспорта, сохранение формы запрещено.

### Постусловия

Вернуть значение `Клиент менял паспорт` в `Нет` или отменить несохраненные изменения.

## 5. Адрес регистрации и фактический адрес

Источник порядка: Таблица 4, rows 032-057.

### TC-ACCA-001

**Название:** Отображение блока адресов клиента и поля адреса регистрации
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `ATOM-001`; `ATOM-002`; `ATOM-008`; `SRC-001`; `SRC-002`; `SRC-003`; `BSR 115`; `BSR 121`

### Предусловия

Открыта карточка заявки на блоке `Адреса клиента`.

### Тестовые данные

Заявка с доступной анкетой клиента.

### Шаги

1. Открыть блок `Адреса клиента`.
2. Проверить наличие поля `Адрес регистрации` и переключателя `Адрес регистрации / Ввести вручную`.

### Итоговый ожидаемый результат

В блоке отображается поле `Адрес регистрации`, а переключатель `Адрес регистрации / Ввести вручную` доступен для просмотра.

### Постусловия

Не требуются

### TC-ACCA-001A

**Название:** Адрес регистрации обязателен только при вводе через DaData
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `SRC-002`; `BSR 115`

### Предусловия

Открыта карточка заявки на блоке `Адреса клиента`.

### Тестовые данные

Не требуются.

### Шаги

1. Установить `Адрес регистрации / Ввести вручную = Нет`.
2. Проверить обязательность поля `Адрес регистрации`.
3. Установить `Адрес регистрации / Ввести вручную = Да`.
4. Проверить обязательность поля `Адрес регистрации`.

### Итоговый ожидаемый результат

При `Адрес регистрации / Ввести вручную = Нет` поле `Адрес регистрации` обязательно. При `Адрес регистрации / Ввести вручную = Да` обязательность переносится на ручные поля адреса, а основное поле `Адрес регистрации` не должно требовать отдельного ручного заполнения.

### Постусловия

Вернуть `Адрес регистрации / Ввести вручную = Нет` при необходимости

### TC-ACCA-002

**Название:** Разложение найденного DaData адреса регистрации по ручным полям
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `ATOM-003`; `ATOM-006`; `SRC-002`; `BSR 116`; `BSR 119`

### Предусловия

Поле `Адрес регистрации` отображается, `Адрес регистрации / Ввести вручную = Нет`.

### Тестовые данные

Адрес, который DaData находит и который содержит регион, город, улицу, дом и квартиру.

### Шаги

1. Начать ввод найденного адреса в поле `Адрес регистрации`.
2. Выбрать предложенный DaData адрес.
3. Открыть ручной ввод для адреса регистрации.

### Итоговый ожидаемый результат

Компоненты выбранного адреса регистрации отображаются в соответствующих ручных полях адреса регистрации.

### Постусловия

Очистить введенный адрес при необходимости

### TC-ACCA-003

**Название:** Подсказка при ненайденном DaData адресе регистрации
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `ATOM-003`; `ATOM-005`; `SRC-002`; `BSR 116`; `BSR 118`

### Предусловия

Поле `Адрес регистрации` отображается, `Адрес регистрации / Ввести вручную = Нет`.

### Тестовые данные

Адресная строка, для которой DaData не находит адрес.

### Шаги

1. Ввести в поле `Адрес регистрации` адресную строку, не найденную DaData.
2. Завершить ввод поля адреса регистрации.

### Итоговый ожидаемый результат

У поля `Адрес регистрации` отображается подсказка `Некорректно указан адрес`.

### Постусловия

Очистить введенный адрес при необходимости

### TC-ACCA-004

**Название:** Подсказка при адресе регистрации без квартиры
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `ATOM-004`; `SRC-002`; `BSR 117`; `GAP-001`

### Предусловия

Поле `Адрес регистрации` отображается, `Клиент зарегистрирован в частном доме` не активирован.

### Тестовые данные

Адрес регистрации с регионом и домом, но без квартиры.

### Шаги

1. Ввести или выбрать адрес регистрации без квартиры.
2. Не активировать флажок `Клиент зарегистрирован в частном доме`.

### Итоговый ожидаемый результат

Поле адреса регистрации подсвечено красным и отображает подсказку `Укажите номер квартиры или поставьте отметку о проживании в частном доме`.

### Постусловия

Очистить введенный адрес при необходимости

### TC-ACCA-005

**Название:** Значение по умолчанию переключателя ручного ввода адреса регистрации
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** `ATOM-009`; `SRC-003`; `BSR 122`

### Предусловия

Открыта карточка заявки на блоке `Адреса клиента`.

### Тестовые данные

Новая или очищенная анкета клиента.

### Шаги

1. Открыть блок `Адреса клиента`.
2. Проверить значение переключателя `Адрес регистрации / Ввести вручную` до действий пользователя.

### Итоговый ожидаемый результат

Переключатель `Адрес регистрации / Ввести вручную` имеет значение `Нет`.

### Постусловия

Не требуются

### TC-ACCA-006

**Название:** Автосборка адреса регистрации при ручном заполнении
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `ATOM-007`; `SRC-002`; `BSR 120`

### Предусловия

`Адрес регистрации / Ввести вручную = Да`.

### Тестовые данные

Почтовый индекс `123456`, регион `Москва`, район `Тверской`, населенный пункт пустой, город `Москва`, улица `Тверская`, дом `1`, корпус `2`, квартира `3`.

### Шаги

1. Включить `Адрес регистрации / Ввести вручную`.
2. Заполнить ручные поля адреса регистрации тестовыми данными.
3. Проверить значение поля `Адрес регистрации`.

### Итоговый ожидаемый результат

Поле `Адрес регистрации` автоматически заполнено значениями ручных полей в порядке: почтовый индекс, регион, район при наличии, населенный пункт при наличии, город, улица, дом, корпус при наличии, квартира при наличии.

### Постусловия

Очистить ручные поля адреса регистрации при необходимости

### TC-ACCA-007

**Название:** Отображение ручных полей адреса регистрации
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `ATOM-010`; `SRC-004`..`SRC-012`; `BSR 123`; `BSR 125`; `BSR 126`; `BSR 127`; `BSR 128`; `BSR 129`; `BSR 130`; `BSR 131`; `BSR 133`

### Предусловия

`Адрес регистрации / Ввести вручную = Нет`.

### Тестовые данные

Новая или очищенная анкета клиента.

### Шаги

1. Включить `Адрес регистрации / Ввести вручную`.
2. Проверить состав ручных полей адреса регистрации.

### Итоговый ожидаемый результат

Отображаются ручные поля адреса регистрации: почтовый индекс, регион, район, населенный пункт, город, улица, дом, корпус и квартира.

### Постусловия

Вернуть `Адрес регистрации / Ввести вручную = Нет` при необходимости

### TC-ACCA-008

**Название:** Обязательность региона и дома в ручном адресе регистрации
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `ATOM-012`; `SRC-005`; `SRC-010`

### Предусловия

`Адрес регистрации / Ввести вручную = Да`.

### Тестовые данные

Новая или очищенная анкета клиента.

### Шаги

1. Открыть ручной ввод адреса регистрации.
2. Проверить маркер обязательности у полей `Адрес регистрации / Регион` и `Адрес регистрации / Дом`.
3. Открыть список поля `Адрес регистрации / Регион`.

### Итоговый ожидаемый результат

У полей `Адрес регистрации / Регион` и `Адрес регистрации / Дом` отображается маркер обязательности. Поле `Адрес регистрации / Регион` является списком и использует значения из справочника регионов.

### Постусловия

Не требуются

### TC-ACCA-009

**Название:** Маркеры условной обязательности населенного пункта и города адреса регистрации
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `ATOM-013`; `ATOM-014`; `SRC-007`; `SRC-008`

### Предусловия

`Адрес регистрации / Ввести вручную = Да`.

### Тестовые данные

Новая или очищенная анкета клиента.

### Шаги

1. Оставить пустым `Адрес регистрации / Город` и проверить маркер обязательности у `Адрес регистрации / Населенный пункт`.
2. Заполнить `Адрес регистрации / Город`, затем оставить пустым `Адрес регистрации / Населенный пункт` и проверить маркер обязательности у `Адрес регистрации / Город`.

### Итоговый ожидаемый результат

У поля `Адрес регистрации / Населенный пункт` отображается маркер обязательности при пустом городе, а у поля `Адрес регистрации / Город` отображается маркер обязательности при пустом населенном пункте.

### Постусловия

Восстановить исходные значения при необходимости

### TC-ACCA-010

**Название:** Допустимые числовые значения в ручных числовых полях адреса регистрации
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** `ATOM-011`; `SRC-004`; `SRC-011`; `SRC-012`; `BSR 124`; `BSR 132`; `BSR 134`; `GAP-002`

### Предусловия

`Адрес регистрации / Ввести вручную = Да`.

### Тестовые данные

Почтовый индекс `123456`, корпус `2`, квартира `34`.

### Шаги

1. Ввести тестовые числовые значения в поля `Адрес регистрации / Почтовый индекс`, `Адрес регистрации / Корпус`, `Адрес регистрации / Квартира`.

### Итоговый ожидаемый результат

Поля принимают указанные числовые значения; отдельный expected result по invalid feedback не проверяется.

### Постусловия

Очистить значения при необходимости

### TC-ACCA-011

**Название:** Подсказка о квартире в ручном адресе регистрации при невыбранном частном доме
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `ATOM-004`; `ATOM-015`; `SRC-002`; `SRC-012`; `BSR 117`; `GAP-001`

### Предусловия

`Адрес регистрации / Ввести вручную = Да`, `Клиент зарегистрирован в частном доме = Нет`.

### Тестовые данные

Ручной адрес регистрации с регионом и домом, но без квартиры.

### Шаги

1. Заполнить ручной адрес регистрации без квартиры.
2. Оставить флажок `Клиент зарегистрирован в частном доме` неактивным.

### Итоговый ожидаемый результат

Поле адреса регистрации подсвечено красным и отображает подсказку `Укажите номер квартиры или поставьте отметку о проживании в частном доме`.

### Постусловия

Очистить значения при необходимости

### TC-ACCA-012

**Название:** Отображение и default флажка частного дома для адреса регистрации
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** `ATOM-016`; `ATOM-017`; `SRC-013`; `BSR 135`; `BSR 136`

### Предусловия

В адресе регистрации не указан номер квартиры.

### Тестовые данные

Адрес регистрации без квартиры.

### Шаги

1. Ввести адрес регистрации без квартиры.
2. Проверить флажок `Клиент зарегистрирован в частном доме`.

### Итоговый ожидаемый результат

Флажок `Клиент зарегистрирован в частном доме` отображается и имеет значение по умолчанию `Нет`.

### Постусловия

Очистить адрес при необходимости

### TC-ACCA-013

**Название:** Снятие подсказки о квартире при отметке частного дома регистрации
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `ATOM-018`; `SRC-013`; `BSR 137`

### Предусловия

У поля `Адрес регистрации` отображается подсказка о квартире.

### Тестовые данные

Адрес регистрации без квартиры.

### Шаги

1. Активировать флажок `Клиент зарегистрирован в частном доме`.

### Итоговый ожидаемый результат

Подсказка `Укажите номер квартиры или поставьте отметку о проживании в частном доме` исчезает с поля адреса регистрации.

### Постусловия

Вернуть флажок в исходное состояние при необходимости

### TC-ACCA-014

**Название:** Default совпадения фактического адреса с адресом регистрации
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** `ATOM-019`; `ATOM-020`; `SRC-014`; `BSR 138`; `BSR 139`

### Предусловия

Открыта карточка заявки на блоке `Адреса клиента`.

### Тестовые данные

Новая или очищенная анкета клиента.

### Шаги

1. Проверить переключатель `Адрес фактического места жительства совпадает с адресом регистрации` до действий пользователя.

### Итоговый ожидаемый результат

Переключатель отображается и имеет значение по умолчанию `Да`.

### Постусловия

Не требуются

### TC-ACCA-015

**Название:** Отображение фактического адреса при несовпадении с регистрацией
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** `ATOM-021`; `ATOM-027`; `ATOM-028`; `SRC-015`; `SRC-017`; `BSR 140`; `BSR 148`; `BSR 149`

### Предусловия

`Адрес фактического места жительства совпадает с адресом регистрации = Да`.

### Тестовые данные

Новая или очищенная анкета клиента.

### Шаги

1. Установить `Адрес фактического места жительства совпадает с адресом регистрации = Нет`.
2. Проверить поле `Адрес фактического места жительства` и переключатель `Фактический адрес / Ввести вручную`.

### Итоговый ожидаемый результат

Поле `Адрес фактического места жительства` отображается, а переключатель `Фактический адрес / Ввести вручную` отображается со значением `Нет`.

### Постусловия

Вернуть переключатель совпадения в исходное состояние при необходимости

### TC-ACCA-016

**Название:** Разложение найденного DaData фактического адреса по ручным полям
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** `ATOM-022`; `ATOM-025`; `SRC-015`; `BSR 141`; `BSR 144`

### Предусловия

`Адрес фактического места жительства совпадает с адресом регистрации = Нет`, `Фактический адрес / Ввести вручную = Нет`.

### Тестовые данные

Фактический адрес, который DaData находит и который содержит регион, город, улицу, дом и квартиру.

### Шаги

1. Начать ввод найденного адреса в поле `Адрес фактического места жительства`.
2. Выбрать предложенный DaData адрес.
3. Открыть ручной ввод фактического адреса.

### Итоговый ожидаемый результат

Компоненты выбранного фактического адреса отображаются в соответствующих ручных полях фактического адреса.

### Постусловия

Очистить введенный адрес при необходимости

### TC-ACCA-017

**Название:** Подсказка при ненайденном DaData фактическом адресе
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** `ATOM-022`; `ATOM-024`; `SRC-015`; `BSR 141`; `BSR 143`

### Предусловия

`Адрес фактического места жительства совпадает с адресом регистрации = Нет`, `Фактический адрес / Ввести вручную = Нет`.

### Тестовые данные

Адресная строка, для которой DaData не находит адрес.

### Шаги

1. Ввести в поле `Адрес фактического места жительства` адресную строку, не найденную DaData.
2. Завершить ввод поля фактического адреса.

### Итоговый ожидаемый результат

У поля `Адрес фактического места жительства` отображается подсказка `Некорректно указан адрес`.

### Постусловия

Очистить введенный адрес при необходимости

### TC-ACCA-018

**Название:** Подсказка при фактическом адресе без квартиры
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** `ATOM-023`; `SRC-015`; `BSR 142`; `GAP-001`

### Предусловия

`Адрес фактического места жительства совпадает с адресом регистрации = Нет`, `Клиент проживает в частном доме = Нет`.

### Тестовые данные

Фактический адрес с регионом и домом, но без квартиры.

### Шаги

1. Ввести или выбрать фактический адрес без квартиры.
2. Не активировать флажок `Клиент проживает в частном доме`.

### Итоговый ожидаемый результат

Поле фактического адреса подсвечено красным и отображает подсказку `Укажите номер квартиры или поставьте отметку о проживании в частном доме`.

### Постусловия

Очистить введенный адрес при необходимости

### TC-ACCA-019

**Название:** Отображение ручных полей фактического адреса
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** `ATOM-029`; `SRC-018`..`SRC-026`; `BSR 150`; `BSR 151`; `BSR 152`; `BSR 153`; `BSR 154`; `BSR 155`; `BSR 156`; `BSR 158`; `BSR 160`

### Предусловия

`Адрес фактического места жительства совпадает с адресом регистрации = Нет`, `Фактический адрес / Ввести вручную = Нет`.

### Тестовые данные

Новая или очищенная анкета клиента.

### Шаги

1. Включить `Фактический адрес / Ввести вручную`.
2. Проверить состав ручных полей фактического адреса.

### Итоговый ожидаемый результат

Отображаются ручные поля фактического адреса: регион, район, населенный пункт, город, улица, дом, корпус, квартира и почтовый индекс.

### Постусловия

Вернуть `Фактический адрес / Ввести вручную = Нет` при необходимости

### TC-ACCA-026

**Название:** Адрес из DaData заполняет kladr в модели данных
**Тип:** Integration
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** `SRC-002`; `SRC-015`; `BSR 325`

### Предусловия

Открыта карточка заявки на блоке `Адреса клиента`; интеграция DaData доступна на стенде; доступен наблюдаемый источник проверки модели данных заявки: API, БД, лог или иной согласованный evidence source.

### Тестовые данные

Адрес регистрации или фактического места жительства, найденный через DaData и содержащий известный код КЛАДР.

### Шаги

1. Установить для проверяемого адреса `Ввести вручную = Нет`.
2. Начать ввод адреса и выбрать предложенный DaData адрес.
3. Сохранить заявку или выполнить действие, после которого модель данных доступна для проверки.
4. Проверить поле `kladr` в модели данных заявки через evidence source.

### Итоговый ожидаемый результат

Если адрес клиента заполнен выбором из DaData, в модели данных заявки заполнено поле `kladr` с кодом КЛАДР выбранного адреса.

### Постусловия

Очистить адрес или отменить несохраненные изменения при необходимости.

### TC-ACCA-020

**Название:** Автосборка фактического адреса при ручном заполнении
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** `ATOM-026`; `SRC-015`; `BSR 145`

### Предусловия

`Фактический адрес / Ввести вручную = Да`.

### Тестовые данные

Почтовый индекс `654321`, регион `Красноярский край`, район пустой, населенный пункт пустой, город `Красноярск`, улица `Мира`, дом `10`, корпус пустой, квартира `20`.

### Шаги

1. Заполнить ручные поля фактического адреса тестовыми данными.
2. Проверить значение поля `Адрес фактического места жительства`.

### Итоговый ожидаемый результат

Поле `Адрес фактического места жительства` автоматически заполнено значениями ручных полей в порядке: почтовый индекс, регион, район при наличии, населенный пункт при наличии, город, улица, дом, корпус при наличии, квартира при наличии.

### Постусловия

Очистить ручные поля фактического адреса при необходимости

### TC-ACCA-021

**Название:** Обязательность ключевых ручных полей фактического адреса
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** `ATOM-030`; `SRC-018`; `SRC-021`; `SRC-022`; `SRC-023`

### Предусловия

`Фактический адрес / Ввести вручную = Да`.

### Тестовые данные

Новая или очищенная анкета клиента.

### Шаги

1. Проверить маркер обязательности у полей `Фактический адрес / Регион`, `Фактический адрес / Город`, `Фактический адрес / Улица`, `Фактический адрес / Дом`.
2. Открыть список поля `Фактический адрес / Регион`.

### Итоговый ожидаемый результат

У полей `Фактический адрес / Регион`, `Фактический адрес / Город`, `Фактический адрес / Улица` и `Фактический адрес / Дом` отображается маркер обязательности. Поле `Фактический адрес / Регион` является списком и использует значения из справочника регионов.

### Постусловия

Не требуются

### TC-ACCA-022

**Название:** Допустимые числовые значения в ручных числовых полях фактического адреса
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** `ATOM-032`; `SRC-025`; `SRC-026`; `BSR 159`; `BSR 161`; `GAP-002`

### Предусловия

`Фактический адрес / Ввести вручную = Да`.

### Тестовые данные

Квартира `45`, почтовый индекс `654321`.

### Шаги

1. Ввести тестовые числовые значения в поля `Фактический адрес / Квартира` и `Фактический адрес / Почтовый индекс`.

### Итоговый ожидаемый результат

Поля принимают указанные числовые значения; отдельный expected result по invalid feedback не проверяется.

### Постусловия

Очистить значения при необходимости

### TC-ACCA-023

**Название:** Подсказка о квартире в ручном фактическом адресе при невыбранном частном доме
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** `ATOM-023`; `ATOM-031`; `SRC-015`; `SRC-025`; `BSR 142`; `BSR 157`; `GAP-001`

### Предусловия

`Фактический адрес / Ввести вручную = Да`, `Клиент проживает в частном доме = Нет`.

### Тестовые данные

Ручной фактический адрес с регионом и домом, но без квартиры.

### Шаги

1. Заполнить ручной фактический адрес без квартиры.
2. Оставить флажок `Клиент проживает в частном доме` неактивным.

### Итоговый ожидаемый результат

Поле фактического адреса подсвечено красным и отображает подсказку `Укажите номер квартиры или поставьте отметку о проживании в частном доме`.

### Постусловия

Очистить значения при необходимости

### TC-ACCA-024

**Название:** Отображение и default флажка частного дома для фактического адреса
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** `ATOM-033`; `ATOM-034`; `SRC-016`; `BSR 146`; `BSR 147`

### Предусловия

`Адрес фактического места жительства совпадает с адресом регистрации = Нет`; в фактическом адресе не указан номер квартиры.

### Тестовые данные

Фактический адрес без квартиры.

### Шаги

1. Ввести фактический адрес без квартиры.
2. Проверить флажок `Клиент проживает в частном доме`.

### Итоговый ожидаемый результат

Флажок `Клиент проживает в частном доме` отображается и имеет значение по умолчанию `Нет`.

### Постусловия

Очистить адрес при необходимости

### TC-ACCA-025

**Название:** Фактический адрес обязателен только при вводе через DaData
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** `SRC-015`; `BSR 140`

### Предусловия

`Адрес фактического места жительства совпадает с адресом регистрации = Нет`.

### Тестовые данные

Не требуются.

### Шаги

1. Установить `Фактический адрес / Ввести вручную = Нет`.
2. Проверить обязательность поля `Адрес фактического места жительства`.
3. Установить `Фактический адрес / Ввести вручную = Да`.
4. Проверить обязательность поля `Адрес фактического места жительства`.

### Итоговый ожидаемый результат

При `Фактический адрес / Ввести вручную = Нет` поле `Адрес фактического места жительства` обязательно. При `Фактический адрес / Ввести вручную = Да` обязательность переносится на ручные поля фактического адреса, а основное поле `Адрес фактического места жительства` не должно требовать отдельного ручного заполнения.

### Постусловия

Вернуть `Фактический адрес / Ввести вручную = Нет` при необходимости

## 6. Контакты клиента, контактные лица и дополнительная информация

Источник порядка: Таблица 4, rows 058-080.

### TC-ACCEI-001

**Название:** Отображение блоков контактов и дополнительной информации

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-001`; `ATOM-002`; `ATOM-022`; `SRC-001`; `SRC-002`; `SRC-003`; `SRC-017`; `BSR 162`; `BSR 165`; `WP-01`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к области анкеты с блоками `Контакты клиента` и `Дополнительная информация`.

### Итоговый ожидаемый результат

Отображаются блоки `Контакты клиента` и `Дополнительная информация`; в блоке `Контакты клиента` отображаются поля `Мобильный телефон` и `E-mail`.

### Постусловия

Не требуются.

### TC-ACCEI-002

**Название:** Маркеры обязательности всегда отображаемых полей контактов и дополнительной информации

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-003`; `ATOM-028`; `ATOM-033`; `ATOM-035`; `ATOM-037`; `SRC-002`; `SRC-003`; `SRC-018`; `SRC-021`; `SRC-022`; `SRC-023`; `BSR 163`; `BSR 166`; `BSR 191`; `BSR 202`; `BSR 203`; `BSR 204`; `WP-01`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блокам `Контакты клиента` и `Дополнительная информация`.
2. Проверить видимые UI-маркеры обязательности у полей `Мобильный телефон`, `E-mail`, `ИНН клиента`, `Кодовое слово`, `Семейное положение`, `Количество иждивенцев`.

### Итоговый ожидаемый результат

Поля `Мобильный телефон`, `ИНН клиента`, `Семейное положение`, `Количество иждивенцев` имеют видимый UI-маркер обязательного поля; поля `E-mail` и `Кодовое слово` не имеют видимого UI-маркера обязательного поля.

### Постусловия

Не требуются.

### TC-ACCEI-003

**Название:** Редактируемость полей контактов и дополнительной информации кроме ИНН клиента

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-004`; `ATOM-028`; `ATOM-034`; `ATOM-036`; `ATOM-038`; `SRC-002`; `SRC-003`; `SRC-018`; `SRC-021`; `SRC-022`; `SRC-023`; `BSR 164`; `BSR 166`; `BSR 192`; `BSR 202`; `BSR 203`; `BSR 204`; `WP-01`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Мобильный телефон: `9123456789`.
- E-mail: `client@example.test`.
- Кодовое слово: `Север`.
- Семейное положение: `Холост / не замужем`.
- Количество иждивенцев: `2`.

### Шаги

1. Ввести или выбрать указанные тестовые значения в полях `Мобильный телефон`, `E-mail`, `Кодовое слово`, `Семейное положение`, `Количество иждивенцев`.

### Итоговый ожидаемый результат

Поля `Мобильный телефон`, `E-mail`, `Кодовое слово`, `Семейное положение`, `Количество иждивенцев` доступны для пользовательского изменения и отображают введенные или выбранные значения. Редактируемость поля `ИНН клиента` проверяется отдельными кейсами, так как по ФТ поле доступно для ручного изменения только при незаполнении через интеграцию.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACCEI-004

**Название:** Скрытие полей дополнительного телефона до добавления повторителя

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-007`; `SRC-004`; `SRC-005`; `BSR 167`; `BSR 168`; `GAP-002`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В текущей карточке не создан дополнительный телефон в рамках текущей проверки.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Контакты клиента`.
2. Проверить состояние полей дополнительного телефона до нажатия `Добавить телефон`.

### Итоговый ожидаемый результат

Поля дополнительного телефона `Тип телефона` и `Номер телефона` не отображаются.

### Постусловия

Не требуются.

### TC-ACCEI-005

**Название:** Ввод 10 цифр и отображение шаблона +7 (9xx) xxx-xx-xx в поле Мобильный телефон

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-005`; `SRC-002`; `BSR 164`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Мобильный телефон: `9123456789`.

### Шаги

1. Перейти к полю `Мобильный телефон`.
2. Ввести `9123456789`.

### Итоговый ожидаемый результат

Поле `Мобильный телефон` отображает введенный телефон в шаблоне `+7 (9xx) xxx-xx-xx`.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACCEI-006

**Название:** Ввод одного e-mail адреса с символом @

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-006`; `SRC-003`; `BSR 166`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- E-mail: `client@example.test`.

### Шаги

1. Перейти к полю `E-mail`.
2. Ввести `client@example.test`.

### Итоговый ожидаемый результат

Поле `E-mail` отображает один введенный адрес `client@example.test`.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACCEI-007

**Название:** Добавление блока дополнительного телефона

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-008`; `SRC-004`; `SRC-005`; `SRC-006`; `BSR 167`; `BSR 168`; `BSR 171`; `GAP-002`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В текущей карточке не создан дополнительный телефон в рамках текущей проверки.

### Тестовые данные

Не требуются.

### Шаги

1. В блоке `Контакты клиента` нажать `Добавить телефон`.

### Итоговый ожидаемый результат

Отображается новый блок-повторитель дополнительного телефона с полями `Тип телефона` и `Номер телефона`.

### Постусловия

Удалить созданный блок дополнительного телефона через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-008

**Название:** UI-маркеры взаимной обязательности полей дополнительного телефона

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-009`; `SRC-004`; `SRC-005`; `BSR 167`; `BSR 169`; `DICT-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контакты клиента` создан один блок дополнительного телефона.

### Тестовые данные

- Тип телефона: `Мобильный` из `DICT-001`.
- Номер телефона: `9234567890`.

### Шаги

1. В созданном блоке дополнительного телефона заполнить `Номер телефона` значением `9234567890` и проверить видимый UI-маркер обязательности у поля `Тип телефона`.
2. Очистить `Номер телефона`.
3. Выбрать `Тип телефона = Мобильный` и проверить видимый UI-маркер обязательности у поля `Номер телефона`.

### Итоговый ожидаемый результат

После заполнения `Номер телефона` поле `Тип телефона` имеет видимый UI-маркер обязательного поля; после выбора `Тип телефона = Мобильный` поле `Номер телефона` имеет видимый UI-маркер обязательного поля.

### Постусловия

Удалить созданный блок дополнительного телефона через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-009

**Название:** Значения справочника Тип телефона

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-010`; `SRC-004`; `BSR 167`; `DICT-001`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контакты клиента` создан один блок дополнительного телефона.

### Тестовые данные

- Активные значения `DICT-001`: `Мобильный`, `Домашний`, `Рабочий`.

### Шаги

1. Открыть список поля `Тип телефона` в созданном блоке дополнительного телефона.

### Итоговый ожидаемый результат

В списке `Тип телефона` доступны все и только активные значения `DICT-001`: `Мобильный`, `Домашний`, `Рабочий`.

### Постусловия

Удалить созданный блок дополнительного телефона через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-010

**Название:** Ввод 10 цифр и отображение шаблона +7 (9xx) xxx-xx-xx в номер дополнительного телефона

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-011`; `SRC-005`; `BSR 170`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контакты клиента` создан один блок дополнительного телефона.

### Тестовые данные

- Номер телефона: `9234567890`.

### Шаги

1. В поле `Номер телефона` созданного блока ввести `9234567890`.

### Итоговый ожидаемый результат

Поле `Номер телефона` отображает введенный телефон в шаблоне `+7 (9xx) xxx-xx-xx`.

### Постусловия

Удалить созданный блок дополнительного телефона через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-011

**Название:** Удаление созданного блока дополнительного телефона

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-012`; `SRC-007`; `BSR 172`; `GAP-002`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контакты клиента` создан один блок дополнительного телефона.

### Тестовые данные

Не требуются.

### Шаги

1. В созданном блоке дополнительного телефона нажать `Корзина`.

### Итоговый ожидаемый результат

Созданный блок дополнительного телефона с полями `Тип телефона` и `Номер телефона` не отображается.

### Постусловия

Не требуются.

### TC-ACCEI-012

**Название:** Добавление строки контактного лица

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-013`; `ATOM-014`; `SRC-008`; `SRC-009`; `SRC-010`; `SRC-011`; `SRC-012`; `SRC-013`; `SRC-014`; `SRC-015`; `BSR 173`; `BSR 175`; `BSR 177`; `BSR 179`; `BSR 181`; `BSR 184`; `BSR 186`; `BSR 187`; `GAP-002`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В текущей карточке не создана строка контактного лица в рамках текущей проверки.

### Тестовые данные

Не требуются.

### Шаги

1. В блоке `Контактные лица` нажать `Добавить контактное лицо`.

### Итоговый ожидаемый результат

Отображается новая строка контактного лица с полями `Фамилия`, `Имя`, `Отчество`, `Отношение к заявителю`, `Телефон`, `Дата рождения` и виджетами `+` и `Корзина`.

### Постусловия

Удалить созданную строку контактного лица через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-013

**Название:** Маркеры обязательности полей созданной строки контактного лица

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-015`; `SRC-009`; `SRC-010`; `SRC-011`; `SRC-012`; `SRC-013`; `SRC-014`; `BSR 173`; `BSR 175`; `BSR 177`; `BSR 179`; `BSR 182`; `BSR 184`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контактные лица` создана одна строка контактного лица.

### Тестовые данные

Не требуются.

### Шаги

1. Проверить видимые UI-маркеры обязательности у полей созданной строки контактного лица.

### Итоговый ожидаемый результат

Поля `Фамилия`, `Имя`, `Отношение к заявителю`, `Телефон`, `Дата рождения` имеют видимый UI-маркер обязательного поля; поле `Отчество` не имеет видимого UI-маркера обязательного поля.

### Постусловия

Удалить созданную строку контактного лица через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-014

**Название:** Ввод текстовых значений с дефисом в ФИО контактного лица

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-016`; `SRC-009`; `SRC-010`; `SRC-011`; `BSR 174`; `BSR 176`; `BSR 178`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контактные лица` создана одна строка контактного лица.

### Тестовые данные

- Фамилия: `Петров-Сидоров`.
- Имя: `Анна-Мария`.
- Отчество: `Игоревна`.

### Шаги

1. В поле `Фамилия` созданной строки ввести `Петров-Сидоров`.
2. В поле `Имя` созданной строки ввести `Анна-Мария`.
3. В поле `Отчество` созданной строки ввести `Игоревна`.

### Итоговый ожидаемый результат

Поля `Фамилия`, `Имя`, `Отчество` отображают введенные текстовые значения; символ `-` сохранен в полях `Фамилия` и `Имя`.

### Постусловия

Удалить созданную строку контактного лица через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-015

**Название:** Значения справочника Отношение к заявителю

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-017`; `SRC-012`; `BSR 179`; `DICT-002`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контактные лица` создана одна строка контактного лица.

### Тестовые данные

- Активные значения `DICT-002`: `супруг/супруга`, `отец/мать`, `сестра/брат`, `теща/свекровь/тесть/свекр`, `сын/дочь`, `друг/знакомый/коллега`, `иное`.

### Шаги

1. Открыть список поля `Отношение к заявителю` в созданной строке контактного лица.

### Итоговый ожидаемый результат

В списке `Отношение к заявителю` доступны все и только активные значения `DICT-002`: `супруг/супруга`, `отец/мать`, `сестра/брат`, `теща/свекровь/тесть/свекр`, `сын/дочь`, `друг/знакомый/коллега`, `иное`.

### Постусловия

Удалить созданную строку контактного лица через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-016

**Название:** Отображение дополнительного текстового поля при отношении Иное

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-018`; `SRC-012`; `BSR 180`; `DICT-002`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контактные лица` создана одна строка контактного лица.

### Тестовые данные

- Отношение к заявителю: `иное`.

### Шаги

1. В поле `Отношение к заявителю` выбрать значение `иное`.

### Итоговый ожидаемый результат

В созданной строке контактного лица дополнительно отображается текстовое поле ввода для отношения к заявителю.

### Постусловия

Удалить созданную строку контактного лица через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-017

**Название:** Ввод 10 цифр и отображение шаблона +7 (9xx) xxx-xx-xx в телефон контактного лица

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-019`; `SRC-013`; `BSR 183`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контактные лица` создана одна строка контактного лица.

### Тестовые данные

- Телефон: `9345678901`.

### Шаги

1. В поле `Телефон` созданной строки контактного лица ввести `9345678901`.

### Итоговый ожидаемый результат

Поле `Телефон` отображает введенный телефон в шаблоне `+7 (9xx) xxx-xx-xx`.

### Постусловия

Удалить созданную строку контактного лица через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-018

**Название:** Ввод текущей даты в дату рождения контактного лица

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-020`; `SRC-014`; `BSR 185`; `GAP-003`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контактные лица` создана одна строка контактного лица.
- Текущая календарная дата стенда: `30.06.2026`.

### Тестовые данные

- Дата рождения: `30.06.2026`.

### Шаги

1. В поле `Дата рождения` созданной строки контактного лица ввести `30.06.2026`.

### Итоговый ожидаемый результат

Поле `Дата рождения` отображает введенную дату `30.06.2026`.

### Постусловия

Удалить созданную строку контактного лица через `Корзина` или закрыть заявку без сохранения.

### TC-ACCEI-019

**Название:** Удаление созданной строки контактного лица

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-021`; `SRC-016`; `BSR 188`; `BSR 189`; `GAP-002`; `WP-01`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- В блоке `Контактные лица` создана одна строка контактного лица.

### Тестовые данные

Не требуются.

### Шаги

1. В созданной строке контактного лица нажать `Корзина`.

### Итоговый ожидаемый результат

Созданная строка контактного лица со всеми ее полями не отображается.

### Постусловия

Не требуются.

### TC-ACCEI-020

**Название:** Ввод 12 цифр ИНН клиента и успешная верификация устанавливает ИНН проверен в Да

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-024`; `ATOM-025`; `ATOM-027`; `SRC-018`; `SRC-019`; `SRC-020`; `BSR 193`; `BSR 194`; `BSR 195`; `BSR 196`; `BSR 198`; `BSR 199`; `BSR 200`; `BSR 201`; `GAP-001`; `GAP-003`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Тестовый стенд верификации ИНН настроен так, что fixture `FX-001` возвращает успешную верификацию.
- Поле `ИНН клиента` не заполнилось автоматически, поэтому доступен ручной ввод и отображается кнопка `Верифицировать ИНН`.

### Тестовые данные

- ИНН клиента: `500100732259`.

### Шаги

1. В поле `ИНН клиента` ввести `500100732259`.
2. Нажать `Верифицировать ИНН`.

### Итоговый ожидаемый результат

Чекбокс `ИНН проверен` отображается в значении `Да`. После установки `ИНН проверен = Да` поля `Фамилия`, `Имя`, `Отчество`, `Дата рождения`, `Серия паспорта`, `Номер паспорта`, `Дата выдачи паспорта`, `Код подразделения паспорта`, `Кем выдан (паспорт)`, `Место рождения` становятся недоступными для редактирования.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACCEI-021

**Название:** Неуспешная верификация ИНН делает кнопку Далее недоступной

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-025`; `ATOM-026`; `SRC-018`; `SRC-019`; `BSR 194`; `BSR 195`; `BSR 196`; `GAP-001`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Тестовый стенд верификации ИНН настроен так, что fixture `FX-002` возвращает неуспешную верификацию.
- Поле `ИНН клиента` не заполнилось автоматически, поэтому доступен ручной ввод и отображается кнопка `Верифицировать ИНН`.

### Тестовые данные

- ИНН клиента: `111111111111`.

### Шаги

1. В поле `ИНН клиента` ввести `111111111111`.
2. Нажать `Верифицировать ИНН`.
3. Проверить доступность кнопки `Далее`.

### Итоговый ожидаемый результат

После неуспешной верификации ИНН кнопка `Далее` недоступна для нажатия.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACCEI-022

**Название:** Видимость чекбокса ИНН проверен до верификации

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-039`; `SRC-020`; `BSR 197`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к полю `ИНН проверен` в блоке `Дополнительная информация`.

### Итоговый ожидаемый результат

Чекбокс `ИНН проверен` отображается в блоке `Дополнительная информация`.

### Постусловия

Не требуются.

### TC-ACCEI-023

**Название:** Значения справочника Семейное положение

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-030`; `SRC-022`; `BSR 203`; `DICT-003`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Активные значения `DICT-003`: `Холост / не замужем`, `Женат / замужем`.

### Шаги

1. Открыть список поля `Семейное положение`.

### Итоговый ожидаемый результат

В списке `Семейное положение` доступны все и только активные значения `DICT-003`: `Холост / не замужем`, `Женат / замужем`.

### Постусловия

Не требуются.

### TC-ACCEI-024

**Название:** Ввод числового значения в количество иждивенцев

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-032`; `SRC-023`; `BSR 205`; `GAP-003`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

- Количество иждивенцев: `2`.

### Шаги

1. В поле `Количество иждивенцев` ввести `2`.
2. Проверить наличие кнопок увеличения и снижения значения у поля `Количество иждивенцев`.

### Итоговый ожидаемый результат

Поле `Количество иждивенцев` отображает значение `2` и содержит кнопки увеличения и снижения значения.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACCEI-025

**Название:** Отображение полей дополнительной информации клиента

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-023`; `ATOM-029`; `ATOM-031`; `SRC-018`; `SRC-022`; `SRC-023`; `BSR 190`; `BSR 203`; `BSR 204`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Дополнительная информация`.

### Итоговый ожидаемый результат

В блоке `Дополнительная информация` отображаются поля `ИНН клиента`, `Семейное положение` и `Количество иждивенцев`.

### Постусловия

Не требуются.

### TC-ACCEI-026

**Название:** Автозаполнение ИНН клиента после заполнения идентифицирующих полей

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `SRC-018`; `SRC-019`; `SRC-020`; `BSR 191`; `BSR 193`; `BSR 194`; `BSR 195`; `BSR 196`; `BSR 197`; `BSR 198`; `BSR 199`; `BSR 200`; `BSR 201`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Интеграция запроса ИНН доступна на стенде.
- Заполнены поля `Фамилия`, `Имя`, `Отчество`, `Дата рождения`, `Серия паспорта`, `Номер паспорта`, `Дата выдачи паспорта`, кроме последнего поля сценария.

### Тестовые данные

- Дата выдачи паспорта: валидная дата из тестового набора.

### Шаги

1. Заполнить последнее незаполненное поле из набора идентифицирующих данных.
2. Снять фокус с этого поля.
3. Проверить поле `ИНН клиента` и чекбокс `ИНН проверен`.

### Итоговый ожидаемый результат

После потери фокуса с последнего заполненного идентифицирующего поля выполняется запрос ИНН клиента и статуса ИНН. Если интеграция вернула ИНН, поле `ИНН клиента` заполнено автоматически и недоступно для ручного изменения; значение `ИНН проверен` соответствует результату запроса.

### Постусловия

Отменить несохраненные изменения или закрыть заявку без сохранения.

### TC-ACCEI-027

**Название:** Кнопка Верифицировать ИНН отображается только для ручного ввода ИНН

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `SRC-018`; `SRC-019`; `BSR 193`; `BSR 194`; `BSR 195`; `BSR 196`; `WP-02`

### Предусловия

- Открыта карточка заявки на разделе `Заявка`.
- Заполнены поля `Фамилия`, `Имя`, `Отчество`, `Дата рождения`, `Серия паспорта`, `Номер паспорта`, `Дата выдачи паспорта`.

### Тестовые данные

Не требуются.

### Шаги

1. Проверить состояние поля `ИНН клиента`, когда ИНН заполнен автоматически.
2. Подготовить состояние, при котором поле `ИНН клиента` не заполнилось автоматически из-за недоступности интеграционного взаимодействия.
3. Проверить состояние поля `ИНН клиента` и кнопки `Верифицировать ИНН`.

### Итоговый ожидаемый результат

Когда `ИНН клиента` заполнен автоматически, ручное изменение ИНН недоступно и кнопка `Верифицировать ИНН` не используется для ручной проверки. Когда ИНН не заполнился автоматически, поле доступно для ручного ввода, а кнопка `Верифицировать ИНН` отображается при заполненных идентифицирующих полях.

### Постусловия

Восстановить доступность интеграции или отменить несохраненные изменения.

## 7. Документы по заявке и файлы анкеты

Источник порядка: Таблица 4, rows 081-094.

### TC-AF-DOC-001
**Название:** Отображение блока документов и информационного поля анкеты клиента
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-001; ATOM-002; ATOM-003; SRC-001; SRC-002; BSR 206; BSR 207

### Предусловия
Открыта карточка заявки в разделе «Заявка».

### Тестовые данные
Не требуются.

### Шаги
1. Перейти к блоку «Документы по заявке».
2. Найти информационное поле «Анкета клиента. Распечатайте, подпишите с клиентом и загрузите скан в заявку».

### Итоговый ожидаемый результат
В блоке «Документы по заявке» видно информационное поле с инструкцией по анкете клиента.

### Постусловия
Не требуются.

### TC-AF-DOC-002
**Название:** Загрузка анкеты клиента через проводник в поддерживаемых форматах
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-004; ATOM-005; ATOM-008; ATOM-011; SRC-003; BSR 208; BSR 209; BSR 210; BSR 211

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Анкета клиента».

### Тестовые данные
Файлы: `client-questionnaire.jpg`, `client-questionnaire.png`, `client-questionnaire.pdf`.

### Шаги
1. В поле «Анкета клиента» добавить файл `client-questionnaire.jpg` через кнопку открытия проводника.
2. Удалить добавленный файл через доступный UI-механизм удаления вложения.
3. В поле «Анкета клиента» добавить файл `client-questionnaire.png` через drag and drop.
4. Удалить добавленный файл через доступный UI-механизм удаления вложения.
5. В поле «Анкета клиента» добавить файл `client-questionnaire.pdf` через кнопку открытия проводника.

### Итоговый ожидаемый результат
После каждой загрузки в поле «Анкета клиента» отображается имя ровно одного добавленного файла соответствующего формата.

### Постусловия
Удалить файл `client-questionnaire.pdf`, если он остался прикрепленным.

### TC-AF-DOC-003
**Название:** Загрузка анкеты клиента через Drag and Drop
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-004; ATOM-006; ATOM-008; ATOM-011; SRC-003; BSR 208; BSR 209; BSR 210; BSR 211

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Анкета клиента».

### Тестовые данные
Файл: `client-questionnaire-dnd.pdf`.

### Шаги
1. Перетащить файл `client-questionnaire-dnd.pdf` в область Drag and Drop поля «Анкета клиента».

### Итоговый ожидаемый результат
В поле «Анкета клиента» отображается имя файла `client-questionnaire-dnd.pdf`.

### Постусловия
Удалить файл `client-questionnaire-dnd.pdf`, если он остался прикрепленным.

### TC-AF-DOC-004
**Название:** Прием анкеты клиента с размером файла не более 40 МБ
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-004; ATOM-009; ATOM-011; SRC-003; BSR 208; BSR 210; BSR 211

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Анкета клиента».

### Тестовые данные
Файл: `client-questionnaire-40mb.pdf`, размер файла не более 40 МБ.

### Шаги
1. В поле «Анкета клиента» добавить файл `client-questionnaire-40mb.pdf` через кнопку открытия проводника.

### Итоговый ожидаемый результат
В поле «Анкета клиента» отображается имя файла `client-questionnaire-40mb.pdf`.

### Постусловия
Удалить файл `client-questionnaire-40mb.pdf`, если он остался прикрепленным.

### TC-AF-DOC-005
**Название:** Ошибка при загрузке анкеты клиента с файлом больше 40 МБ
**Тип:** Validation
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-010; SRC-003; BSR 210

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Анкета клиента».

### Тестовые данные
Файл: `client-questionnaire-41mb.pdf`, размер файла больше 40 МБ.

### Шаги
1. В поле «Анкета клиента» добавить файл `client-questionnaire-41mb.pdf` через кнопку открытия проводника.

### Итоговый ожидаемый результат
Система выводит текст: «Документы не загружены. Проверьте соответствуют ли документы требованиям: формат jpg, png, pdf, размер не более 40 МБ».

### Постусловия
Убедиться, что файл `client-questionnaire-41mb.pdf` не остался прикрепленным.

### TC-AF-DOC-006
**Название:** Загрузка паспорта клиента через проводник в поддерживаемых форматах
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-013; ATOM-014; ATOM-017; ATOM-018; SRC-004; BSR 213; BSR 214; BSR 215; BSR 216

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Паспорт клиента».

### Тестовые данные
Файлы: `passport-client.jpg`, `passport-client.png`, `passport-client.pdf`.

### Шаги
1. В поле «Паспорт клиента» добавить файл `passport-client.jpg` через кнопку открытия проводника.
2. Удалить добавленный файл через доступный UI-механизм удаления вложения.
3. В поле «Паспорт клиента» добавить файл `passport-client.png` через кнопку открытия проводника.
4. Удалить добавленный файл через доступный UI-механизм удаления вложения.
5. В поле «Паспорт клиента» добавить файл `passport-client.pdf` через кнопку открытия проводника.

### Итоговый ожидаемый результат
После каждой загрузки в поле «Паспорт клиента» отображается имя ровно одного добавленного файла соответствующего формата.

### Постусловия
Удалить файл `passport-client.pdf`, если он остался прикрепленным.

### TC-AF-DOC-007
**Название:** Загрузка паспорта клиента через Drag and Drop
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-013; ATOM-015; ATOM-017; ATOM-018; SRC-004; BSR 213; BSR 214; BSR 215; BSR 216

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Паспорт клиента».

### Тестовые данные
Файл: `passport-client-dnd.pdf`.

### Шаги
1. Перетащить файл `passport-client-dnd.pdf` в область Drag and Drop поля «Паспорт клиента».

### Итоговый ожидаемый результат
В поле «Паспорт клиента» отображается имя файла `passport-client-dnd.pdf`.

### Постусловия
Удалить файл `passport-client-dnd.pdf`, если он остался прикрепленным.

### TC-AF-DOC-008
**Название:** Прием паспорта клиента с размером файла не более 40 МБ
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-013; ATOM-017; ATOM-018; SRC-004; BSR 213; BSR 215; BSR 216

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Паспорт клиента».

### Тестовые данные
Файл: `passport-client-40mb.pdf`, размер файла не более 40 МБ.

### Шаги
1. В поле «Паспорт клиента» добавить файл `passport-client-40mb.pdf` через кнопку открытия проводника.

### Итоговый ожидаемый результат
В поле «Паспорт клиента» отображается имя файла `passport-client-40mb.pdf`.

### Постусловия
Удалить файл `passport-client-40mb.pdf`, если он остался прикрепленным.

### TC-AF-DOC-009
**Название:** Ошибка при загрузке паспорта клиента с неподдерживаемым форматом
**Тип:** Validation
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-017; SRC-004; BSR 215

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Паспорт клиента».

### Тестовые данные
Файл: `passport-client.txt`.

### Шаги
1. В поле «Паспорт клиента» добавить файл `passport-client.txt` через кнопку открытия проводника.

### Итоговый ожидаемый результат
Система выводит текст: «Документы не загружены. Проверьте соответствуют ли документы требованиям: формат jpg, png, pdf, размер не более 40 МБ».

### Постусловия
Убедиться, что файл `passport-client.txt` не остался прикрепленным.

### TC-AF-DOC-010
**Название:** Значение по умолчанию и список поля «Тип документа»
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-020; ATOM-021; ATOM-022; SRC-005; BSR 218; BSR 219; DICT-001

### Предусловия
Открыт блок «Документы по заявке».

### Тестовые данные
Ожидаемые значения `DICT-001`: `ВУ`, `СНИЛС`, `Загран. паспорт`.

### Шаги
1. Проверить поле «Тип документа» до выбора значения.
2. Открыть раскрывающийся список «Тип документа».

### Итоговый ожидаемый результат
До выбора поле «Тип документа» пустое, а раскрывающийся список содержит значения `ВУ`, `СНИЛС`, `Загран. паспорт`.

### Постусловия
Закрыть раскрывающийся список без сохранения изменений.

### TC-AF-DOC-011
**Название:** Недоступность загрузки второго документа без выбранного типа документа
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-023; ATOM-024; SRC-006; BSR 220; BSR 221

### Предусловия
Открыт блок «Документы по заявке», поле «Тип документа» пустое.

### Тестовые данные
Не требуются.

### Шаги
1. Найти поле «Второй документ».
2. Проверить доступность действия загрузки файла в поле «Второй документ».

### Итоговый ожидаемый результат
Поле «Второй документ» видно, но загрузка файла недоступна, пока в поле «Тип документа» не выбран документ.

### Постусловия
Не требуются.

### TC-AF-DOC-012
**Название:** Загрузка второго документа после выбора типа документа через проводник
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-024; ATOM-025; ATOM-026; ATOM-027; SRC-006; BSR 221; BSR 222; BSR 223; BSR 224; DICT-001

### Предусловия
Открыт блок «Документы по заявке», поле «Тип документа» доступно.

### Тестовые данные
Тип документа: `ВУ`; файл: `driver-license.pdf`.

### Шаги
1. В поле «Тип документа» выбрать значение `ВУ`.
2. В поле «Второй документ» добавить файл `driver-license.pdf` через кнопку открытия проводника.

### Итоговый ожидаемый результат
После выбора типа документа загрузка второго документа доступна, а в поле «Второй документ» отображается имя файла `driver-license.pdf`.

### Постусловия
Удалить файл `driver-license.pdf` и очистить значение «Тип документа», если тестовая среда сохраняет изменения.

### TC-AF-DOC-013
**Название:** Загрузка второго документа через Drag and Drop
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-024; ATOM-025; ATOM-026; ATOM-027; SRC-006; BSR 221; BSR 222; BSR 223; BSR 224; DICT-001

### Предусловия
Открыт блок «Документы по заявке», в поле «Тип документа» выбрано значение `ВУ`, поле «Второй документ» не содержит вложений.

### Тестовые данные
Файл: `driver-license-dnd.pdf`.

### Шаги
1. Перетащить файл `driver-license-dnd.pdf` в область Drag and Drop поля «Второй документ».

### Итоговый ожидаемый результат
В поле «Второй документ» отображается имя файла `driver-license-dnd.pdf`.

### Постусловия
Удалить файл `driver-license-dnd.pdf`, если он остался прикрепленным.

### TC-AF-DOC-014
**Название:** Ошибка при загрузке второго документа с файлом больше 40 МБ
**Тип:** Validation
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-026; SRC-006; BSR 223

### Предусловия
Открыт блок «Документы по заявке», в поле «Тип документа» выбрано значение `ВУ`, поле «Второй документ» не содержит вложений.

### Тестовые данные
Файл: `driver-license-41mb.pdf`, размер файла больше 40 МБ.

### Шаги
1. В поле «Второй документ» добавить файл `driver-license-41mb.pdf` через кнопку открытия проводника.

### Итоговый ожидаемый результат
Система выводит текст: «Документы не загружены. Проверьте соответствуют ли документы требованиям: формат jpg, png, pdf, размер не более 40 МБ».

### Постусловия
Убедиться, что файл `driver-license-41mb.pdf` не остался прикрепленным.

### TC-AF-DOC-015
**Название:** Видимость поля «Серия» для выбранного типа документа
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-031; SRC-007; BSR 227; DICT-001

### Предусловия
Открыт блок «Документы по заявке», поле «Тип документа» доступно.

### Тестовые данные
Значения типа документа: `ВУ`, `Загран. паспорт`.

### Шаги
1. В поле «Тип документа» выбрать значение `ВУ`.
2. Проверить наличие поля «Серия».
3. В поле «Тип документа» выбрать значение `Загран. паспорт`.
4. Проверить наличие поля «Серия».

### Итоговый ожидаемый результат
Для значений `ВУ` и `Загран. паспорт` поле «Серия» видно.

### Постусловия
Очистить значение «Тип документа», если тестовая среда сохраняет изменения.

### TC-AF-DOC-016
**Название:** Отсутствие поля «Серия» для типа документа СНИЛС
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-031; SRC-007; BSR 227; DICT-001

### Предусловия
Открыт блок «Документы по заявке», поле «Тип документа» доступно.

### Тестовые данные
Значение типа документа: `СНИЛС`.

### Шаги
1. В поле «Тип документа» выбрать значение `СНИЛС`.
2. Проверить отсутствие поля «Серия».

### Итоговый ожидаемый результат
Для значения `СНИЛС` поле «Серия» не видно в блоке «Документы по заявке».

### Постусловия
Очистить значение «Тип документа», если тестовая среда сохраняет изменения.

### TC-AF-DOC-017
**Название:** Ввод реквизитов второго документа в видимые поля
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-032; ATOM-033; ATOM-035; SRC-008; SRC-009; SRC-010; BSR 228; BSR 229; BSR 231

### Предусловия
Открыт блок «Документы по заявке».

### Тестовые данные
Номер: `345678`; дата выдачи: текущая календарная дата; кем выдан: `ОВД Тестового района`.

### Шаги
1. В поле «Номер» ввести `345678`.
2. В поле «Дата выдачи» ввести текущую календарную дату.
3. В поле «Кем выдан» ввести `ОВД Тестового района`.

### Итоговый ожидаемый результат
Поля «Номер», «Дата выдачи» и «Кем выдан» отображают введенные значения.

### Постусловия
Очистить введенные значения, если тестовая среда сохраняет изменения.

### TC-AF-DOC-018
**Название:** Запрет будущей даты выдачи второго документа
**Тип:** Validation
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-034; SRC-009; BSR 230; coverage_gap:wr-006

### Предусловия
Открыт блок «Документы по заявке», поле «Дата выдачи» доступно для ввода.

### Тестовые данные
Дата выдачи: календарная дата позже текущей даты.

### Шаги
1. В поле «Дата выдачи» ввести дату позже текущей календарной даты.
2. Попытаться зафиксировать введенное значение в поле.

### Итоговый ожидаемый результат
Система не фиксирует дату выдачи больше текущей даты.

### Постусловия
Очистить поле «Дата выдачи», если значение осталось в форме.

### TC-AF-DOC-019
**Название:** Просмотр прикрепленного документа через пиктограмму «глаз»
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-036; ATOM-037; SRC-011; BSR 232; BSR 233

### Предусловия
В любом поле загрузки документа в блоке «Документы по заявке» прикреплен файл `passport-client.pdf`.

### Тестовые данные
Файл: `passport-client.pdf`.

### Шаги
1. Найти пиктограмму «глаз» рядом с прикрепленным документом.
2. Нажать пиктограмму «глаз».

### Итоговый ожидаемый результат
Открывается всплывающее окно просмотра документа `passport-client.pdf`.

### Постусловия
Закрыть всплывающее окно просмотра документа.

### TC-AF-DOC-020
**Название:** Скрытие прикрепленного документа через пиктограмму «корзина»
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-038; ATOM-039; SRC-012; BSR 234; BSR 235; coverage_gap:wr-007

### Предусловия
В любом поле загрузки документа в блоке «Документы по заявке» прикреплен файл `passport-client.pdf`.

### Тестовые данные
Файл: `passport-client.pdf`.

### Шаги
1. Найти пиктограмму «корзина» рядом с прикрепленным документом.
2. Нажать пиктограмму «корзина».

### Итоговый ожидаемый результат
Документ `passport-client.pdf` перестает быть видимым в интерфейсе блока «Документы по заявке».

### Постусловия
Если тестовая среда сохраняет изменения, восстановить исходное вложение через повторную загрузку.

### TC-AF-DOC-021
**Название:** Открытие QR-кода для прикрепления документа с телефона
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-007; ATOM-016; ATOM-040; ATOM-041; SRC-003; SRC-004; SRC-013; BSR 209; BSR 214; BSR 236; BSR 237; GAP-002

### Предусловия
Открыт блок «Документы по заявке».

### Тестовые данные
Не требуются.

### Шаги
1. Нажать кнопку «Прикрепить с телефона».

### Итоговый ожидаемый результат
Открывается всплывающее окно со сгенерированным QR-кодом для прикрепления файлов с мобильного телефона.

### Постусловия
Закрыть всплывающее окно с QR-кодом.

### TC-AF-DOC-022
**Название:** Скачивание заявления-анкеты в формате PDF
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-042; ATOM-043; SRC-014; BSR 238; BSR 239; BSR 240; GAP-003

### Предусловия
Открыта карточка заявки, для которой доступно сформированное заявление-анкета.

### Тестовые данные
Не требуются.

### Шаги
1. Нажать кнопку «Скачать (документ)».
2. Дождаться завершения загрузки файла на локальное рабочее место пользователя.

### Итоговый ожидаемый результат
На локальное рабочее место пользователя выгружен файл заявления-анкеты в формате PDF.

### Постусловия
Удалить скачанный тестовый файл с локального рабочего места, если это требуется правилами тестового контура.

### TC-AF-DOC-023
**Название:** Отображение нескольких файлов анкеты клиента через запятую
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-045; SRC-003; BSR 211

### Предусловия
Открыт блок «Документы по заявке». В поле «Анкета клиента» прикреплены файлы `client-questionnaire-page-1.pdf` и `client-questionnaire-page-2.pdf`.

### Тестовые данные
Файлы: `client-questionnaire-page-1.pdf`, `client-questionnaire-page-2.pdf`.

### Шаги
1. Найти поле «Анкета клиента» с прикрепленными файлами.

### Итоговый ожидаемый результат
В поле «Анкета клиента» отображаются названия файлов `client-questionnaire-page-1.pdf, client-questionnaire-page-2.pdf`, разделенные запятой.

### Постусловия
Удалить файлы `client-questionnaire-page-1.pdf` и `client-questionnaire-page-2.pdf`, если они остались прикрепленными.

### TC-AF-DOC-024
**Название:** Отображение нескольких файлов паспорта клиента через запятую
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-046; SRC-004; BSR 216

### Предусловия
Открыт блок «Документы по заявке». В поле «Паспорт клиента» прикреплены файлы `passport-client-page-1.pdf` и `passport-client-page-2.pdf`.

### Тестовые данные
Файлы: `passport-client-page-1.pdf`, `passport-client-page-2.pdf`.

### Шаги
1. Найти поле «Паспорт клиента» с прикрепленными файлами.

### Итоговый ожидаемый результат
В поле «Паспорт клиента» отображаются названия файлов `passport-client-page-1.pdf, passport-client-page-2.pdf`, разделенные запятой.

### Постусловия
Удалить файлы `passport-client-page-1.pdf` и `passport-client-page-2.pdf`, если они остались прикрепленными.

### TC-AF-DOC-025
**Название:** Отображение нескольких файлов второго документа через запятую
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-047; SRC-006; BSR 224

### Предусловия
Открыт блок «Документы по заявке». В поле «Тип документа» выбрано значение `ВУ`. В поле «Второй документ» прикреплены файлы `driver-license-page-1.pdf` и `driver-license-page-2.pdf`.

### Тестовые данные
Тип документа: `ВУ`; файлы: `driver-license-page-1.pdf`, `driver-license-page-2.pdf`.

### Шаги
1. Найти поле «Второй документ» с прикрепленными файлами.

### Итоговый ожидаемый результат
В поле «Второй документ» отображаются названия файлов `driver-license-page-1.pdf, driver-license-page-2.pdf`, разделенные запятой.

### Постусловия
Удалить файлы `driver-license-page-1.pdf` и `driver-license-page-2.pdf` и очистить значение «Тип документа», если тестовая среда сохраняет изменения.

### TC-AF-DOC-026
**Название:** Ошибка при загрузке анкеты клиента с неподдерживаемым форматом
**Тип:** Validation
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-010; SRC-003; BSR 210

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Анкета клиента».

### Тестовые данные
Файл: `client-questionnaire.txt`.

### Шаги
1. В поле «Анкета клиента» добавить файл `client-questionnaire.txt` через кнопку открытия проводника.

### Итоговый ожидаемый результат
Система выводит текст: «Документы не загружены. Проверьте соответствуют ли документы требованиям: формат jpg, png, pdf, размер не более 40 МБ».

### Постусловия
Убедиться, что файл `client-questionnaire.txt` не остался прикрепленным.

### TC-AF-DOC-027
**Название:** Ошибка при загрузке второго документа с неподдерживаемым форматом
**Тип:** Validation
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-026; SRC-006; BSR 223

### Предусловия
Открыт блок «Документы по заявке», в поле «Тип документа» выбрано значение `ВУ`, поле «Второй документ» не содержит вложений.

### Тестовые данные
Файл: `driver-license.txt`.

### Шаги
1. В поле «Второй документ» добавить файл `driver-license.txt` через кнопку открытия проводника.

### Итоговый ожидаемый результат
Система выводит текст: «Документы не загружены. Проверьте соответствуют ли документы требованиям: формат jpg, png, pdf, размер не более 40 МБ».

### Постусловия
Убедиться, что файл `driver-license.txt` не остался прикрепленным.

### TC-AF-DOC-028
**Название:** Обязательность второго документа зависит от типа программы
**Тип:** Business
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-028; SRC-006; BSR 225

### Предусловия
Открыт блок «Документы по заявке»; поле «Тип документа» доступно; известен тип программы заявки.

### Тестовые данные
Проверяемые варианты:
- программа не ГОС;
- программа ГОС;
- программа для инвалидов.

### Шаги
1. Для заявки с программой не ГОС оставить поле «Второй документ» без вложения и проверить обязательность.
2. Для заявки с программой ГОС выбрать `ВУ` как тип второго документа и проверить обязательность загрузки.
3. Для заявки с программой для инвалидов проверить допустимость типов второго документа `ВУ` и `СНИЛС`.

### Итоговый ожидаемый результат
Для не ГОС программы второй документ не обязателен. Для ГОС программы обязательным вторым документом является `ВУ`. Для программы для инвалидов обязательным вторым документом является `ВУ` или `СНИЛС`.

### Постусловия
Очистить выбранные значения и вложения второго документа при необходимости.

### TC-AF-DOC-029
**Название:** Анкета клиента сохраняется в электронном архиве после добавления файла
**Тип:** Integration
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-012; SRC-003; BSR 212

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Анкета клиента»; доступен наблюдаемый источник проверки электронного архива Банка: archive UI, API, БД или лог.

### Тестовые данные
Файл `client-questionnaire-archive.pdf`, формат pdf, размер не более 40 МБ.

### Шаги
1. В поле «Анкета клиента» добавить файл `client-questionnaire-archive.pdf`.
2. Проверить запись о сохранении документа в электронном архиве Банка через доступный evidence source.

### Итоговый ожидаемый результат
После добавления файла документ «Анкета клиента» сохранен в электронном архиве Банка и связан с текущей заявкой.

### Постусловия
Удалить загруженный файл или очистить тестовую заявку по правилам стенда.

### TC-AF-DOC-030
**Название:** Паспорт клиента сохраняется в электронном архиве после добавления файла
**Тип:** Integration
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-019; SRC-004; BSR 217

### Предусловия
Открыт блок «Документы по заявке» без ранее прикрепленного файла в поле «Паспорт клиента»; доступен наблюдаемый источник проверки электронного архива Банка: archive UI, API, БД или лог.

### Тестовые данные
Файл `passport-client-archive.pdf`, формат pdf, размер не более 40 МБ.

### Шаги
1. В поле «Паспорт клиента» добавить файл `passport-client-archive.pdf`.
2. Проверить запись о сохранении документа в электронном архиве Банка через доступный evidence source.

### Итоговый ожидаемый результат
После добавления файла документ «Паспорт клиента» сохранен в электронном архиве Банка и связан с текущей заявкой.

### Постусловия
Удалить загруженный файл или очистить тестовую заявку по правилам стенда.

### TC-AF-DOC-031
**Название:** Второй документ сохраняется в электронном архиве после добавления файла
**Тип:** Integration
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-029; SRC-006; BSR 226

### Предусловия
Открыт блок «Документы по заявке»; в поле «Тип документа» выбрано значение `ВУ`; поле «Второй документ» не содержит вложений; доступен наблюдаемый источник проверки электронного архива Банка: archive UI, API, БД или лог.

### Тестовые данные
Файл `driver-license-archive.pdf`, формат pdf, размер не более 40 МБ.

### Шаги
1. В поле «Второй документ» добавить файл `driver-license-archive.pdf`.
2. Проверить запись о сохранении документа в электронном архиве Банка через доступный evidence source.

### Итоговый ожидаемый результат
После добавления файла второй документ сохранен в электронном архиве Банка и связан с текущей заявкой.

### Постусловия
Удалить загруженный файл или очистить тестовую заявку по правилам стенда.

## 8. Созаемщик и залогодатель

Источник порядка: Таблица 4, rows 095-103.

### TC-ACP-001

**Название:** Отображение блока участников в карточке заявки

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-001; SRC-001; no_requirement_code:SRC-001; WP-01

### Предусловия

- Открыта карточка заявки.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к области участников заявки.

### Итоговый ожидаемый результат

Блок `Участники` отображается в карточке заявки.

### Постусловия

Не требуются.

### TC-ACP-002

**Название:** Видимость кнопки добавления созаемщика

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-002; SRC-002; BSR 241; WP-02

### Предусловия

- Открыта карточка заявки.
- Отображается блок `Участники`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Участники`.

### Итоговый ожидаемый результат

Кнопка `+ Добавить созаемщика` отображается.

### Постусловия

Не требуются.

### TC-ACP-003

**Название:** Открытие окна заявки для добавления созаемщика

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-003; SRC-002; BSR 242; WP-02

### Предусловия

- Открыта карточка заявки.
- Отображается блок `Участники`.

### Тестовые данные

Не требуются.

### Шаги

1. Нажать кнопку `+ Добавить созаемщика`.

### Итоговый ожидаемый результат

Открыто окно `Заявка` для добавления созаемщика.

### Постусловия

Закрыть окно без сохранения, если оно осталось открытым.

### TC-ACP-004

**Название:** Исключение калькуляторного виджета из окна созаемщика

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-004; SRC-002; BSR 242; GAP-001; WP-02

### Предусловия

- Открыта карточка заявки.
- Открыто окно `Заявка` после нажатия `+ Добавить созаемщика`.

### Тестовые данные

Не требуются.

### Шаги

1. Осмотреть верхнюю часть окна созаемщика и область entrypoints калькулятора.

### Итоговый ожидаемый результат

В окне созаемщика отсутствует виджет `Краткая информация с калькулятора`.

### Постусловия

Закрыть окно без сохранения, если оно осталось открытым.

### TC-ACP-005

**Название:** Замена наименования клиента на созаемщика в окне созаемщика

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** ATOM-005; SRC-002; BSR 243; WP-02

### Предусловия

- Открыта карточка заявки.
- Открыто окно `Заявка` после нажатия `+ Добавить созаемщика`.

### Тестовые данные

Не требуются.

### Шаги

1. Просмотреть видимые наименования в окне созаемщика, где в форме заемщика используется роль `клиент`.

### Итоговый ожидаемый результат

В видимых наименованиях окна используется роль `созаемщик` вместо роли `клиент`.

### Постусловия

Закрыть окно без сохранения, если оно осталось открытым.

### TC-ACP-006

**Название:** Видимость кнопки добавления залогодателя

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-006; SRC-003; BSR 244; WP-03

### Предусловия

- Открыта карточка заявки.
- Отображается блок `Участники`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Участники`.

### Итоговый ожидаемый результат

Кнопка `+ Добавить залогодателя` отображается.

### Постусловия

Не требуются.

### TC-ACP-007

**Название:** Открытие окна заявки для добавления залогодателя

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-007; SRC-003; BSR 245; WP-03

### Предусловия

- Открыта карточка заявки.
- Отображается блок `Участники`.

### Тестовые данные

Не требуются.

### Шаги

1. Нажать кнопку `+ Добавить залогодателя`.

### Итоговый ожидаемый результат

Открыто окно `Заявка` для добавления залогодателя.

### Постусловия

Закрыть окно без сохранения, если оно осталось открытым.

### TC-ACP-008

**Название:** Исключение FT-backed блоков и полей из окна залогодателя

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-008; SRC-003; BSR 245; GAP-001; WP-03

### Предусловия

- Открыта карточка заявки.
- Открыто окно `Заявка` после нажатия `+ Добавить залогодателя`.

### Тестовые данные

Исключения по ФТ: `Краткая информация с калькулятора`; `Сведения о занятости`; `контактные лица`; `кодовое слово`; `СНИЛС (при прикреплении документа)`; `верификация ИНН ФЛ`; `семейное положение`; `количество иждивенцев`; `социальный статус`; `подтверждение дохода`.

### Шаги

1. Просмотреть окно залогодателя.
2. Сверить наличие каждого элемента из списка исключений в тестовых данных.

### Итоговый ожидаемый результат

В окне залогодателя не отображаются элементы из списка исключений в тестовых данных.

### Постусловия

Закрыть окно без сохранения, если оно осталось открытым.

### TC-ACP-009

**Название:** Замена наименования клиента на залогодателя в окне залогодателя

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** ATOM-009; SRC-003; BSR 246; WP-03

### Предусловия

- Открыта карточка заявки.
- Открыто окно `Заявка` после нажатия `+ Добавить залогодателя`.

### Тестовые данные

Не требуются.

### Шаги

1. Просмотреть видимые наименования в окне залогодателя, где в форме заемщика используется роль `клиент`.

### Итоговый ожидаемый результат

В видимых наименованиях окна используется роль `залогодатель` вместо роли `клиент`.

### Постусловия

Закрыть окно без сохранения, если оно осталось открытым.

### TC-ACP-010

**Название:** Отсутствие отдельно добавленной строки залогодателя до действия добавления

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** ATOM-010; SRC-003; BSR 247; GAP-003; WP-03

### Предусловия

- Открыта карточка заявки.
- В текущей заявке пользователь не нажимал `+ Добавить залогодателя`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к таблице участников.

### Итоговый ожидаемый результат

В таблице участников не отображается отдельная строка с типом участия `залогодатель`, созданная через действие `+ Добавить залогодателя`.

### Постусловия

Не требуются.

### TC-ACP-011

**Название:** Видимость виджета редактирования участника

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** ATOM-011; SRC-004; BSR 248; WP-04

### Предусловия

- Открыта карточка заявки.
- Отображается блок `Участники`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к таблице участников.

### Итоговый ожидаемый результат

Виджет `Редактировать` отображается.

### Постусловия

Не требуются.

### TC-ACP-012

**Название:** Доступность редактирования при выбранном участнике

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-012; SRC-004; BSR 249; WP-04

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть строка созаемщика `Иванов Иван Иванович`.

### Тестовые данные

Участник: `Иванов Иван Иванович`.

### Шаги

1. Выбрать строку участника из тестовых данных в таблице участников.

### Итоговый ожидаемый результат

Виджет `Редактировать` доступен к нажатию.

### Постусловия

Снять выбор строки, если он остался.

### TC-ACP-013

**Название:** Недоступность редактирования без выбранного участника

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-013; SRC-004; BSR 249; WP-04

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть хотя бы одна строка.
- Ни одна строка участника не выбрана.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к таблице участников без выбора строки.

### Итоговый ожидаемый результат

Виджет `Редактировать` находится в состоянии disabled.

### Постусловия

Не требуются.

### TC-ACP-014

**Название:** Открытие окна редактирования с данными выбранного участника

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-014; SRC-004; BSR 250; WP-04

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть строка созаемщика с ФИО `Иванов Иван Иванович` и паспортом `4512 123456`.

### Тестовые данные

Участник: тип `созаемщик`; ФИО `Иванов Иван Иванович`; паспорт `4512 123456`.

### Шаги

1. Выбрать строку участника из тестовых данных.
2. Нажать виджет `Редактировать`.

### Итоговый ожидаемый результат

Открыто окно `Заявка` с данными выбранного участника из тестовых данных.

### Постусловия

Закрыть окно без сохранения, если оно осталось открытым.

### TC-ACP-015

**Название:** Видимость виджета корзины участника

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** ATOM-015; SRC-005; BSR 251; WP-04

### Предусловия

- Открыта карточка заявки.
- Отображается блок `Участники`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к таблице участников.

### Итоговый ожидаемый результат

Виджет `Корзина` отображается.

### Постусловия

Не требуются.

### TC-ACP-016

**Название:** Доступность корзины при выбранном участнике

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-016; SRC-005; BSR 252; WP-04

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть строка созаемщика `Иванов Иван Иванович`.

### Тестовые данные

Участник: `Иванов Иван Иванович`.

### Шаги

1. Выбрать строку участника из тестовых данных в таблице участников.

### Итоговый ожидаемый результат

Виджет `Корзина` доступен к нажатию.

### Постусловия

Снять выбор строки, если он остался.

### TC-ACP-017

**Название:** Недоступность корзины без выбранного участника

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-017; SRC-005; BSR 252; WP-04

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть хотя бы одна строка.
- Ни одна строка участника не выбрана.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к таблице участников без выбора строки.

### Итоговый ожидаемый результат

Виджет `Корзина` находится в состоянии disabled.

### Постусловия

Не требуются.

### TC-ACP-018

**Название:** Открытие подтверждения удаления участника

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-018; SRC-005; BSR 254; GAP-002; WP-04

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть строка созаемщика `Иванов Иван Иванович`.

### Тестовые данные

Участник: `Иванов Иван Иванович`.

### Шаги

1. Выбрать строку участника из тестовых данных.
2. Нажать виджет `Корзина`.

### Итоговый ожидаемый результат

Отображается popup подтверждения удаления участника с кнопками `Да` и `Отмена`.

### Постусловия

Выбрать `Отмена`, если popup остался открытым.

### TC-ACP-019

**Название:** Удаление участника при подтверждении Да

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-019; SRC-005; BSR 255; GAP-002; WP-04

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть строка созаемщика `Иванов Иван Иванович`.
- Открыт popup подтверждения удаления для этой строки.

### Тестовые данные

Участник: `Иванов Иван Иванович`.

### Шаги

1. В popup подтверждения выбрать `Да`.

### Итоговый ожидаемый результат

Строка участника `Иванов Иван Иванович` удалена из таблицы участников.

### Постусловия

Восстановить удаленного участника через `+ Добавить созаемщика`, если состояние заявки сохраняется в тестовом контуре.

### TC-ACP-020

**Название:** Отмена удаления участника

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** ATOM-020; SRC-005; BSR 255; GAP-002; WP-04

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть строка созаемщика `Иванов Иван Иванович`.
- Открыт popup подтверждения удаления для этой строки.

### Тестовые данные

Участник: `Иванов Иван Иванович`.

### Шаги

1. В popup подтверждения выбрать `Отмена`.

### Итоговый ожидаемый результат

Popup закрыт, строка участника `Иванов Иван Иванович` остается в таблице участников.

### Постусловия

Не требуются.

### TC-ACP-021

**Название:** Отображение типа участия в таблице участников

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** ATOM-021; SRC-006; BSR 257; WP-05

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть созданные участники из тестовых данных.

### Тестовые данные

Участник 1: создан как `созаемщик`. Участник 2: создан как `залогодатель`.

### Шаги

1. Просмотреть колонку `Тип участия` для участников из тестовых данных.

### Итоговый ожидаемый результат

Для каждого участника отображается тип участия, выбранный при его создании: `созаемщик` или `залогодатель`.

### Постусловия

Не требуются.

### TC-ACP-022

**Название:** Отображение ФИО участника в таблице участников

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** ATOM-022; SRC-007; BSR 259; WP-05

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть созданный созаемщик `Иванов Иван Иванович`.

### Тестовые данные

ФИО участника: `Иванов Иван Иванович`.

### Шаги

1. Найти строку участника из тестовых данных в таблице участников.

### Итоговый ожидаемый результат

В строке участника отображается ФИО `Иванов Иван Иванович`.

### Постусловия

Не требуются.

### TC-ACP-023

**Название:** Отображение паспорта участника в таблице участников

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** ATOM-023; SRC-008; BSR 261; WP-05

### Предусловия

- Открыта карточка заявки.
- В таблице участников есть созданный созаемщик с паспортом `4512 123456`.

### Тестовые данные

Паспорт участника: `4512 123456`.

### Шаги

1. Найти строку участника из тестовых данных в таблице участников.

### Итоговый ожидаемый результат

В строке участника отображается паспорт `4512 123456`.

### Постусловия

Не требуются.

### TC-ACP-024

**Название:** Автозаполнение ID клиента участника после сохранения заявки

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** ATOM-024; SRC-009; BSR 263; WP-05

### Предусловия

- Открыта сохраненная карточка заявки.
- Для участника `Иванов Иван Иванович` система АБС вернула ID клиента `ABS-COB-001` после сохранения заявки.

### Тестовые данные

Участник: `Иванов Иван Иванович`. ID клиента из АБС: `ABS-COB-001`.

### Шаги

1. Найти строку участника из тестовых данных в таблице участников.

### Итоговый ожидаемый результат

В поле `ID клиента` для участника отображается значение `ABS-COB-001`.

### Постусловия

Не требуются.

### TC-ACP-025

**Название:** Видимость поля `Тип участия` в таблице участников

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-05

**Трассировка:** ATOM-025; SRC-006; BSR 256; WP-05

### Предусловия

- Открыта карточка заявки.
- Доступен блок `Участники`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Участники`.
2. Проверить состав отображаемых полей/колонок таблицы участников.

### Ожидаемый результат

Поле/колонка `Тип участия` отображается в таблице участников.

### Постусловия

Не требуются.

### TC-ACP-026

**Название:** Видимость поля `ФИО` в таблице участников

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-05

**Трассировка:** ATOM-026; SRC-007; BSR 258; WP-05

### Предусловия

- Открыта карточка заявки.
- Доступен блок `Участники`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Участники`.
2. Проверить состав отображаемых полей/колонок таблицы участников.

### Ожидаемый результат

Поле/колонка `ФИО` отображается в таблице участников.

### Постусловия

Не требуются.

### TC-ACP-027

**Название:** Видимость поля `Паспорт` в таблице участников

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-05

**Трассировка:** ATOM-027; SRC-008; BSR 260; WP-05

### Предусловия

- Открыта карточка заявки.
- Доступен блок `Участники`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Участники`.
2. Проверить состав отображаемых полей/колонок таблицы участников.

### Ожидаемый результат

Поле/колонка `Паспорт` отображается в таблице участников.

### Постусловия

Не требуются.

### TC-ACP-028

**Название:** Видимость поля `ID клиента` в таблице участников

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-05

**Трассировка:** ATOM-028; SRC-009; BSR 262; WP-05

### Предусловия

- Открыта карточка заявки.
- Доступен блок `Участники`.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к блоку `Участники`.
2. Проверить состав отображаемых полей/колонок таблицы участников.

### Ожидаемый результат

Поле/колонка `ID клиента` отображается в таблице участников.

### Постусловия

Не требуются.

## 9. Сведения о занятости, доходы и подтверждение дохода

Источник порядка: Таблица 4, rows 104-129.

### TC-AFEIG-001

**Название:** Отображение блока занятости и подблока основной работы
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-001; ATOM-002; SRC-001; SRC-002

### Предусловия

Открыта карточка заявки с анкетой клиента.

### Тестовые данные

Не требуются.

### Шаги

1. Открыть раздел анкеты клиента, содержащий сведения о занятости.

### Итоговый ожидаемый результат

В карточке отображается блок «Сведения о занятости», внутри него отображается подблок «Основная работа».

### Постусловия

Не требуются.

### TC-AFEIG-002

**Название:** Состав значений раскрывающегося списка социального статуса
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-003; ATOM-004; BSR 264; SRC-003; DICT-001; GAP-001

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Значения DICT-001: собственник бизнеса; ИП; работа по найму; пенсионер (не работает); самозанятый; военнослужащий.

### Шаги

1. Открыть раскрывающийся список социального статуса / типа занятости в подблоке «Основная работа».

### Итоговый ожидаемый результат

Список отображается всегда и содержит все значения DICT-001 без дополнительных активных значений.

### Постусловия

Не требуются.

### TC-AFEIG-003

**Название:** Поля работодателя отображаются для работы по найму
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-005; ATOM-009; ATOM-012; ATOM-014; ATOM-017; SRC-004; SRC-005; SRC-006; SRC-007; SRC-008; DICT-001

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Социальный статус / тип занятости: работа по найму.

### Шаги

1. В раскрывающемся списке социального статуса / типа занятости выбрать «работа по найму».

### Итоговый ожидаемый результат

Отображаются поля «Наименование организации, ИНН», «ОПФ», «Фактический адрес работы», «Рабочий телефон» и «Тип должности».

### Постусловия

Не требуются.

### TC-AFEIG-004

**Название:** Поля работодателя скрыты для пенсионера и самозанятого
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-008; ATOM-009; ATOM-012; ATOM-017; SRC-004; SRC-005; SRC-006; SRC-008; DICT-001

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Проверяемые значения: пенсионер (не работает); самозанятый.

### Шаги

1. Выбрать значение «пенсионер (не работает)».
2. Проверить поля работодателя.
3. Выбрать значение «самозанятый».
4. Повторно проверить поля работодателя.

### Итоговый ожидаемый результат

Для каждого проверяемого значения поля «Наименование организации, ИНН», «ОПФ», «Фактический адрес работы» и «Тип должности» не отображаются.

### Постусловия

Не требуются.

### TC-AFEIG-005

**Название:** Рабочий телефон отображается для военнослужащего
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** ATOM-014; BSR 272; SRC-007; DICT-001

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Социальный статус / тип занятости: военнослужащий.

### Шаги

1. В раскрывающемся списке социального статуса / типа занятости выбрать «военнослужащий».

### Итоговый ожидаемый результат

Поле «Рабочий телефон» отображается.

### Постусловия

Не требуются.

### TC-AFEIG-006

**Название:** Недействующая организация DaData блокирует переход далее
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-007; BSR 267; SRC-004

### Предусловия

Открыт блок «Сведения о занятости»; выбран статус «работа по найму»; интеграция DaData доступна на стенде.

### Тестовые данные

Организация из тестового контура DaData со статусом, отличным от «действующая».

### Шаги

1. В поле «Наименование организации, ИНН» выбрать организацию со статусом не «действующая».
2. Нажать кнопку «Далее».

### Итоговый ожидаемый результат

Под полем отображается красная подсказка «Организация не является действующей», переход по кнопке «Далее» не выполняется.

### Постусловия

Выбрать действующую организацию, если заявка используется далее.

### TC-AFEIG-007

**Название:** Выбор действующей организации DaData заполняет организационные поля
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-006; ATOM-011; ATOM-013; BSR 266; BSR 269; BSR 271; BSR 326; SRC-004; SRC-005; SRC-006; DICT-002

### Предусловия

Открыт блок «Сведения о занятости»; выбран статус «работа по найму».

### Тестовые данные

Действующая организация тестового контура DaData с известными значениями статуса организации, наименования, ОПФ, ИНН и фактического адреса.

### Шаги

1. В поле «Наименование организации, ИНН» начать ввод названия действующей организации.
2. Выбрать подсказку DaData этой организации.

### Итоговый ожидаемый результат

После выбора организации из подсказки DaData заполнены данные выбранной организации: статус организации, наименование, ОПФ, ИНН и фактический адрес работы.

### Постусловия

Очистить поля основной работы при необходимости.

### TC-AFEIG-008

**Название:** Состав значений ОПФ
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** ATOM-010; BSR 268; SRC-005; DICT-002

### Предусловия

Открыт блок «Сведения о занятости»; поле «ОПФ» отображается.

### Тестовые данные

Значения DICT-002: ООО; ПАО; АО (НАО); полное товарищество; товарищество на вере; хозяйственное партнерство; производственный кооператив; КФХ; ФГУП; ГУП; МУП; потребительский кооператив; общественная организация; общественное движение; ассоциация; союз; ТСН (ТСЖ); казачье общество; община коренных народов; нотариальная палата; адвокатская палата; фонд; учреждение; АНО; религиозная организация; госкорпорация; госкомпания; публично-правовая компания; ИП; глава КФХ; самозанятый; частнопрактикующее лицо; простое товарищество; инвестиционное товарищество; ПИФ; филиал иностранного юрлица.

### Шаги

1. Открыть раскрывающийся список «ОПФ».

### Итоговый ожидаемый результат

Список содержит все значения DICT-002 без дополнительных активных значений.

### Постусловия

Не требуются.

### TC-AFEIG-009

**Название:** Состав значений типа должности
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** ATOM-018; BSR 275; SRC-008; DICT-003

### Предусловия

Открыт блок «Сведения о занятости»; выбран статус «работа по найму».

### Тестовые данные

Значения DICT-003: Сотрудник/Рабочий/Ассистент; Индивидуальный предприниматель; Главный специалист/Руководитель среднего звена; Топ менеджер/Руководитель высшего звена; Эксперт/Старший или Ведущий специалист.

### Шаги

1. Открыть раскрывающийся список «Тип должности».

### Итоговый ожидаемый результат

Список содержит все значения DICT-003 без дополнительных активных значений.

### Постусловия

Не требуются.

### TC-AFEIG-010

**Название:** Поле должности отображается и принимает текст
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** ATOM-019; BSR 276; SRC-009

### Предусловия

Открыт блок «Сведения о занятости»; выбран статус «работа по найму».

### Тестовые данные

Должность: Менеджер.

### Шаги

1. В поле «Должность» ввести «Менеджер».

### Итоговый ожидаемый результат

Поле «Должность» отображается и содержит значение «Менеджер».

### Постусловия

Очистить поле при необходимости.

### TC-AFEIG-011

**Название:** Дата начала работы в компании отображается для работы по найму
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** ATOM-020; BSR 277; SRC-010

### Предусловия

Открыт блок «Сведения о занятости»; выбран статус «работа по найму».

### Тестовые данные

Дата начала работы: 01.02.2024.

### Шаги

1. В поле «Дата начала работы в компании» ввести 01.02.2024.

### Итоговый ожидаемый результат

Поле «Дата начала работы в компании» отображается и содержит дату 01.02.2024.

### Постусловия

Очистить поле при необходимости.

### TC-AFEIG-012

**Название:** Начало общего трудового стажа принимает формат ММ.ГГГГ
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** ATOM-021; ATOM-022; BSR 278; BSR 279; SRC-011; GAP-005

### Предусловия

Открыт блок «Сведения о занятости»; выбран любой заполненный социальный статус / тип занятости.

### Тестовые данные

Начало стажа: 05.2018.

### Шаги

1. В поле «Начало общего трудового стажа» ввести 05.2018.

### Итоговый ожидаемый результат

Поле «Начало общего трудового стажа» отображается и содержит значение 05.2018.

### Постусловия

Очистить поле при необходимости.

### TC-AFEIG-013

**Название:** Рабочий телефон принимает 10 цифр и использует шаблон +7
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** ATOM-015; ATOM-016; BSR 273; BSR 274; SRC-007; GAP-004

### Предусловия

Открыт блок «Сведения о занятости»; выбран статус «работа по найму».

### Тестовые данные

Рабочий телефон: 9234567890.

### Шаги

1. Установить фокус в поле «Рабочий телефон».
2. Ввести 9234567890.

### Итоговый ожидаемый результат

Поле «Рабочий телефон» содержит введенные 10 цифр в шаблоне с кодом страны «+7(xxx)-xxx-xx-xx».

### Постусловия

Очистить поле при необходимости.

### TC-AFEIG-014

**Название:** Основной среднемесячный доход принимает минимальную сумму X
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-023; ATOM-024; ATOM-025; ATOM-026; BSR 280; BSR 281; BSR 282; BSR 283; SRC-012

### Предусловия

Открыт блок «Сведения о занятости»; выбран любой заполненный социальный статус / тип занятости.

### Тестовые данные

Среднемесячный доход: 2000 при значении системной переменной `X = 2000`.

### Шаги

1. В поле «Среднемесячный доход после вычета налогов» основной работы ввести 2000.
2. Снять фокус с поля.

### Итоговый ожидаемый результат

Поле отображается и содержит значение 2000 без красной подсветки и без подсказки «Введите сумму более X руб.»

### Постусловия

Очистить поле при необходимости.

### TC-AFEIG-015

**Название:** Основной среднемесячный доход менее X показывает ошибку
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-026; BSR 283; SRC-012

### Предусловия

Открыт блок «Сведения о занятости»; выбран любой заполненный социальный статус / тип занятости.

### Тестовые данные

Среднемесячный доход: 1999 при значении системной переменной `X = 2000`.

### Шаги

1. В поле «Среднемесячный доход после вычета налогов» основной работы ввести 1999.
2. Снять фокус с поля.

### Итоговый ожидаемый результат

Поле окрашивается красным цветом и отображается подсказка «Введите сумму более X руб.»

### Постусловия

Ввести корректную сумму при необходимости.

### TC-AFEIG-016

**Название:** Основной среднемесячный доход более 1 000 000 показывает предупреждение
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-027; BSR 284; SRC-012

### Предусловия

Открыт блок «Сведения о занятости»; выбран любой заполненный социальный статус / тип занятости.

### Тестовые данные

Среднемесячный доход: 1000001.

### Шаги

1. В поле «Среднемесячный доход после вычета налогов» основной работы ввести 1000001.
2. Снять фокус с поля.

### Итоговый ожидаемый результат

Поле окрашивается красным цветом и отображается подсказка «Перепроверьте доход».

### Постусловия

Ввести корректную сумму при необходимости.

### TC-AFEIG-017

**Название:** Корзина основной работы очищает поля подблока
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-01
**Трассировка:** ATOM-028; BSR 285; SRC-013

### Предусловия

Открыт блок «Сведения о занятости»; поля основной работы заполнены тестовыми значениями.

### Тестовые данные

Заполненные поля основной работы: организация, ОПФ, фактический адрес работы, рабочий телефон, тип должности, должность, дата начала работы, начало стажа, среднемесячный доход.

### Шаги

1. Нажать кнопку «Корзина» в подблоке «Основная работа».

### Итоговый ожидаемый результат

Поля подблока «Основная работа» очищены.

### Постусловия

Заполнить поля заново, если заявка используется далее.

### TC-AFEIG-018

**Название:** Кнопка добавления источника дохода отображается всегда
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-035; BSR 290; SRC-017; SRC-019

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Не требуются.

### Шаги

1. Проверить область подблока «Дополнительный доход».

### Итоговый ожидаемый результат

Кнопка «Добавить источник дохода» отображается.

### Постусловия

Не требуются.

### TC-AFEIG-019

**Название:** Добавление источника дохода отображает поля дополнительного дохода
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-029; ATOM-030; ATOM-033; ATOM-036; BSR 286; BSR 288; BSR 291; SRC-014; SRC-015; SRC-016; SRC-017

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Не требуются.

### Шаги

1. Нажать кнопку «Добавить источник дохода».

### Итоговый ожидаемый результат

Отображается блок-повторитель «Дополнительный доход» с полями «Тип дохода» и «Среднемесячный доход после вычета налогов».

### Постусловия

Удалить добавленный источник дохода.

### TC-AFEIG-020

**Название:** Состав значений типа дохода
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-031; BSR 286; SRC-015; DICT-004

### Предусловия

Открыт блок-повторитель «Дополнительный доход».

### Тестовые данные

Значения DICT-004: Аренда; Пенсия; Дивиденды.

### Шаги

1. Открыть раскрывающийся список «Тип дохода».

### Итоговый ожидаемый результат

Список содержит все значения DICT-004 без дополнительных активных значений.

### Постусловия

Удалить добавленный источник дохода при необходимости.

### TC-AFEIG-021

**Название:** Тип дополнительного дохода нельзя добавить повторно
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-032; BSR 287; SRC-015; DICT-004

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Проверяемые значения: Пенсия; Аренда; Дивиденды.

### Шаги

1. Нажать «Добавить источник дохода».
2. В поле «Тип дохода» выбрать проверяемое значение.
3. Нажать «Добавить источник дохода» еще раз.
4. Открыть «Тип дохода» во втором блоке.

### Итоговый ожидаемый результат

Во втором блоке то же проверяемое значение недоступно для повторного выбора.

### Постусловия

Удалить добавленные источники дохода.

### TC-AFEIG-022

**Название:** Дополнительный среднемесячный доход принимает числовое значение
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-034; BSR 289; SRC-016; GAP-006

### Предусловия

Открыт блок-повторитель «Дополнительный доход».

### Тестовые данные

Среднемесячный доход: 15000.

### Шаги

1. В поле «Среднемесячный доход после вычета налогов» дополнительного дохода ввести 15000.

### Итоговый ожидаемый результат

Поле содержит значение 15000.

### Постусловия

Удалить добавленный источник дохода при необходимости.

### TC-AFEIG-023

**Название:** Корзина дополнительного дохода удаляет блок-повторитель
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-037; ATOM-038; BSR 292; BSR 293; SRC-017; SRC-018

### Предусловия

Открыт блок-повторитель «Дополнительный доход»; поля блока заполнены тестовыми значениями.

### Тестовые данные

Тип дохода: Аренда; среднемесячный доход: 15000.

### Шаги

1. Нажать пиктограмму «Корзина» в блоке «Дополнительный доход».

### Итоговый ожидаемый результат

Поля блока очищены, блок-повторитель «Дополнительный доход» удален.

### Постусловия

Не требуются.

### TC-AFEIG-024

**Название:** Способ подтверждения дохода отображается с default «Бумажное»
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-039; ATOM-040; BSR 294; BSR 295; SRC-020

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Не требуются.

### Шаги

1. Проверить группу радиокнопок «Способ подтверждения дохода».

### Итоговый ожидаемый результат

Группа радиокнопок отображается всегда; по умолчанию выбран вариант «Бумажное».

### Постусловия

Не требуются.

### TC-AFEIG-025

**Название:** Для бумажного подтверждения отображается поле загрузки подтверждения дохода
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-041; BSR 296; SRC-020; SRC-022

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Способ подтверждения дохода: Бумажное.

### Шаги

1. Выбрать вариант «Бумажное», если он не выбран.

### Итоговый ожидаемый результат

Отображается поле добавления файлов «Подтверждение дохода» для добавления скана документа.

### Постусловия

Не требуются.

### TC-AFEIG-026

**Название:** Для Госуслуг отображается текст о QR-коде подтверждения доходов
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-042; BSR 297; SRC-020; GAP-002

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Способ подтверждения дохода: Госуслуги.

### Шаги

1. В группе «Способ подтверждения дохода» выбрать «Госуслуги».

### Итоговый ожидаемый результат

На форме отображается текст о том, что при формировании анкеты будет сгенерирован QR-код для подтверждения доходов в Госуслугах.

### Постусловия

Вернуть способ подтверждения «Бумажное» при необходимости.

### TC-AFEIG-027

**Название:** Поле дохода клиента отображается всегда
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-044; BSR 299; SRC-021

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Не требуются.

### Шаги

1. Проверить поле «Доход клиента».

### Итоговый ожидаемый результат

Поле «Доход клиента» отображается.

### Постусловия

Не требуются.

### TC-AFEIG-028

**Название:** Подтверждение дохода загружается через проводник
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-045; ATOM-046; BSR 300; BSR 301; SRC-022

### Предусловия

Открыт блок «Сведения о занятости»; выбран способ подтверждения «Бумажное».

### Тестовые данные

Файл `income-proof.pdf`, формат pdf, размер не более 40 МБ.

### Шаги

1. В поле «Подтверждение дохода» нажать кнопку выбора файла.
2. Выбрать файл `income-proof.pdf`.

### Итоговый ожидаемый результат

Поле «Подтверждение дохода» отображается и принимает выбранный файл.

### Постусловия

Удалить загруженный файл.

### TC-AFEIG-029

**Название:** Подтверждение дохода загружается через Drag and Drop
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-046; BSR 301; SRC-022

### Предусловия

Открыт блок «Сведения о занятости»; выбран способ подтверждения «Бумажное».

### Тестовые данные

Файл `income-proof-dnd.jpg`, формат jpg, размер не более 40 МБ.

### Шаги

1. Перетащить файл `income-proof-dnd.jpg` в область поля «Подтверждение дохода».

### Итоговый ожидаемый результат

Поле «Подтверждение дохода» принимает файл, добавленный через Drag and Drop.

### Постусловия

Удалить загруженный файл.

### TC-AFEIG-030

**Название:** Некорректный файл подтверждения дохода показывает текст ошибки
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-047; BSR 302; SRC-022

### Предусловия

Открыт блок «Сведения о занятости»; выбран способ подтверждения «Бумажное».

### Тестовые данные

Файл `income-proof.txt`.

### Шаги

1. Добавить файл `income-proof.txt` в поле «Подтверждение дохода».

### Итоговый ожидаемый результат

Система отображает текст «Документы не загружены. Проверьте соответствуют ли документы требованиям: формат jpg, png, pdf, размер не более 40 МБ».

### Постусловия

Не требуются.

### TC-AFEIG-031

**Название:** После загрузки отображается название файла подтверждения дохода
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-048; BSR 303; SRC-022

### Предусловия

Открыт блок «Сведения о занятости»; выбран способ подтверждения «Бумажное».

### Тестовые данные

Файл `income-proof.pdf`, формат pdf, размер не более 40 МБ.

### Шаги

1. Добавить файл `income-proof.pdf` в поле «Подтверждение дохода».

### Итоговый ожидаемый результат

В поле отображается название прикрепленного бинарного файла `income-proof.pdf`.

### Постусловия

Удалить загруженный файл.

### TC-AFEIG-032

**Название:** Пиктограмма глаз открывает просмотр подтверждения дохода
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-050; ATOM-051; BSR 305; BSR 306; SRC-023

### Предусловия

В поле «Подтверждение дохода» вложен файл `income-proof.pdf`.

### Тестовые данные

Не требуются.

### Шаги

1. Нажать пиктограмму «глаз» рядом с вложенным файлом.

### Итоговый ожидаемый результат

Если вложен документ, пиктограмма «глаз» отображается и открывает всплывающее окно просмотра документа.

### Постусловия

Закрыть окно просмотра.

### TC-AFEIG-033

**Название:** Пиктограмма корзина скрывает вложенное подтверждение дохода
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-052; ATOM-053; BSR 307; BSR 308; SRC-024; GAP-007

### Предусловия

В поле «Подтверждение дохода» вложен файл `income-proof.pdf`.

### Тестовые данные

Не требуются.

### Шаги

1. Нажать пиктограмму «корзина» рядом с вложенным файлом.

### Итоговый ожидаемый результат

Если вложен документ, пиктограмма «корзина» отображается; после удаления файл перестает быть видимым в интерфейсе.

### Постусловия

Загрузить файл заново, если заявка используется далее.

### TC-AFEIG-034

**Название:** Прикрепить с телефона открывает QR-код для подтверждения дохода
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-046; ATOM-054; ATOM-055; BSR 301; BSR 309; BSR 310; SRC-022; SRC-025; GAP-008

### Предусловия

Открыт блок «Сведения о занятости»; выбран способ подтверждения «Бумажное».

### Тестовые данные

Не требуются.

### Шаги

1. Нажать кнопку «Прикрепить с телефона» в области подтверждения дохода.

### Итоговый ожидаемый результат

Кнопка отображается; система показывает всплывающее окно со сгенерированным QR-кодом для прикрепления файлов с мобильного телефона.

### Постусловия

Закрыть всплывающее окно.

### TC-AFEIG-035

**Название:** Основной среднемесячный доход не принимает нечисловое значение
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-056; BSR 281; SRC-012; GAP-009

### Предусловия

Открыт блок «Сведения о занятости»; выбран любой заполненный социальный статус / тип занятости.

### Тестовые данные

Среднемесячный доход: `abc`.

### Шаги

1. Установить фокус в поле «Среднемесячный доход после вычета налогов» основной работы.
2. Попытаться ввести `abc`.

### Итоговый ожидаемый результат

В поле «Среднемесячный доход после вычета налогов» основной работы не введено значение, отличное от числового.

### Постусловия

Очистить поле при необходимости.

### TC-AFEIG-036

**Название:** Основной среднемесячный доход не принимает отрицательное значение
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-056; BSR 281; SRC-012; GAP-009

### Предусловия

Открыт блок «Сведения о занятости»; выбран любой заполненный социальный статус / тип занятости.

### Тестовые данные

Среднемесячный доход: `-2000`.

### Шаги

1. Установить фокус в поле «Среднемесячный доход после вычета налогов» основной работы.
2. Попытаться ввести `-2000`.

### Итоговый ожидаемый результат

В поле «Среднемесячный доход после вычета налогов» основной работы отрицательное значение не введено.

### Постусловия

Очистить поле при необходимости.

### TC-AFEIG-037

**Название:** Основной среднемесячный доход принимает дробное значение через точку и отображает сотые
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-056; BSR 281; SRC-012; GAP-009

### Предусловия

Открыт блок «Сведения о занятости»; выбран любой заполненный социальный статус / тип занятости.

### Тестовые данные

Среднемесячный доход: `66660.981`.

### Шаги

1. В поле «Среднемесячный доход после вычета налогов» основной работы ввести `66660.981`.
2. Снять фокус с поля.

### Итоговый ожидаемый результат

Поле «Среднемесячный доход после вычета налогов» основной работы отображает значение `66 660.98`, округленное до сотых и введенное через разделитель дробной части `"."`.

### Постусловия

Очистить поле при необходимости.

### TC-AFEIG-038

**Название:** Основной среднемесячный доход 1 000 000 не показывает предупреждение о превышении
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-027; BSR 284; SRC-012

### Предусловия

Открыт блок «Сведения о занятости»; выбран любой заполненный социальный статус / тип занятости.

### Тестовые данные

Среднемесячный доход: 1000000.

### Шаги

1. В поле «Среднемесячный доход после вычета налогов» основной работы ввести 1000000.
2. Снять фокус с поля.

### Итоговый ожидаемый результат

Поле содержит значение 1000000 без красной подсветки и без подсказки «Перепроверьте доход».

### Постусловия

Очистить поле при необходимости.

### TC-AFEIG-039

**Название:** Пиктограмма глаз не отображается до добавления подтверждения дохода
**Тип:** Negative
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-050; BSR 305; SRC-023

### Предусловия

Открыт блок «Сведения о занятости»; выбран способ подтверждения «Бумажное»; в поле «Подтверждение дохода» нет вложенного файла.

### Тестовые данные

Не требуются.

### Шаги

1. Проверить область поля «Подтверждение дохода» до добавления файла.

### Итоговый ожидаемый результат

Пиктограмма «глаз» для просмотра подтверждения дохода не отображается, так как документ не вложен.

### Постусловия

Не требуются.

### TC-AFEIG-040

**Название:** После загрузки нескольких файлов подтверждения дохода названия отображаются через запятую
**Тип:** Positive
**Приоритет:** Medium
**package_id:** WP-02
**Трассировка:** ATOM-048; BSR 303; SRC-022

### Предусловия

Открыт блок «Сведения о занятости»; выбран способ подтверждения «Бумажное».

### Тестовые данные

Файлы `income-proof.pdf` и `tax-return.png`, форматы pdf/png, размер каждого файла не более 40 МБ.

### Шаги

1. Добавить файлы `income-proof.pdf` и `tax-return.png` в поле «Подтверждение дохода».

### Итоговый ожидаемый результат

В поле отображаются названия прикрепленных бинарных файлов `income-proof.pdf, tax-return.png` через запятую.

### Постусловия

Удалить загруженные файлы.

### TC-AFEIG-041

**Название:** Рабочий телефон, должность и дата начала работы скрыты для пенсионера и самозанятого
**Тип:** Positive
**Приоритет:** High
**package_id:** WP-01
**Трассировка:** ATOM-014; ATOM-019; ATOM-020; BSR 272; BSR 276; BSR 277; SRC-007; SRC-009; SRC-010; DICT-001

### Предусловия

Открыт блок «Сведения о занятости».

### Тестовые данные

Проверяемые значения: пенсионер (не работает); самозанятый.

### Шаги

1. Выбрать значение «пенсионер (не работает)».
2. Проверить поля «Рабочий телефон», «Должность» и «Дата начала работы в компании».
3. Выбрать значение «самозанятый».
4. Повторно проверить поля «Рабочий телефон», «Должность» и «Дата начала работы в компании».

### Итоговый ожидаемый результат

Для каждого проверяемого значения поля «Рабочий телефон», «Должность» и «Дата начала работы в компании» не отображаются.

### Постусловия

Не требуются.

### TC-AFEIG-042

**Название:** Файл подтверждения дохода размером более 40 МБ показывает текст ошибки
**Тип:** Negative
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-047; BSR 302; SRC-022

### Предусловия

Открыт блок «Сведения о занятости»; выбран способ подтверждения «Бумажное».

### Тестовые данные

Файл `income-proof.pdf`, формат pdf, размер более 40 МБ.

### Шаги

1. Добавить файл `income-proof.pdf` в поле «Подтверждение дохода».

### Итоговый ожидаемый результат

Система отображает текст «Документы не загружены. Проверьте соответствуют ли документы требованиям: формат jpg, png, pdf, размер не более 40 МБ».

### Постусловия

Не требуются.

### TC-AFEIG-043

**Название:** При выборе Госуслуг и сохранении заявки направляется запрос в сервис
**Тип:** Integration
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-042; BSR 298; SRC-020

### Предусловия

Открыт блок «Сведения о занятости»; интеграция с Госуслугами доступна на стенде или заменена управляемым stub-сервисом; доступен наблюдаемый источник проверки запроса: service log, stub history, API evidence или иной согласованный артефакт.

### Тестовые данные

Способ подтверждения дохода: Госуслуги.

### Шаги

1. В группе «Способ подтверждения дохода» выбрать «Госуслуги».
2. Сохранить заявку через кнопку «Далее».
3. Проверить evidence запроса в сервис Госуслуг.

### Итоговый ожидаемый результат

После выбора «Госуслуги» и сохранения заявки система направляет запрос в сервис Госуслуг для подтверждения доходов.

### Постусловия

Вернуть способ подтверждения «Бумажное» или очистить тестовую заявку при необходимости.

### TC-AFEIG-044

**Название:** Подтверждение дохода сохраняется в электронном архиве после добавления файла
**Тип:** Integration
**Приоритет:** High
**package_id:** WP-02
**Трассировка:** ATOM-047; BSR 304; SRC-022

### Предусловия

Открыт блок «Сведения о занятости»; выбран способ подтверждения «Бумажное»; в поле «Подтверждение дохода» нет вложенного файла; доступен наблюдаемый источник проверки электронного архива Банка: archive UI, API, БД или лог.

### Тестовые данные

Файл `income-proof-archive.pdf`, формат pdf, размер не более 40 МБ.

### Шаги

1. Добавить файл `income-proof-archive.pdf` в поле «Подтверждение дохода».
2. Проверить запись о сохранении документа в электронном архиве Банка через доступный evidence source.

### Итоговый ожидаемый результат

После добавления файла документ «Подтверждение дохода» сохранен в электронном архиве Банка и связан с текущей заявкой.

### Постусловия

Удалить загруженный файл или очистить тестовую заявку по правилам стенда.

## 10. Визуальная информация, согласия и проверки

Источник порядка: Таблица 4, rows 130-136.

## TC-ACVCC-001

**Название:** Отображение блока `Визуальная информация` и поля `Визуальная информация`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-001`; `ATOM-002`; `SRC-001`; `SRC-002`; `BSR 311`

### Предусловия

- Открыта карточка заявки.

### Тестовые данные

Не требуются.

### Шаги

1. Перейти к области карточки заявки с блоком визуальной оценки.

### Итоговый ожидаемый результат

В карточке заявки отображается блок `Визуальная информация` с полем `Визуальная информация`.

### Постусловия

Не требуются.

## TC-ACVCC-002

**Название:** Значение по умолчанию `Нет` в поле `Визуальная информация`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-003`; `SRC-002`; `BSR 312`

### Предусловия

- Открыта карточка заявки.
- Значение поля `Визуальная информация` в текущей карточке пользователем еще не изменялось.

### Тестовые данные

Не требуются.

### Шаги

1. Открыть блок `Визуальная информация`.

### Итоговый ожидаемый результат

В поле `Визуальная информация` отображается значение `Нет`.

### Постусловия

Не требуются.

## TC-ACVCC-003

**Название:** Отображение параметров визуальной оценки при выборе `Да`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-004`; `ATOM-005`; `SRC-002`; `SRC-003`; `BSR 313`; `BSR 314`

### Предусловия

- Открыта карточка заявки.
- Поле `Визуальная информация` доступно для изменения.

### Тестовые данные

- Значение поля `Визуальная информация`: `Да`.

### Шаги

1. В поле `Визуальная информация` выбрать значение `Да`.

### Итоговый ожидаемый результат

После выбора `Да` отображается список `Параметры визуальной оценки` с возможностью множественного выбора.

### Постусловия

- Если состояние карточки сохраняется, вернуть поле `Визуальная информация` в исходное значение `Нет`.

## TC-ACVCC-004

**Название:** Checkbox-контролы для значений `Параметры визуальной оценки`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-006`; `SRC-003`; `BSR 315`; `DICT-001`

### Предусловия

- Открыта карточка заявки.
- В поле `Визуальная информация` выбрано значение `Да`.
- Список `Параметры визуальной оценки` отображается.

### Тестовые данные

- Справочник `Параметры визуальной оценки`: полный состав групп и значений из `work/test-design/14-application-card/appendix-1-visual-assessment-reference.md`.

### Шаги

1. Просмотреть отображенные группы и значения списка `Параметры визуальной оценки`.

### Итоговый ожидаемый результат

Список `Параметры визуальной оценки` содержит все группы и значения из Приложения 1; у каждого отображенного значения доступен checkbox.

### Постусловия

- Если состояние карточки сохраняется, вернуть поле `Визуальная информация` в исходное значение `Нет`.

## TC-ACVCC-005

**Название:** Отметка checkbox одного значения в `Параметры визуальной оценки`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-006`; `SRC-003`; `BSR 315`; `DICT-001`

### Предусловия

- Открыта карточка заявки.
- В поле `Визуальная информация` выбрано значение `Да`.
- Список `Параметры визуальной оценки` отображается.

### Тестовые данные

- Любое значение списка `Параметры визуальной оценки` из Приложения 1, кроме `Другое (комментарий обязателен)`.

### Шаги

1. Отметить checkbox одного отображенного значения из тестовых данных.

### Итоговый ожидаемый результат

Выбранное значение списка `Параметры визуальной оценки` отображается отмеченным.

### Постусловия

- Если состояние карточки сохраняется, снять отметку выбранного checkbox и вернуть поле `Визуальная информация` в исходное значение `Нет`.

## TC-ACVCC-019

**Название:** Нельзя пройти дальше без выбранного параметра визуальной оценки

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-007`; `SRC-002`; `SRC-003`; `BSR 316`

### Предусловия

- Открыта карточка заявки.
- Блок `Визуальная информация` раскрыт.
- Поле `Визуальная информация` установлено в значение `Да`.
- Список `Параметры визуальной оценки` отображается.

### Тестовые данные

Не требуются.

### Шаги

1. Убедиться, что ни один checkbox в списке `Параметры визуальной оценки` не выбран.
2. Нажать кнопку `Далее`.

### Итоговый ожидаемый результат

Переход на следующий этап не выполняется; система требует выбрать хотя бы одно значение в списке `Параметры визуальной оценки`.

### Постусловия

Выбрать валидный параметр визуальной оценки или отменить несохраненные изменения.

## TC-ACVCC-006

**Название:** Скрытие параметров визуальной оценки при значении `Нет`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-004`; `ATOM-005`; `SRC-002`; `SRC-003`; `BSR 313`; `BSR 314`

### Предусловия

- Открыта карточка заявки.
- Поле `Визуальная информация` доступно для изменения.

### Тестовые данные

- Значение поля `Визуальная информация`: `Нет`.

### Шаги

1. В поле `Визуальная информация` выбрать значение `Нет`.

### Итоговый ожидаемый результат

Список `Параметры визуальной оценки` не отображается.

### Постусловия

Не требуются.

## TC-ACVCC-007

**Название:** Блок `Согласия / проверки` свернут по умолчанию и раскрывается

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-009`; `SRC-004`; `BSR 318`

### Предусловия

- Открыта карточка заявки.
- Блок `Согласия / проверки` пользователем еще не раскрывался.

### Тестовые данные

Не требуются.

### Шаги

1. Найти блок `Согласия / проверки`.
2. Раскрыть блок `Согласия / проверки`.

### Итоговый ожидаемый результат

Изначально блок `Согласия / проверки` отображается свернутым; после действия пользователя содержимое блока отображается раскрытым.

### Постусловия

Не требуются.

## TC-ACVCC-008

**Название:** Отображение виджета `Согласия` с полем `БКИ и персональные данные`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-010`; `SRC-005`; `BSR 319`; `DICT-002`

### Предусловия

- Открыта карточка заявки.
- Блок `Согласия / проверки` раскрыт.

### Тестовые данные

- Состав подблока `Согласия`: `APP2-CHECK-001` из `work/test-design/14-application-card/appendix-2-consents-checks-reference.md`.

### Шаги

1. Просмотреть подблок `Согласия`.

### Итоговый ожидаемый результат

В подблоке `Согласия` отображается поле `БКИ и персональные данные` и текст согласия из Приложения 2.

### Постусловия

Не требуются.

## TC-ACVCC-009

**Название:** Значение по умолчанию `Да` для `БКИ и персональные данные`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-011`; `SRC-005`; `BSR 320`; `DICT-002`

### Предусловия

- Открыта карточка заявки.
- Блок `Согласия / проверки` раскрыт.
- Значение поля `БКИ и персональные данные` пользователем еще не изменялось.

### Тестовые данные

Не требуются.

### Шаги

1. Открыть подблок `Согласия`.

### Итоговый ожидаемый результат

В поле `БКИ и персональные данные` отображается значение `Да`.

### Постусловия

Не требуются.

## TC-ACVCC-010

**Название:** Проставление checkbox в поле `БКИ и персональные данные`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-012`; `SRC-005`; `BSR 319`; `DICT-002`; `GAP-002`

### Предусловия

- Открыта карточка заявки.
- Блок `Согласия / проверки` раскрыт.

### Тестовые данные

- Поле `БКИ и персональные данные`: подготовительное значение `Нет`, проверяемое значение `Да`.

### Шаги

1. В поле `БКИ и персональные данные` снять checkbox, чтобы поле отображалось в состоянии `Нет`.
2. В поле `БКИ и персональные данные` установить checkbox в значение `Да`.

### Итоговый ожидаемый результат

Checkbox поля `БКИ и персональные данные` отображается отмеченным после повторного проставления из состояния `Нет`.

### Постусловия

Не требуются: итоговое значение поля соответствует default `Да`.

## TC-ACVCC-011

**Название:** Отображение `FATCA/CRS проверка` и значение по умолчанию `Нет`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-013`; `ATOM-014`; `SRC-006`; `BSR 321`; `BSR 322`; `DICT-002`

### Предусловия

- Открыта карточка заявки.
- Блок `Согласия / проверки` раскрыт.
- Значение поля `FATCA/CRS проверка` пользователем еще не изменялось.

### Тестовые данные

- Состав подблока `FATCA/CRS проверка`: `APP2-CHECK-002` из `work/test-design/14-application-card/appendix-2-consents-checks-reference.md`.

### Шаги

1. Просмотреть подблок `FATCA/CRS проверка`.

### Итоговый ожидаемый результат

В подблоке отображается поле `FATCA/CRS проверка` из Приложения 2 со значением по умолчанию `Нет`.

### Постусловия

Не требуются.

## TC-ACVCC-012

**Название:** Проставление checkbox `FATCA/CRS проверка`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-015`; `SRC-006`; `BSR 321`; `DICT-002`; `GAP-002`

### Предусловия

- Открыта карточка заявки.
- Блок `Согласия / проверки` раскрыт.

### Тестовые данные

- Поле `FATCA/CRS проверка`: `Да`.

### Шаги

1. В поле `FATCA/CRS проверка` установить checkbox в значение `Да`.

### Итоговый ожидаемый результат

Checkbox поля `FATCA/CRS проверка` отображается отмеченным.

### Постусловия

- Если состояние карточки сохраняется, вернуть поле `FATCA/CRS проверка` в исходное значение `Нет`.

## TC-ACVCC-013

**Название:** Отображение полей подблока `AML проверка`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-016`; `SRC-007`; `BSR 323`; `DICT-002`

### Предусловия

- Открыта карточка заявки.
- Блок `Согласия / проверки` раскрыт.

### Тестовые данные

- `Иностранное публичное должностное лицо`.
- `Должностное лицо публичной международной организации`.
- `Должностное лицо Российской Федерации, включенное в перечни должностей, определяемые Президентом Российской Федерации, а также назначаемое/освобождаемое от должности Президентом или Правительством Российской Федерации (РПДЛ)`.
- `Родственник ИПДЛ`.
- `Родственник МПДЛ`.
- `Родственник РПДЛ`.

### Шаги

1. Просмотреть подблок `AML проверка`.

### Итоговый ожидаемый результат

В подблоке `AML проверка` отображаются все поля `APP2-CHECK-003` из Приложения 2.

### Постусловия

Не требуются.

## TC-ACVCC-014

**Название:** Значение по умолчанию `Нет` во всех полях `AML проверка`

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-02

**Трассировка:** `ATOM-017`; `SRC-007`; `BSR 324`; `DICT-002`

### Предусловия

- Открыта карточка заявки.
- Блок `Согласия / проверки` раскрыт.
- Поля подблока `AML проверка` пользователем еще не изменялись.

### Тестовые данные

- Поля подблока `AML проверка`: `APP2-CHECK-003` из `work/test-design/14-application-card/appendix-2-consents-checks-reference.md`.

### Шаги

1. Просмотреть значения полей подблока `AML проверка`.

### Итоговый ожидаемый результат

Во всех полях подблока `AML проверка` из Приложения 2 отображается значение `Нет`.

### Постусловия

Не требуются.

## TC-ACVCC-015

**Название:** Проставление checkbox в поле подблока `AML проверка`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-02

**Трассировка:** `ATOM-018`; `SRC-007`; `BSR 323`; `DICT-002`; `GAP-002`

### Предусловия

- Открыта карточка заявки.
- Блок `Согласия / проверки` раскрыт.

### Тестовые данные

- Поле `Родственник РПДЛ`: `Да`.

### Шаги

1. В подблоке `AML проверка` установить checkbox поля `Родственник РПДЛ` в значение `Да`.

### Итоговый ожидаемый результат

Checkbox поля `Родственник РПДЛ` отображается отмеченным.

### Постусловия

- Если состояние карточки сохраняется, вернуть поле `Родственник РПДЛ` в исходное значение `Нет`.

## TC-ACVCC-016

**Название:** Появление комментария при выборе `Другое` в параметрах визуальной оценки

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-008`; `SRC-003`; `BSR 317`; `DICT-001`

### Предусловия

- Открыта карточка заявки.
- В поле `Визуальная информация` выбрано значение `Да`.
- Список `Параметры визуальной оценки` отображается.
- В одном из блоков 1-7 списка отображается checkbox `Другое (комментарий обязателен)`.

### Тестовые данные

- Блок списка с checkbox `Другое (комментарий обязателен)`: любой отображенный блок 1-7 из Приложения 1.

### Шаги

1. Отметить checkbox `Другое (комментарий обязателен)` в выбранном блоке.

### Итоговый ожидаемый результат

После выбора `Другое` в выбранном блоке отображается поле `Комментарий`.

### Постусловия

- Если состояние карточки сохраняется, снять checkbox `Другое` и вернуть поле `Визуальная информация` в исходное значение `Нет`.

## TC-ACVCC-017

**Название:** Сохранение введенного комментария при снятии и повторном выборе `Другое`

**Тип:** Positive

**Приоритет:** Medium

**package_id:** WP-01

**Трассировка:** `ATOM-020`; `SRC-003`; `BSR 317`; `DICT-001`

### Предусловия

- Открыта карточка заявки.
- В поле `Визуальная информация` выбрано значение `Да`.
- Список `Параметры визуальной оценки` отображается.
- В одном из блоков 1-7 списка отображается checkbox `Другое (комментарий обязателен)`.

### Тестовые данные

- Блок списка с checkbox `Другое (комментарий обязателен)`: любой отображенный блок 1-7 из Приложения 1.
- Значение поля `Комментарий`: `Тестовый комментарий`.

### Шаги

1. Отметить checkbox `Другое (комментарий обязателен)` в выбранном блоке.
2. В появившемся поле `Комментарий` ввести значение из тестовых данных.
3. Снять checkbox `Другое`.
4. Повторно отметить checkbox `Другое` в том же блоке без закрытия формы.

### Итоговый ожидаемый результат

После снятия checkbox `Другое` поле `Комментарий` перестает отображаться; после повторного выбора `Другое` в рамках текущего открытия формы поле `Комментарий` снова отображается с ранее введенным значением.

### Постусловия

- Если состояние карточки сохраняется, снять checkbox `Другое` и вернуть поле `Визуальная информация` в исходное значение `Нет`.

## TC-ACVCC-018

**Название:** Сообщение обязательности для пустого комментария при выбранном `Другое`

**Тип:** Negative

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `ATOM-019`; `SRC-003`; `BSR 317`; `DICT-001`

### Предусловия

- Открыта карточка заявки.
- В поле `Визуальная информация` выбрано значение `Да`.
- Список `Параметры визуальной оценки` отображается.
- В одном из блоков 1-7 списка отображается checkbox `Другое (комментарий обязателен)`.

### Тестовые данные

- Блок списка с checkbox `Другое (комментарий обязателен)`: любой отображенный блок 1-7 из Приложения 1.
- Значение поля `Комментарий`: пустое.

### Шаги

1. Отметить checkbox `Другое (комментарий обязателен)` в выбранном блоке.
2. Перевести фокус в появившееся поле `Комментарий`.
3. Не заполняя поле `Комментарий`, перевести фокус из поля в другой элемент формы.

### Итоговый ожидаемый результат

После потери фокуса пустым полем `Комментарий` отображается сообщение `Обязательно к заполнению`.

### Постусловия

- Если состояние карточки сохраняется, снять checkbox `Другое` и вернуть поле `Визуальная информация` в исходное значение `Нет`.

## 11. Действия «Далее» и «Вернуть в общий список»

Источник порядка: Описание действий в разделе «Заявка».

### TC-AF-CARD-003

**Название:** Кнопка Далее проверяет готовность заявки и переводит на следующий этап

**Тип:** Business

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `Описание действий в разделе «Заявка»`; `BSR 327`

### Предусловия

Открыта карточка `Заявка`; доступна заявка, в которой можно подготовить все обязательные поля, обязательные документы, детализацию доходов, проверенный ИНН и выбранное предложение в калькуляторе.

### Тестовые данные

- Валидные значения для всех обязательных полей заявки.
- Набор обязательных документов по типу программы.
- Заполненная детализация доходов, если она требуется текущей заявке.
- `ИНН проверен = Да`.
- Выбранное предложение в кредитном калькуляторе.

### Шаги

1. Заполнить все обязательные поля заявки валидными значениями.
2. Приложить обязательные документы по типу программы.
3. Заполнить детализацию доходов, если она требуется.
4. Убедиться, что `ИНН проверен = Да`.
5. Убедиться, что в кредитном калькуляторе выбрано предложение.
6. Нажать кнопку `Далее`.

### Итоговый ожидаемый результат

Система проверяет обязательные поля, обязательные документы, детализацию доходов, `ИНН проверен = Да` и наличие выбранного предложения в калькуляторе. Если все проверки успешны, заявка переводится на следующий этап.

### Постусловия

Вернуть тестовую заявку в исходное состояние, если это требуется правилами стенда.

### TC-AF-CARD-004

**Название:** Отмена возврата заявки в общий список

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `Описание действий в разделе «Заявка»`; `BSR 328`

### Предусловия

Заявка закреплена за текущим пользователем и открыта в карточке `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Нажать кнопку `Вернуть в общий список`.
2. В окне подтверждения проверить текст и доступные кнопки.
3. Нажать кнопку `Нет`.

### Итоговый ожидаемый результат

Отображается всплывающее окно с текстом `Вы уверены, что хотите вернуть заявку в общий список заявок` и кнопками `Да` и `Нет`. После нажатия `Нет` окно закрывается, заявка остается закрепленной за текущим пользователем.

### Постусловия

Не требуются.

### TC-AF-CARD-005

**Название:** Подтверждение возврата заявки в общий список

**Тип:** Positive

**Приоритет:** High

**package_id:** WP-01

**Трассировка:** `Описание действий в разделе «Заявка»`; `BSR 328`

### Предусловия

Заявка закреплена за текущим пользователем и открыта в карточке `Заявка`.

### Тестовые данные

Не требуются.

### Шаги

1. Нажать кнопку `Вернуть в общий список`.
2. В окне подтверждения нажать кнопку `Да`.
3. Проверить доступность заявки в общем списке заявок.

### Итоговый ожидаемый результат

Заявка отвязывается от текущего пользователя, возвращается в общий список заявок и становится видима и доступна для привязки другим сотрудникам.

### Постусловия

При необходимости заново закрепить заявку за тестовым пользователем.
