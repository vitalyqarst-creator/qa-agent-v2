# Исправление матрицы — 9.3.3 «Карточка реквизитов»

## Основание

- Reviewer-сессия: `01a00593-a62b-7d02-9d32-17b37266ac68`.
- Исходный вердикт: `matrix-changes-required`.
- Выполнен единственный разрешённый пакет исправлений. Матрица остаётся `draft` до проверки закрытия findings в независимой reviewer-сессии.

## Выполненные исправления

| Finding | Изменение | Затронутые артефакты | Остаточное ограничение |
| --- | --- | --- | --- |
| P1: неатомарная обязательность пяти полей | `M-G-001` заменена на `M-G-001`–`M-G-005`: отдельная идея для каждого обязательного поля. Для «Наименования партнёра», БИК и корреспондентского счёта зафиксировано отсутствие подтверждённого пути к пустому readonly-состоянию; для банка/БИК и расчётного счёта отдельно зафиксирована неизвестная наблюдаемая реакция. | `source-scope.md`, `scope-clarification-requests.md`, `test-design-matrix.md` | `GAP-005`, `GAP-012`; UI-калибровка нужна, но сообщение, подсветка и блокировка не предполагаются. |
| P1: пропущен глобальный контракт типа «Дата» | В source scope добавлены `SRC-9.3.3-040`–`043`: границы года, месяца, дня и сохранение без сдвига часового пояса. В матрице добавлены допустимые календарные классы, отдельные негативные классы синтаксиса/года/месяца/дня и проверка сохранения даты. | `source-scope.md`, `scope-clarification-requests.md`, `test-design-matrix.md` | `GAP-010`; реакция на недопустимую дату и способ наблюдать сохранённое значение требуют UI-калибровки или среды наблюдения. |
| P2: неоднозначна граница «40 МБ» | Добавлены `GAP-011` и `CLR-004` о точном числе байт. `M-012` покрывает все три формата на 1 000 000 байт, `M-013` — неподдерживаемый формат и файл 42 000 000 байт, гарантированно превышающий обе распространённые трактовки 40 МБ. `M-G-014` отдельно удерживает точную границу до ответа. | `source-scope.md`, `scope-clarification-requests.md`, `test-design-matrix.md` | Без ответа `CLR-004` не проверять файлы ровно на границе и на один байт больше неё. |

## Узкая правка по незакрытому замечанию 2

- `01.01.10000` исключено из `M-G-007` и перенесено в `M-G-006`: пятизначный год нарушает синтаксис формата `дд.мм.гггг`, а не только диапазон года.
- В `M-G-007` оставлено только `31.12.1899` как представитель года ниже нижней границы `1900`.
- Не менялись границы scope, source assertions, вопросы БА, другие строки матрицы, тест-кейсы или review.

## Проверка writer-а

- В матрице есть строки для всех `SRC-9.3.3-001`–`SRC-9.3.3-043` либо точные `M-G-*` для ограничений.
- Разные обязательные поля, календарные классы и файловые границы не склеены в общий ожидаемый результат.
- Для файлов явно описана выбранная выборка и остаточный риск комбинаций разрешённого формата с точной границей размера.
- Тест-кейсы, повторный review и другие workflow-артефакты не создавались.
