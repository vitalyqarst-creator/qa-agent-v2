# Исправление тест-кейсов — 9.3.3 «Карточка реквизитов»

## Основание

- Reviewer-сессия: `01a005b2-95bf-7a02-8277-e68fa519e44c`.
- Исходный вердикт: `tc-changes-required`.
- Выполнен единственный разрешённый пакет исправлений по findings 1–3. Набор остаётся `draft` до проверки закрытия findings независимым reviewer-ом.

## Выполненные исправления

| Finding | Изменение | Затронутые артефакты | Остаточное ограничение |
| --- | --- | --- | --- |
| 1. Полный валидный набор для сохранения и отмены | В `TC-9.3.3-007`, `018`, `021`, `022`, `024`, `029` и `030` произвольные номера, выдаваемые за сохраняемые, заменены свойствами подготовленного набора: текущий поставщик с известным наименованием, запись БИК с известными наименованием банка/БИК/корр. счётом и подтверждённый допустимый цифровой расчётный счёт с нужным исходным отсутствием или ранее сохранённым значением. Обе ветви отмены/закрытия начинаются с такого полного набора. | `test-cases/9.3.3-9-3-3-kartochka-rekvizitov.md`, `test-design-matrix.md` | Набор БИК и сохранённые банковские данные отсутствуют в пакете (`GAP-002`); затронутые кейсы помечены `needs-test-data`. |
| 2. Ненаблюдаемый статус | В `TC-9.3.3-029` и `M-014` из основного ожидаемого результата исключён внутренний статус «Подтвержден». Основной oracle — зелёный индикатор на сущности. `GAP-008` уточнён: для статуса не передан наблюдаемый UI/API/log/DB-артефакт, а расположение индикатора требует калибровки. | `test-cases/9.3.3-9-3-3-kartochka-rekvizitov.md`, `test-design-matrix.md`, `source-scope.md`, `scope-clarification-requests.md` | До UI-калибровки не известно расположение и представление зелёного индикатора; статус «Подтвержден» не проверяется как pass/fail результат. |
| 3. Дубли digits-only | `TC-9.3.3-020` параметризован значениями с латинской буквой, кириллической буквой, пробелом и специальным символом. Латинская и кириллическая буквы явно сохранены как `derived input variants` разных Unicode-наборов с единым действием и ожидаемой реакцией, а не как самостоятельные source-бизнес-классы. Отдельные прежние `TC-9.3.3-021`–`025` удалены; остальные кейсы перенумерованы непрерывно. | `test-cases/9.3.3-9-3-3-kartochka-rekvizitov.md`, `test-design-matrix.md` | Механизм отклонения для каждого параметра не задан источником и остаётся `candidate-ui-calibration`; прочие буквенные Unicode-символы не представлены. |

## Узкая exception-правка closure finding 3

- В `M-G-011` и `TC-9.3.3-020` явно закреплено, что латинская `A` и кириллическая `А` — `derived input variants` разных Unicode-наборов одного буквенного класса.
- Сценарий, число кейсов, источники, основной ожидаемый результат и статус исполнения не менялись.
- Остаточный риск ограничен прочими буквенными Unicode-символами, не представленными в параметрах.

## Самопроверка writer-а

- В canonical-наборе 30 кейсов с непрерывными идентификаторами и номерами `1/30`–`30/30`.
- Ровно один кейс с трассировкой `M-G-011` содержит все четыре параметра; латинская и кириллическая буквы отмечены как `derived input variants` разных Unicode-наборов, а новые требования и новые matrix-строки не добавлялись.
- В основных ожидаемых результатах отсутствует ненаблюдаемый статус «Подтвержден».
- Неформируемые matrix gap-строки не превращены в тест-кейсы.
- Независимый TC review не запускался.
