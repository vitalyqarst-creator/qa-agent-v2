# Независимое review v3, раунд 2 — 9.3.2 «Карточка партнёра»

**Вердикт:** `changes-required`.

Review выполнено отдельной верхнеуровневой Codex-сессией по commit
`bbe8295ff7857063f003c70f327ccd188731555e`; reviewer не изменял файлы.

## Findings и решение

1. Статусы матрицы для недопустимых дат не совпадают с кандидатными TC.
   **Принято:** строки `SCN-9-3-2-035-INVALID-*` переведены в
   `candidate-ui-calibration`.
2. Для перехода к существующей карточке reviewer не нашёл source-backed
   основания. **Отклонено:** DOCX/XHTML, раздел 9.3, `AS.5`, пункт 1 прямо
   требует выдать подсказку о существующем партнёре и дать возможность перейти
   в его карточку. Исправлена трассировка `TC-9-3-2-046-CREATE` и строки
   матрицы; сам сценарий сохранён.
3. Сценарии альтернативной кнопки закрытия содержат открытый UI-вопрос при
   статусе `needs-test-data`. **Принято:** `TC-9-3-2-046-CREATE` и
   `TC-9-3-2-051-*`/`052-*` переведены в `candidate-ui-calibration`.
4. В `TC-9-3-2-047-PAIR-NAME-EDIT` дублируется условие о заглушке DaData или
   ручном вводе. **Принято:** условие оставлено только в «Тестовых данных».
5. Отсутствуют full practical v0.9 receipt-артефакты именно внутри папки
   кандидата. **Не является дефектом test-design:** v3 — отдельный кандидат,
   а не canonical baseline; его source obligations, workflow state и старые
   receipts находятся в исходном scope `work/practical-v0.9/9.3.2-kartochka-partnera/`.
   До формального выпуска baseline потребуется отдельная migration-процедура,
   но она не нужна для независимой оценки содержания кандидатных TC.

## Следующее действие

Провести короткий независимый re-review только изменённых findings 1–4 и
источника `AS.5`; не создавать и не менять baseline v2.
