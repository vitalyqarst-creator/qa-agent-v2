# Независимое review v3, раунд 1 — 9.3.2 «Карточка партнёра»

**Вердикт:** `changes-required`.

Review выполнено отдельной верхнеуровневой Codex-сессией по снимку кандидата
на commit `8177385`; reviewer не изменял файлы кандидата.

## Findings

1. Не покрыты границы 2 000 / 2 001 символ для полей типа «Текст» и одиночный
   выбор для списка «Тип партнёра».
2. Решение БА `BA-DEC-003` не доказано частичными совпадениями ключа:
   наименование совпадает при ином ИНН и ИНН совпадает при ином наименовании.
3. Негативные даты не должны подразумевать неизвестную форму UI-реакции;
   требуется `candidate-ui-calibration`. В трассировке должна быть Таблица 4.
4. Edit-варианты проверок дубликата не должны описывать создание новой
   карточки; изменение типа должно отличаться от исходного значения.
5. Успешные create-сценарии с парой `ПАО СБЕРБАНК` / `7707083893` требуют
   явного отсутствия этой пары до начала прогона.

## Реакция writer-а

Все пять findings внесены в отдельный кандидат v3, revision 2. Статус негативных дат
исправлен на `candidate-ui-calibration`; прочие кейсы сохраняют
`needs-test-data`, поскольку требуют внешней подготовки доступа, карточек,
файлов или заглушки. Требуется финальное независимое review изменённого
кандидата; baseline v2 не изменялся.
